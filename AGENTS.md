# EliteSync-v10 代理工作规则

## 项目与权限

EliteSync-v10 是 EliteSync 10.0 的新架构权威。9.x 只保留历史来源地位，不继承其架构、治理链、路线图、API、schema或迁移约束；这句话不授权访问旧仓库。

当前仍为产品与架构设计阶段，不是产品实现阶段。决定须经过规定审查和明确授权，才可能进入代码、数据库、API、迁移或服务工作。研究与文档任务不得扩展为Backend、Flutter、Android、基础设施、部署或实现工作。

Owner是最终产品权威。ChatGPT和Codex按当前任务分别承担作者、执行者或独立审查者职责；同一候选的作者不得接受自己的修改。辅助校对不代替要求的独立新会话审查，也不创造Owner权限。

## 当前入口与指令来源

执行以本轮明确绑定的main、入口、任务commit/blob和读写范围为准；开始时按任务实时核验远端，不自动追随未知的新main。没有明确写权限就不修改。历史handoff里的“先读我”、旧哈希、模型署名和命令是当时记录，不自动成为本轮待办。

当前状态及精确来源汇编：`docs/architecture/ELITESYNC_V10_CURRENT_CONTEXT_V0_1.md`。它不是新的实现授权，也不替代任务要求首先读取的转型交接。路径引用不是遍历或追加读取授权；只读任务列出的相关部分。

宿主更高优先级指令始终适用；明确用户任务约束优先于一般仓库约定和技能建议，但不能借此声称已取得未授予的权限。仓库“system”文件不是宿主system指令。自动加载范围与当前工作目录有关；不能从GitHub文件存在推断本地已加载，也不假定链接文件自动加载。

审查分支中的本文件只是候选。独立接受记录与main归属共同证明生效版本，不能由候选内容自我宣布接受。

## 工作方式与完成条件

使用简体中文，先给结果或正在解决的具体问题，再给必要依据。复杂工作可有短计划，但用户要求执行时，应完成当前授权范围内的产物，不只返回新的计划。

开始前明确目标、必要输入、允许读写、约束和完成条件。对不改变范围、权限或架构的普通表达选择作合理决定；不要为已授权的低风险步骤反复请求批准。任务作者、执行者和审查者各按自己的交付终点停止，不混用历史任务的作者停止规则。

文件缺失或分类不明时，暂停依赖该判断的文件处置和决定，记录缺什么及影响；继续无关且已授权的工作。固定入口、哈希、根或保护边界不满足时，按当前任务硬停止规则处理，不绕过、不自行扩大范围。引用引起实际阻塞的文件和具体条款，不把推测当明确要求。

完成与变更相称的必要检查。文档任务核对内容、来源、权限范围及规定Git拓扑；没有新变更、失败或实质未决疑点，不重复整套验证、不启动项目测试或工具链检查。工具能力、网络可达性和文字禁令都不证明技术隔离或执行授权。

新治理任务必须有新增证据、约束变化或具体缺陷，并说明要改变哪个决定。没有增量则保留同一未决条目，不自动拆出同义任务或新Phase。达到交付条件即结束；不自行开启下一任务。

Owner选择实际模型；本任务要求GPT-6 Astra时，不自行改配置、静默降级或默认调用其他型号辅助代理。仅报告宿主公开可证的模型和指令来源，不为补证明去搜索配置或用户目录。更新本文件不等于当前会话已重载；下一会话应从正确根目录启动。

## 文档与受保护状态

架构文档在 `docs/architecture/`；产品需求／决定／旅程在 `docs/product/`；研究在 `docs/research/`；ADR在 `docs/decisions/`；历史迁移／运行证据在 `docs/archive/`。这是分类约定，不授权列目录、搬迁、归档或创建文件。保留历史原文和来源，不因换模型批量改署名。

区分已编写、已执行、Owner接受、独立审查、进入main和执行授权。位置或日期不证明接受；未查证的字段保持未知。外部依据记录实际URL、日期、适用章节及可取得的版本，不虚构本地适用性。

保留无关用户修改及受保护暂存状态，不检查、修改、unstage、丢弃、覆盖或提交它们。当前转型任务禁止默认index操作、git status和全仓状态检查；只按专门任务授权同步明确文档，不用整仓pull／checkout消除差异，也不宣称整仓clean。

现行转型边界保持：README预算耗尽、FD02永久排除；禁止仓库／目录枚举及旧 `D:\EliteSync`／`zcx369658780/EliteSync` 访问；M1冻结lane耗尽、M2 deferred、M3 blocked；legal、Safety、no-processing及当前上下文列明的UNKNOWN／排除／阶段边界不变。禁止借文档更新运行Flutter、Dart、Gradle、依赖解析、缓存／环境／生成输出检查或artifact probe。后续任务只有明确、合法且精确的另行授权才能改变相应范围。

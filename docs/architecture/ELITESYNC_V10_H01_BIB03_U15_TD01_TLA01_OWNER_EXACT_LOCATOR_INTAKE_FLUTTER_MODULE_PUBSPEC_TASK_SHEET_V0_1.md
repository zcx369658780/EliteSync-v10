# EliteSync v10 H-01 BIB-03 U-15 TD-01 TLA-01 Owner Exact-Locator Intake — Flutter Module Pubspec Task Sheet v0.1

Status: `PROPOSED — TD-01 TLA-01 OWNER EXACT-LOCATOR INTAKE ONLY — OWNER RESTART CONDITION SATISFIED — ZERO EXISTENCE/TYPE/CONTENT CHECK — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-tla01-owner-exact-locator-intake-flutter-module-pubspec-v0-1`

Exact durable live-main authority required at task start:

`65e8c1ea3f685cb97473ac6fc97b0046d528ed11`

This task authorizes one later fresh GPT L3/Codex candidate action for exactly one purpose: intake one explicit Owner-supplied EliteSync-v10 repository-relative candidate locator for TD-01, without checking whether the path exists, without checking its type, without reading its content, and without turning prior old-repository evidence into V10 inheritance authority.

## 1. Memory isolation and permanent boundaries

Global/cross-project memory is intentionally NOT an authority source for EliteSync-v10.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no V10 implementation-content read, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

## 2. Durable restart-gate state

Durable consolidation result at current `main` established:

- TD-01 remains deferred because exact-locator precondition was unsatisfied;
- TD-02 and TD-03 remain not discovered;
- TD-04/05 remain uninspected / separately gated;
- architecture-design entry remains not yet determinable;
- current Lane-A technical-evidence progression is blocked on external exact-locator input, not legal authority;
- no new TLA-01 intake task may be authored unless Owner explicitly supplies one valid exact V10 repository-relative locator for a named TD class and non-design question.

The Owner has now explicitly supplied the following input for this task.

## 3. Frozen Owner input

TD class:

`TD-01 — V10 TOOLCHAIN / BUILD / DEPENDENCY FACTS`

Owner-supplied exact V10 repository-relative candidate locator:

`apps/flutter_elitesync_module/pubspec.yaml`

Named future non-design question:

`WHAT TOOLCHAIN / BUILD / DEPENDENCY FACTS ARE EXPLICITLY PRESENT IN THE CONTENT AT THIS EXACT OWNER-SUPPLIED V10 LOCATOR?`

The Owner explicitly supplied this string as an EliteSync-v10 TLA-01 candidate locator and explicitly preserved:

`OLD ELITESYNC REPOSITORY LOCATOR != ELITESYNC-V10 REPOSITORY LOCATOR`

`OWNER-SUPPLIED V10 LOCATOR != VERIFIED EXISTING V10 PATH`

`OWNER-SUPPLIED V10 LOCATOR != TECHNICAL FACT`

`OWNER-SUPPLIED V10 LOCATOR != ARCHITECTURE CHOICE`

`No claim is made yet that this path exists in EliteSync-v10.`

This Owner input satisfies the restart condition for one locator-intake action only.

It does NOT itself authorize an existence check, a type check, a content read, technical inference, architecture selection, implementation planning, or implementation.

## 4. Exact authorized substantive-content read budget

The later candidate intake action may perform exactly TWO substantive repository-content reads total:

1. this exact task sheet at this exact task-sheet commit;
2. the durable consolidation result below at exact durable `main`.

No third substantive repository-content read is authorized.

Minimum ref/commit metadata required to verify exact authority/topology is allowed only if it does not become tree/path discovery or content inspection.

### Durable consolidation basis

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_LANEA_EXACT_LOCATOR_DEPENDENCY_CONSOLIDATION_GOVERNANCE_RETRY_REVIEW_V0_1.md`

Required blob:

`8c5c8e5065de4232d8ca6d083a31a5627d51cbdc`

No other repository artifact is authorized as substantive basis.

## 5. Sole intake question

Determine ONLY:

`MAY THE EXACT OWNER-SUPPLIED STRING apps/flutter_elitesync_module/pubspec.yaml BE RECORDED AS THE SOLE TLA-01 CANDIDATE LOCATOR FOR TD-01 AND THE NAMED FUTURE NON-DESIGN QUESTION, WHILE PRESERVING THAT INTAKE DOES NOT VERIFY EXISTENCE, TYPE, CONTENT, TECHNICAL FACT, OR ARCHITECTURE SUITABILITY?`

This is locator intake only.

No repository operation may test the locator.

No technical evidence may be read.

## 6. Valid-locator-form check permitted by the frozen string only

The candidate action may determine from the literal Owner-supplied string itself that it is syntactically a single repository-relative file-like locator because it is:

- one string;
- non-empty;
- not absolute;
- not a directory-only string;
- not a basename-only value;
- not a wildcard or glob;
- not an ellipsis or placeholder;
- not a list of alternatives.

This literal-form determination MUST NOT use repository operations.

`VALID LOCATOR FORM != VERIFIED PATH EXISTENCE`

## 7. Outcome A — Owner exact locator intake established

If current authority supports intake, the candidate MUST use exactly this classification:

`U-15 TD-01 TLA-01 OWNER EXACT-LOCATOR INTAKE = OWNER EXPLICITLY SUPPLIED apps/flutter_elitesync_module/pubspec.yaml AS THE SOLE ELITESYNC-V10 REPOSITORY-RELATIVE CANDIDATE LOCATOR FOR TD-01 TOOLCHAIN / BUILD / DEPENDENCY FACT DISCOVERY AND THE NAMED FUTURE NON-DESIGN QUESTION — LOCATOR FORM ACCEPTED FOR INTAKE ONLY — OWNER RESTART CONDITION SATISFIED FOR ONE LATER SEPARATELY AUTHORED EXACT LOCATOR-EXISTENCE TASK — OWNER-SUPPLIED V10 LOCATOR != VERIFIED EXISTING V10 PATH — OWNER-SUPPLIED V10 LOCATOR != TECHNICAL FACT — OWNER-SUPPLIED V10 LOCATOR != ARCHITECTURE CHOICE — OLD ELITESYNC REPOSITORY LOCATOR != ELITESYNC-V10 REPOSITORY LOCATOR — ZERO EXISTENCE CHECK — ZERO TYPE CHECK — ZERO CONTENT READ — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Outcome A means only:

- the exact string is durably recorded as the sole Owner-supplied TLA-01 candidate locator for this TD-01 question;
- a later separately authored exact task may perform a bounded existence check against this exact V10 locator;
- no existence claim is made now;
- no type claim is made now;
- no content claim is made now;
- no toolchain/build/dependency fact is established now;
- no architecture choice or inheritance decision is made now.

Candidate accounting:

- candidate TD-01 TLA-01 Owner exact-locator intake reviews = `1`;
- accepted/durable intake reviews = `0`;
- exact locators tested = `0`;
- existence checks = `0`;
- type checks = `0`;
- technical-evidence content reads = `0`;
- repository search/enumeration/path discovery = `0/0/0`;
- technical-design actions = `0`;
- implementation-planning actions = `0`;
- implementation actions = `0`.

## 8. Outcome B / operational blockage

If the exact Owner input cannot truthfully be recorded under current authority, classify exactly:

`EXPECTED OUTCOME B — U-15 TD-01 TLA-01 OWNER EXACT-LOCATOR INTAKE NOT ESTABLISHED UNDER CURRENT AUTHORITY — FAIL CLOSED — ZERO EXISTENCE/TYPE/CONTENT CHECK — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

For exact main/ref/blob/topology failure use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 TLA-01 OWNER EXACT-LOCATOR INTAKE DETERMINATION MADE`

If `main` is not exactly `65e8c1ea3f685cb97473ac6fc97b0046d528ed11` at task start, STOP before substantive content read except minimum ref metadata needed to detect mismatch.

If the durable consolidation basis blob mismatches, STOP.

If the review branch advances beyond this exact task-sheet commit before candidate publication, fail closed rather than rewriting or substituting history.

## 9. Mandatory zero activity

The candidate intake action MUST perform ZERO:

- existence checking of `apps/flutter_elitesync_module/pubspec.yaml`;
- type checking of that locator;
- content reading of that locator;
- repository-wide or directory-wide enumeration;
- repository-tree inspection;
- directory listing used for discovery;
- filename search;
- code search;
- GitHub code search;
- path discovery;
- guessed conventional filename/path testing;
- alternate-locator testing;
- manifest/config/build/workflow/source locator testing beyond the frozen literal string;
- `git cat-file` object/path testing;
- source repository access;
- old-repository access;
- source-content reads;
- EliteSync-v10 implementation/code/config/build/workflow content reads;
- Backend/API/Database/Flutter/infrastructure/deployment design;
- implementation planning;
- implementation;
- participant/data activity;
- README use;
- FD02 use;
- LC-03;
- LC-04;
- Phase 36.

## 10. Required candidate publication

For Outcome A or Outcome B create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_OWNER_EXACT_LOCATOR_INTAKE_FLUTTER_MODULE_PUBSPEC_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_OWNER_EXACT_LOCATOR_INTAKE_FLUTTER_MODULE_PUBSPEC_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: record H-01 BIB-03 U-15 TD-01 TLA-01 owner exact locator intake`

Publish non-force only to:

`review/h01-bib03-u15-td01-tla01-owner-exact-locator-intake-flutter-module-pubspec-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent ACCEPT/REJECT.

## 11. Strict phase separation

Task-sheet authoring -> STOP.

Candidate locator intake -> STOP.

Acceptance/promotion -> STOP.

Exact locator existence check -> separately authored later task.

Any type check -> separately authorized only if specifically needed.

Any content read -> separately authored later task after a truthful locator-existence result.

Technical design -> separately authorized later task.

Implementation planning / implementation -> separately authorized later task.

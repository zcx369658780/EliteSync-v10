# EliteSync v10 H-01 BIB-03 Manifest ML-1 Identity Execution Task Sheet v0.4

## 1. Purpose and publication non-authority

This document defines exactly one possible later execution:

`H-01 BIB-03 MANIFEST-GUIDED STAGE ML-1 IDENTITY EXECUTION V0.4 — FINAL CURRENT CONVENTION-MANIFEST EXACT IDENTITY HYPOTHESIS — NO CONTENT READ`

Repository:

`zcx369658780/EliteSync-v10`

Publication parent authority:

`d5b3de5994c63eebcb0dd73fece1b4cfc2521a2d`

The accepted prior ML-1 negative facts are preserved:

`4ab7e22ee97dbbe726663fbd9dbe517be40deeb5:pubspec.yaml — MISS`

`fa54179aad8a79ca435d46461fb496c62a9f9a77:client/pubspec.yaml — MISS`

Preserve:

`ROOT PUBSPEC MISS != NO MANIFEST EXISTS ELSEWHERE`

`CLIENT MANIFEST MISS != CLIENT DIRECTORY ABSENCE`

`TWO MANIFEST HYPOTHESIS MISSES != NO FLUTTER/DART PACKAGE EXISTS`

`TWO MANIFEST HYPOTHESIS MISSES != TP-SOURCE-CLASS-01 EMPTY`

The historical nonconforming v0.1 execution commit remains unaccepted and non-promotable:

`b9a88c2d2eb7fd29caaa1243c23ab7868f989d41`

Publication of this task sheet does NOT execute ML-1 and does NOT authorize:

- manifest content reads;
- YAML parsing;
- package-root identification;
- Stage ML-2;
- source-path hypotheses;
- source-content reads;
- technical/config read budget;
- candidate source identification;
- candidate acceptance;
- bounded basis-instance acceptance;
- source-target binding;
- inspection;
- evidence collection;
- U-15 fact resolution;
- PUI reconsideration;
- design;
- implementation;
- Safety Operations;
- legal research;
- LC-03;
- LC-04;
- Phase 36.

## 2. Controlling accepted method authority

Controlling accepted method review:

`docs/architecture/ELITESYNC_V10_H01_BIB03_MANIFEST_GUIDED_BOUNDED_LOCATOR_METHOD_REVIEW_V0_1.md`

Accepted method-review Git blob:

`bba1f5d978d2dba9e240625ffe08c0d4ba40e3bb`

Controlling method model:

`BIB03-METHOD-02 — MANIFEST-GUIDED BOUNDED LOCATOR METHOD — STAGED EXACT-MANIFEST IDENTITY THEN SEPARATELY AUTHORIZED MINIMAL MANIFEST METADATA READ — NON-ENUMERATIVE — FAIL CLOSED`

Preserve:

`METHOD MODEL ACCEPTED != METHOD EXECUTED`

`MANIFEST IDENTITY ESTABLISHED != MANIFEST CONTENT READ AUTHORIZED`

## 3. Exact future execution contract

Future execution artifact:

`docs/architecture/ELITESYNC_V10_H01_BIB03_MANIFEST_ML1_IDENTITY_EXECUTION_V0_4.md`

Future execution branch:

`review/h01-bib03-manifest-ml1-identity-execution-v0.4`

Future exact commit subject:

`docs: execute H-01 BIB-03 manifest ML-1 workspace identity probe v0.4`

The future execution branch must be created exactly from this task-sheet publication commit.

Exactly one changed path is permitted: the v0.4 execution artifact above.

Do not promote `main` during execution.

## 4. Primary-agent-only execution rule

For this invocation:

`NO HELPER OR SUB-AGENT MAY EXECUTE THE PROBE, MODIFY THE WORKTREE, CREATE/EDIT THE EXECUTION ARTIFACT, COMMIT, OR PUSH`

The primary execution agent must perform the probe and Git mutations itself.

Preserve:

`READ-ONLY REVIEW ROLE != EXECUTION AUTHORITY`

`INTERNAL DELEGATION MUST NOT EXPAND OWNER-DELEGATED AUTHORITY`

Any helper/sub-agent probe/write/commit/push is:

`OPERATIONAL BLOCKED — EXECUTION PROVENANCE VIOLATION`

## 5. Protected-path and non-enumeration rule

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

Do NOT use or authorize:

- `rg --files`;
- `find`;
- `fd`;
- `git ls-files`;
- recursive `ls`, `dir`, or `tree`;
- repository/directory path globs;
- GitHub code search;
- broad filename search;
- `git grep`;
- repository source discovery;
- directory source discovery;
- neighboring-directory inspection.

FD02 remains protected and excluded.

Do not search for, reveal, open, read, hash, re-hash, inspect, cite, select, allowlist, modify, move, delete, stage, commit, or use FD02.

Do not independently re-prove its presence or uniqueness.

Historical incident remains historical only:

`PROTECTED-PATH ENUMERATION EXPOSURE — NO CONTENT ACCESS — STAGE A VOID`

## 6. Exact immutable ML-1 probe baseline

All ML-1 identity operations under this task sheet are pinned to exactly:

`d5b3de5994c63eebcb0dd73fece1b4cfc2521a2d`

Do not substitute the task-sheet publication commit or any later commit.

Preserve:

`ML-1 PROBE COMMIT != ML-1 TASK-SHEET PUBLICATION COMMIT`

The baseline is used only for exact manifest-object identity testing.

## 7. Exactly one final current convention-manifest hypothesis

Instantiate exactly:

### ML1-HYP-03

Origin class:

`MANIFEST-ORIGIN-01 — DETERMINISTIC STANDARD MANIFEST EXACT-PATH HYPOTHESIS PREDECLARED IN A SEPARATE EXECUTION TASK SHEET`

Exact manifest path hypothesis:

`melos.yaml`

Exact object hypothesis:

`d5b3de5994c63eebcb0dd73fece1b4cfc2521a2d:melos.yaml`

Rationale is limited to deterministic Flutter/Dart monorepo/workspace convention. This does NOT establish that the repository uses Melos, that the path exists, that any package exists, that the manifest is relevant to TP-TARGET-01, or that it identifies a unique package root.

This is the final current `MANIFEST-ORIGIN-01` convention-manifest hypothesis authorized by the present bounded sequence.

No second manifest hypothesis is instantiated.

No fallback manifest hypothesis is authorized.

No nested path.

No path mutation.

No source-path hypothesis.

Preserve:

`MANIFEST PATH HYPOTHESIS != MANIFEST EXISTS`

`MANIFEST EXISTS != MANIFEST RELEVANT`

`MANIFEST EXISTS != PACKAGE ROOT ACCEPTED`

`WORKSPACE MANIFEST IDENTITY != UNIQUE PACKAGE ROOT`

`FINAL CURRENT CONVENTION HYPOTHESIS != BIB03-METHOD-02 EXHAUSTED`

## 8. Exact future ML-1 identity probe

A separately authorized execution may perform exactly one logical identity probe against:

`d5b3de5994c63eebcb0dd73fece1b4cfc2521a2d:melos.yaml`

Allowed operation:

`git cat-file -e d5b3de5994c63eebcb0dd73fece1b4cfc2521a2d:melos.yaml`

If and only if existence is established:

`git rev-parse d5b3de5994c63eebcb0dd73fece1b4cfc2521a2d:melos.yaml`

and, if needed:

`git cat-file -t <resolved-object-sha>`

The resolved object must be a Git `blob`.

ML-1 may establish only:

1. exact path existence;
2. Git object type;
3. Git blob SHA.

Do NOT emit or inspect manifest bytes.

Do NOT use:

- `git show` on the manifest;
- `git cat-file -p`;
- `git cat-file blob`;
- manifest-content diff;
- `git grep`;
- YAML parsing;
- package/dependency/workspace parsing;
- line counting;
- any command or API returning manifest content.

Preserve:

`MANIFEST IDENTITY PROBE != MANIFEST READ`

`MANIFEST IDENTITY PROBE != SOURCE INSPECTION`

`MANIFEST PROBE BUDGET != TECHNICAL/SOURCE READ BUDGET`

## 9. Manifest identity output rule

If and only if ML1-HYP-03 resolves to exactly one Git blob, emit exactly one manifest identity:

`zcx369658780/EliteSync-v10@d5b3de5994c63eebcb0dd73fece1b4cfc2521a2d:melos.yaml#blob=<resolved-git-blob-sha>`

It is only a:

`MANIFEST IDENTITY`

It is NOT:

- manifest content;
- a package-root locator candidate;
- a unique package-root identity;
- a source identifier;
- candidate source acceptance;
- a bounded basis instance;
- a source-target binding;
- inspection authority;
- ML-2 authority.

Preserve:

`MANIFEST IDENTITY != MANIFEST CONTENT`

`WORKSPACE MANIFEST IDENTITY != PACKAGE-ROOT LOCATOR CANDIDATE`

`WORKSPACE MANIFEST IDENTITY != SOURCE IDENTIFIER`

`MANIFEST IDENTITY != ML-2 READ AUTHORITY`

## 10. Outcome A — one workspace manifest identity

If the exact path resolves to one Git blob:

- emit exactly one canonical manifest identity;
- do not read content;
- do not infer workspace package paths;
- do not infer a unique package root;
- do not enter ML-2 in this invocation.

Terminal label:

`EXPECTED OUTCOME A`

Terminal statement:

`H-01 BIB-03 STAGE ML-1 V0.4 PRODUCED ONE WORKSPACE-MANIFEST IDENTITY — NO MANIFEST CONTENT READ — ML-2 REMAINS SEPARATELY GATED — NO PACKAGE-ROOT LOCATOR, SOURCE IDENTIFIER, BINDING, INSPECTION, EVIDENCE, PUI, DESIGN, OR IMPLEMENTATION AUTHORITY`

## 11. Outcome B — final current convention-manifest MISS

If the exact path misses, cannot be resolved, is not a Git blob, or cannot be resolved without content exposure:

- emit no manifest identity;
- do not try another manifest path;
- do not mutate the hypothesis;
- do not infer a package root;
- do not search for manifests;
- do not inspect neighboring paths;
- do not enter ML-2.

Terminal label:

`EXPECTED OUTCOME B`

Terminal statement:

`H-01 BIB-03 STAGE ML-1 V0.4 PRODUCED NO MANIFEST IDENTITY — CURRENT MANIFEST-ORIGIN-01 CONVENTION SEQUENCE CLOSED FAIL-CLOSED — NO FALLBACK MANIFEST SEARCH OR ADDITIONAL CONVENTION-MANIFEST HYPOTHESIS AUTHORIZED`

Preserve:

`THREE CONVENTION-MANIFEST MISSES != NO MANIFEST EXISTS ELSEWHERE`

`THREE CONVENTION-MANIFEST MISSES != NO FLUTTER/DART PACKAGE EXISTS`

`THREE CONVENTION-MANIFEST MISSES != TP-SOURCE-CLASS-01 EMPTY`

`CURRENT MANIFEST-ORIGIN-01 CONVENTION SEQUENCE CLOSED != BIB03-METHOD-02 EXHAUSTED`

`CURRENT MANIFEST-ORIGIN-01 CONVENTION SEQUENCE CLOSED != AUTHORITY TO SEARCH`

After a conforming Outcome B and later acceptance/promotion, no additional `MANIFEST-ORIGIN-01` convention-manifest hypothesis successor is established by the current sequence. Any future identification successor requires a separately justified governance basis or another already-accepted origin class; it may not be treated as an automatic fourth convention guess.

## 12. Stage ML-2 remains separately gated

Even if exactly one manifest identity is emitted, Stage ML-2 remains unauthorized.

Preserve exactly:

`MANIFEST READ IS A TECHNICAL/CONFIG READ`

`MANIFEST METADATA-ONLY SEMANTICS != ZERO-BYTE CONTENT READ`

`MANIFEST IDENTITY ESTABLISHED != MANIFEST CONTENT READ AUTHORIZED`

`ANY FUTURE STAGE ML-2 MANIFEST CONTENT READ REQUIRES A SEPARATE TASK SHEET, EXPLICIT TECHNICAL/CONFIG READ BUDGET, AND INDEPENDENT GPT L3 REVIEW`

No ML-2 read budget is selected.

## 13. Frozen project state

Preserve:

`TP-SOURCE-CLASS-01`

as the sole abstract source-class envelope.

Preserve:

`TP-TARGET-01`

as the sole evidence-subject target.

Preserve:

`CURRENT ACCEPTED BOUNDED CONCRETE IDENTIFICATION BASIS INSTANCES = 0`

Preserve:

`U-12 accepted concrete targets = 0`

All three PUI entries remain deferred.

Preserve:

`PUI-PREREQ-12 threshold satisfaction = 0`

U-10 remains `UNRESOLVED`.

U-14 exclusion remains unchanged.

U-15 fact values remain unresolved.

Preserve exactly:

`D02-DURABLE-UNKNOWN-01`

D-02 remains `UNRESOLVED` and is not reopened.

Backend remains:

`0/10 NOT INSPECTED / NOT AUTHORIZED`

Database remains:

`0/8 NOT INSPECTED / NOT AUTHORIZED`

Cumulative accepted technical/source-content reads remain exactly:

`10`

Identity-only ML-1 does not increment technical/source-content reads.

## 14. Mandatory complete execution counter ledger

The future v0.4 execution artifact MUST explicitly contain every line below:

- manifest-guided method executions = `1` if probe attempted;
- Stage ML-1 executions = `1` if probe attempted;
- Stage ML-2 executions = `0`;
- manifest-path hypotheses instantiated = exactly `1`;
- manifest identity probes executed = exactly `1` if attempted;
- manifest hits = `0` or `1`;
- manifest identities emitted = `0` or `1`;
- manifest content reads = `0`;
- runtime/test/build/config/manifest reads = `0`;
- package-root locator candidates emitted = `0`;
- package-root locator candidates accepted = `0`;
- source-path hypotheses created = `0`;
- candidate source identifiers emitted = `0`;
- candidate source identifiers accepted = `0`;
- bounded basis instances accepted = `0`;
- concrete source-target bindings = `0`;
- repository-source metadata passes = `0` beyond the exact authorized manifest identity probe;
- repository-wide source discovery = `0`;
- directory-wide source discovery = `0`;
- technical-source reads = `0`;
- source-content reads = `0`;
- Flutter/client source reads = `0`;
- technical/platform inspection actions = `0`;
- future technical/source read budgets selected = `0`;
- evidence added/collected = `0`;
- U-15 fact values resolved = `0`;
- D-02 reopen actions = `0`;
- PUI-ENTRY verdict changes = `0`;
- PUI-ENTRY reconsideration actions = `0`;
- PUI-PREREQ-12 threshold satisfactions claimed = `0`;
- U-12 concrete targets added = `0`;
- technical-design decisions = `0`;
- implementation actions = `0`;
- telemetry/analytics/measurement operations = `0`;
- Safety Operations = `0`;
- legal-research operations = `0`;
- LC-03 / LC-04 / Phase 36 actions = `0 / 0 / 0`;
- DeepSeek calls = `0`;
- cumulative accepted technical/source-content reads = exactly `10`;
- current-execution repository/directory path-enumeration exposure = `0`;
- current-execution FD02 search/access = `0 / 0`;
- helper/sub-agent probe/write/commit/push actions = `0`.

Any required ledger omission before commit is:

`OPERATIONAL BLOCKED — EXECUTION ARTIFACT INCOMPLETE`

## 15. Mandatory artifact authority fields

The artifact must accurately record:

Task-sheet publication commit: the exact commit that publishes this v0.4 task sheet.

Task-sheet path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_MANIFEST_ML1_IDENTITY_EXECUTION_TASK_SHEET_V0_4.md`

Immutable probe baseline:

`d5b3de5994c63eebcb0dd73fece1b4cfc2521a2d`

Method-review path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_MANIFEST_GUIDED_BOUNDED_LOCATOR_METHOD_REVIEW_V0_1.md`

Method-review blob:

`bba1f5d978d2dba9e240625ffe08c0d4ba40e3bb`

After publication, the exact task-sheet Git blob must be supplied by the execution authority and recorded accurately.

Any mismatch before commit is:

`OPERATIONAL BLOCKED — PUBLICATION-AUTHORITY RECORD MISMATCH`

## 16. Future publication and execution stop

Publication of this task sheet creates no probe-execution authority by itself.

`H-01 BIB-03 ML-1 IDENTITY EXECUTION TASK SHEET V0.4 PUBLISHED — FINAL CURRENT MANIFEST-ORIGIN-01 CONVENTION HYPOTHESIS PREDECLARED — NO MANIFEST PROBE OR CONTENT READ AUTHORITY CREATED BY PUBLICATION`

STOP AFTER ML-1 V0.4 TASK-SHEET PUBLICATION — BEFORE MANIFEST IDENTITY PROBE, MANIFEST READ, ML-2, PACKAGE-ROOT LOCATOR IDENTIFICATION, SOURCE HYPOTHESIS, CANDIDATE ACCEPTANCE, BASIS-INSTANCE ACCEPTANCE, SOURCE-TARGET BINDING, INSPECTION, READ-BUDGET AUTHORIZATION, EVIDENCE COLLECTION, PUI RECONSIDERATION, DESIGN, OR IMPLEMENTATION

# EliteSync v10 H-01 BIB-03 U-15 TD-01 Settings-Level Fact Sufficiency and Next-Gap Disposition Governance Task Sheet v0.1

Status: `PROPOSED — TD-01 SETTINGS-LEVEL FACT SUFFICIENCY / NEXT-GAP DISPOSITION GOVERNANCE ONLY — ZERO NEW TECHNICAL CONTENT READ — ZERO TARGET REREAD — ZERO LOCATOR ACQUISITION — ZERO DEPENDENCY/BUILD EXECUTION — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-settings-level-fact-sufficiency-next-gap-disposition-v0-1`

Exact durable live-main authority required at task start:

`841d31e353a634b1a44f7c7aa16fe7a35ad40883`

This task authorizes one later fresh GPT L3/Codex governance candidate action for exactly one purpose: determine what the now-durable revision-scoped app-level plus settings-level host/native integration declaration facts establish for TD-01, whether the current bounded TD-01 fact set is sufficient for architecture-entry determination, what material facts remain unestablished, and, if additional evidence is still required, select exactly one next bounded TD-01 evidence-gap class without acquiring a locator, running Gradle, resolving dependencies, testing artifact availability, reading any new technical target, or making technical-design or implementation decisions.

## 1. Permanent boundaries

Global/cross-project memory is NOT EliteSync-v10 governance authority.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no new V10 technical-target read, no dependency-resolution execution, no Gradle/build execution, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

Historical incidents, rejected candidates, stale tasks, and deviations remain historical truth and must not be rewritten as if they did not occur.

Preserve:

`PREMATURELY AUTHORED STALE TASK != CURRENT GOVERNANCE AUTHORITY`

The stale task `ad84587a8bd0d7fb5e373417aa94b33be936b58c` remains stale and not current authority.

The rejected existence-result candidate `701e0ad03b5fc22694d8fcef18250580e4549e82` remains rejected and must not be promoted, reused, amended, rebased, cherry-picked, merged, or used as substantive basis.

## 2. Exact durable basis A — settings-level repository-registration fact register

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_SETTINGS_LEVEL_FLUTTER_AAR_REPOSITORY_REGISTRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Required blob:

`9ad03334b39779879124c4463d70b684480fc625`

Exact revision-scoped technical source represented by this durable fact register:

`b019b10e795080710d8486469202939120ef95ee:apps/android/settings.gradle.kts`

Preserve from Basis A only at that exact revision+locator source:

- `dependencyResolutionManagement` declares `RepositoriesMode.PREFER_SETTINGS`;
- dependency-resolution repositories declare the local Flutter-module host-output Maven source `../flutter_elitesync_module/build/host/outputs/repo`;
- Flutter storage base selection is declared from non-empty `FLUTTER_STORAGE_BASE_URL`, otherwise `https://storage.flutter-io.cn`;
- the settings file declares `$flutterStorageBaseUrl/download.flutter.io` with `includeGroup("io.flutter")`;
- Aliyun google/public/releases dependency repositories declare `excludeGroup("io.flutter")`;
- generic dependency repositories include `google()`, `mavenCentral()`, and `https://jitpack.io`;
- a conditional `D:/EliteSync/gradle-local-m2` Maven source is declared in both plugin-management and dependency-resolution repository blocks when the OS-name condition and local-path-existence condition hold, with `mavenPom()` and `artifact()` metadata sources;
- plugin-management repositories and Android/Kotlin plugin-resolution mappings are explicitly declared, while not themselves establishing Flutter AAR dependency resolution.

Preserve exactly:

`SETTINGS-LEVEL REPOSITORY DECLARATION != DEPENDENCY RESOLUTION SUCCESS`

`DECLARED REPOSITORY SOURCE != ARTIFACT AVAILABILITY`

`DECLARED CONTENT FILTER != RESOLUTION SUCCESS`

`SINGLE SETTINGS FILE CONTENT FACT != COMPLETE HOST BUILD GRAPH`

`REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT`

`CONTENT FACT != ARCHITECTURE CHOICE`

`CONTENT FACT != INHERITANCE DECISION`

## 3. Exact durable basis B — prior app-level host/native sufficiency disposition

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_HOST_NATIVE_SINGLE_FILE_SUFFICIENCY_AND_NEXT_GAP_DISPOSITION_GOVERNANCE_RETRY_REVIEW_V0_1.md`

Required blob:

`d94e379651f9b009b38ccdcfc99c94a5fdf97a96`

Preserve from Basis B:

- TD-01 remained `PARTIALLY DISCOVERED`;
- app-level host/native integration had materially advanced at revision-scoped single-file declaration level;
- declared Flutter module reference, Flutter SDK/executable selection logic relevant to AAR generation, Flutter AAR-generation task, `preBuild -> syncFlutterAar` task dependency, AAR output location, and debug/profile/release Flutter AAR dependency coordinates were established for the exact authorized app-build source only;
- settings-level repository registration / dependency-repository source was the then-next priority evidence-gap class;
- actual AAR generation success, actual dependency resolution / artifact availability, installed toolchain state, and build-success/workflow facts remained unestablished;
- current TD-01 facts were not yet sufficient for architecture-entry determination.

Preserve exactly:

`DECLARED AAR GENERATION TASK != AAR BUILD SUCCESS`

`DECLARED AAR DEPENDENCY != RESOLVED AAR DEPENDENCY`

`SINGLE FILE CONTENT FACT != COMPLETE HOST BUILD GRAPH`

`PARTIALLY DISCOVERED != ARCHITECTURE-ENTRY SUFFICIENT`

`FACT SUFFICIENCY DETERMINATION != ARCHITECTURE DESIGN`

## 4. Exact authorized substantive-content read budget

Exactly THREE substantive repository-content reads are authorized for the later candidate action:

1. this exact task sheet;
2. Basis A at required blob `9ad03334b39779879124c4463d70b684480fc625`;
3. Basis B at required blob `d94e379651f9b009b38ccdcfc99c94a5fdf97a96`.

No fourth substantive repository-content read is authorized.

Do NOT reread:

- `apps/android/settings.gradle.kts`;
- `apps/android/app/build.gradle.kts`;
- `.metadata`;
- `pubspec.yaml`;
- `pubspec.lock`;
- AndroidManifest;
- Kotlin/Java source;
- CI/workflows;
- any other Gradle/settings/native-build file;
- README;
- FD02;
- source repository;
- old repository.

Minimum exact ref/commit/compare metadata required to verify current authority/topology is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 5. Sole governance question

Determine ONLY:

`GIVEN THE DURABLE APP-LEVEL AAR GENERATION/CONSUMPTION DECLARATIONS AND THE DURABLE SETTINGS-LEVEL REPOSITORY-REGISTRATION / DEPENDENCY-REPOSITORY-SOURCE DECLARATIONS, WHAT TD-01 HOST/NATIVE BUILD-INTEGRATION FACTS ARE NOW ESTABLISHED AT REVISION-SCOPED DECLARATION LEVEL, WHAT MATERIAL FACTS REMAIN UNESTABLISHED, ARE CURRENT TD-01 FACTS SUFFICIENT FOR ARCHITECTURE-ENTRY DETERMINATION, AND, IF NOT, WHAT SINGLE NEXT BOUNDED TD-01 EVIDENCE-GAP CLASS SHOULD BE PRIORITIZED WITHOUT ACQUIRING OR EXECUTING ANY EVIDENCE METHOD?`

This task does not authorize architecture selection.

This task does not authorize any new technical evidence collection.

This task does not authorize any command, build, dependency-resolution, artifact test, locator acquisition, or content read.

## 6. Authorized sufficiency dimensions

### A. Declared host/native integration chain now established

The candidate may determine whether the combined durable basis establishes, at revision-scoped declaration level only:

- an app-level Flutter module reference;
- an app-level Flutter AAR generation task and declared output path;
- an app-level `preBuild` dependency on the AAR-generation task;
- app-level debug/profile/release Flutter AAR coordinates;
- settings-level preference for settings repositories;
- a settings-level local Maven source corresponding to the Flutter module host-output repository;
- a settings-level remote Flutter artifact repository expression;
- explicit `io.flutter` include/exclude content filters relevant to repository selection;
- a declared app-level generation/consumption path plus settings-level repository-source path that together form a materially more complete declared host/native integration chain.

Do not infer actual task execution, artifact generation, repository reachability, artifact availability, or dependency resolution.

### B. Material facts that remain unestablished

The candidate may identify only bounded evidence-gap classes, including:

- actual Flutter AAR generation success;
- actual Flutter AAR artifact availability in a declared repository source;
- actual dependency-resolution state for the declared app-level Flutter AAR coordinates;
- installed toolchain state;
- build-success / workflow facts;
- runtime host/embedding facts only if later needed, without prioritizing them unless the bounded basis supports that priority.

### C. Architecture-entry sufficiency

Determine only whether the current TD-01 facts are sufficient for architecture-entry determination.

Preserve:

`DECLARED BUILD-INTEGRATION CHAIN != EXECUTED BUILD-INTEGRATION CHAIN`

`DECLARED RESOLUTION PATH != SUCCESSFUL DEPENDENCY RESOLUTION`

`DECLARED ARTIFACT OUTPUT PATH != ARTIFACT AVAILABILITY`

`FACT SUFFICIENCY DETERMINATION != ARCHITECTURE DESIGN`

### D. Next bounded evidence-gap priority

If more evidence is required, select exactly ONE next evidence-gap class.

Expected next priority:

`ACTUAL FLUTTER AAR ARTIFACT AVAILABILITY / DEPENDENCY-RESOLUTION STATE`

Rationale boundary: the current durable facts now establish a materially coherent declared chain from Flutter AAR generation/output and app-level coordinate consumption to settings-level repository registration/filter policy. The next unresolved question is no longer whether such declaration paths exist, but whether the declared AAR artifact is actually available and the declared coordinates actually resolve.

This expected priority is an evidence-gap class only.

Preserve exactly:

`EVIDENCE-GAP PRIORITY != EXECUTION AUTHORITY`

`EVIDENCE-GAP PRIORITY != PERMISSION TO RUN GRADLE`

`EVIDENCE-GAP PRIORITY != PERMISSION TO RESOLVE DEPENDENCIES`

`EVIDENCE-GAP PRIORITY != LOCATOR`

`EVIDENCE-GAP PRIORITY != PERMISSION TO SEARCH`

`ARTIFACT-AVAILABILITY / RESOLUTION EVIDENCE CLASS != AUTHORIZED TEST METHOD`

No command, Gradle task, network test, repository reachability check, artifact lookup, cache inspection, locator, or execution mechanism may be selected or authorized by this task.

## 7. Expected Outcome A

If the exact durable basis supports the expected disposition, use exactly:

`U-15 TD-01 SETTINGS-LEVEL FACT SUFFICIENCY / NEXT-GAP DISPOSITION = TD-01 REMAINS PARTIALLY DISCOVERED BUT HOST / NATIVE BUILD INTEGRATION HAS MATERIALLY ADVANCED FROM APP-LEVEL SINGLE-FILE DECLARATIONS TO A REVISION-SCOPED DECLARED APP + SETTINGS RESOLUTION CHAIN — APP-LEVEL FLUTTER MODULE REFERENCE, DECLARED AAR GENERATION TASK / OUTPUT, DECLARED PREBUILD -> AAR-GENERATION TASK EDGE, AND DEBUG / PROFILE / RELEASE FLUTTER AAR COORDINATES ARE JOINED BY SETTINGS-LEVEL PREFER_SETTINGS POLICY, LOCAL FLUTTER-MODULE HOST-OUTPUT MAVEN SOURCE, DECLARED FLUTTER ARTIFACT REPOSITORY EXPRESSION, AND io.flutter INCLUDE/EXCLUDE CONTENT-FILTER DECLARATIONS — PRIOR SETTINGS-LEVEL FLUTTER AAR REPOSITORY-REGISTRATION EVIDENCE GAP IS SATISFIED AT REVISION-SCOPED DECLARATION LEVEL ONLY — ACTUAL AAR GENERATION SUCCESS, ACTUAL FLUTTER AAR ARTIFACT AVAILABILITY, ACTUAL DEPENDENCY-RESOLUTION STATE, INSTALLED TOOLCHAIN STATE, AND BUILD-SUCCESS / WORKFLOW FACTS REMAIN UNESTABLISHED — CURRENT TD-01 FACTS ARE NOT YET SUFFICIENT FOR ARCHITECTURE-ENTRY DETERMINATION — NEXT PRIORITY BOUNDED TD-01 EVIDENCE-GAP CLASS = ACTUAL FLUTTER AAR ARTIFACT AVAILABILITY / DEPENDENCY-RESOLUTION STATE — NO EVIDENCE METHOD / COMMAND / LOCATOR / EXECUTION AUTHORIZED — ZERO NEW TECHNICAL CONTENT READ — ZERO TARGET REREAD — ZERO DEPENDENCY/BUILD EXECUTION — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Outcome A means only:

- the prior settings-level declaration evidence gap is satisfied at the exact revision-scoped declaration level represented by Basis A;
- combined app-level and settings-level facts establish a materially more complete declared build-integration/resolution chain;
- the chain remains declarative, not executed or empirically validated;
- actual AAR generation, artifact availability, dependency resolution, installed toolchain, and build-success/workflow remain unestablished;
- architecture-entry remains not yet determinable from current TD-01 facts;
- the single next evidence-gap class is `ACTUAL FLUTTER AAR ARTIFACT AVAILABILITY / DEPENDENCY-RESOLUTION STATE`;
- no evidence method, command, locator, Gradle task, resolution attempt, artifact check, technical design, or implementation is authorized.

Preserve exactly:

`DECLARED BUILD-INTEGRATION CHAIN != EXECUTED BUILD-INTEGRATION CHAIN`

`DECLARED RESOLUTION PATH != SUCCESSFUL DEPENDENCY RESOLUTION`

`DECLARED ARTIFACT OUTPUT PATH != ARTIFACT AVAILABILITY`

`PARTIALLY DISCOVERED != ARCHITECTURE-ENTRY SUFFICIENT`

`EVIDENCE-GAP PRIORITY != EXECUTION AUTHORITY`

`EVIDENCE-GAP PRIORITY != PERMISSION TO RUN GRADLE`

`EVIDENCE-GAP PRIORITY != PERMISSION TO RESOLVE DEPENDENCIES`

`ARTIFACT-AVAILABILITY / RESOLUTION EVIDENCE CLASS != AUTHORIZED TEST METHOD`

`FACT SUFFICIENCY DETERMINATION != ARCHITECTURE DESIGN`

## 8. Outcome B / operational blockage

If the current durable basis does not support the expected next-priority disposition, use exactly:

`EXPECTED OUTCOME B — U-15 TD-01 SETTINGS-LEVEL FACT SUFFICIENCY / NEXT-GAP DISPOSITION DOES NOT SUPPORT ACTUAL FLUTTER AAR ARTIFACT AVAILABILITY / DEPENDENCY-RESOLUTION STATE AS THE NEXT PRIORITY UNDER CURRENT AUTHORITY — FAIL CLOSED — NO EVIDENCE METHOD / COMMAND / LOCATOR / EXECUTION AUTHORIZED — ZERO NEW TECHNICAL CONTENT READ — ZERO TARGET REREAD — ZERO DEPENDENCY/BUILD EXECUTION — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

For exact main/ref/blob/topology failure use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 SETTINGS-LEVEL FACT SUFFICIENCY / NEXT-GAP DISPOSITION DETERMINATION MADE`

If `main` is not exactly `841d31e353a634b1a44f7c7aa16fe7a35ad40883` at task start, STOP before substantive content read except minimum ref metadata needed to detect mismatch.

If the review branch advances beyond this exact task-sheet commit before candidate publication, fail closed rather than rewriting or substituting history.

## 9. Mandatory zero activity

The candidate action MUST perform ZERO:

- new technical target reads;
- target rereads;
- Gradle/build task execution;
- dependency-resolution execution;
- artifact availability tests;
- repository/network reachability tests;
- cache inspection;
- generated-output inspection;
- new locator acquisition/intake;
- existence/type checks;
- alternate locator/revision testing;
- repository-wide or directory-wide enumeration;
- repository-tree inspection;
- directory listing used for discovery;
- filename search;
- code search;
- GitHub code search;
- path discovery;
- source repository access;
- old-repository access;
- README use;
- FD02 use;
- architecture selection;
- Backend/API/Database/Flutter/infrastructure/deployment design;
- implementation planning;
- implementation;
- participant/data activity;
- LC-03;
- LC-04;
- Phase 36.

## 10. Required candidate publication

For Outcome A or Outcome B create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_SETTINGS_LEVEL_FACT_SUFFICIENCY_AND_NEXT_GAP_DISPOSITION_GOVERNANCE_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_SETTINGS_LEVEL_FACT_SUFFICIENCY_AND_NEXT_GAP_DISPOSITION_GOVERNANCE_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: review H-01 BIB-03 U-15 TD-01 settings fact sufficiency disposition`

Publish non-force only to:

`review/h01-bib03-u15-td01-settings-level-fact-sufficiency-next-gap-disposition-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent `ACCEPT / REJECT`.

## 11. Strict phase separation

Task-sheet authoring -> STOP.

Candidate fact sufficiency / next-gap disposition -> STOP.

Fresh GPT L3 ACCEPT/REJECT -> STOP.

If accepted, normal non-force promotion -> separate acceptance action.

Any future artifact-availability/dependency-resolution evidence-method selection -> separately authored governance task.

Any actual dependency-resolution/build/artifact test -> separately authorized only after its method and side-effect boundaries are governed.

Architecture-entry determination -> separately authorized later governance task after sufficient bounded technical facts.

Technical design -> separately authorized later task.

Implementation planning / implementation -> separately authorized later task.
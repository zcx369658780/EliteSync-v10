# EliteSync v10｜H01-BIB03-U15-TD01 M3 Post-R7 B12 DEP13 Local-State Review Task v0.1

Status: `TASK AUTHORIZED — EXACT 13-COORDINATE GUEST-LOCAL-STATE REVIEW ONLY — NO SOURCE/OBJECT RESEARCH — NO ACQUISITION — NO GRADLE/M3 — NO M2`

Date: 2026-09-11

Repository: `zcx369658780/EliteSync-v10`

## 1. Purpose

Perform one bounded, read-only guest-local Gradle cache-state review for the 13 unique actual dependency targets identified by the accepted Atomic B12 Scope Review.

This task exists only to establish whether each exact target is already locally available, incomplete, absent, or not determinable within the authorized read scope before any later production source/object-contract task is considered.

This task does **not** authorize source/object research, Maven/network access, artifact acquisition, persistence, cache mutation, local Maven repository creation, Gradle execution, M3, AAR probing, or M2.

`LOCAL-STATE REVIEW != SOURCE-READ AUTHORITY`

`LOCAL-STATE REVIEW != ACQUISITION AUTHORITY`

## 2. Mandatory repository startup gate

Fresh-fetch GitHub `main` first.

Require exact current durable authority:

`47cc470e3b9d6344bd03b8f0f9a8acf82b8fc81b`

Require exact tree:

`9a6e6e2635a78ed26c3a5ec6dcf1571b579feeeb`

Read FIRST:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M3_POST_R7_ATOMIC_B12_SCOPE_REVIEW_ACCEPTANCE_V0_1.md`

Require FIRST blob:

`129734f57a50363a5af8aae545665af6b6d1a2da`

Also preserve the prior accepted handoff/FIRST chain, including fixed transition FIRST blob:

`18485c95262051e43892436fb52413132c5ee626`

Conversation text and memory are context/navigation only and are not execution authority.

If any required repository/FIRST identity gate fails, stop before Sandbox or guest access and report the exact mismatch.

## 3. Accepted decision basis

Do not reopen B10, B11, B12-PRE, B12-SRC v0.1 incident, B12-SRC v0.2, or the Atomic B12 Scope Review absent contradictory fresh repository evidence.

Accepted facts relevant to this task include:

- B12 remains NOT AUTHORIZED;
- primary + nine direct coordinates are only an eligible future acquisition candidate subset, not a proven complete atomic classpath set;
- 18 accepted actual dependency edges point to 13 unique actual dependency target coordinates;
- those 13 targets require both bounded local-state review and bounded production source/object-contract review before atomic acquisition can be considered;
- 12 BOM constraint-only declarations remain excluded unless later actual-edge evidence exists;
- denied test/unrelated classifier objects remain excluded;
- API/model compatibility variant selection remains unresolved and allowlisted variants remain candidates only.

This task performs only the first of the two required evidence categories for the 13 targets: guest-local-state review.

## 4. Exact 13-coordinate allowlist

Review only these exact coordinates/versions derived from accepted actual dependency edges:

1. `org.jetbrains.kotlin:kotlin-build-tools-api:2.1.0`
2. `org.jetbrains.kotlin:kotlin-gradle-plugin-annotations:2.1.0`
3. `org.jetbrains.kotlin:kotlin-native-utils:2.1.0`
4. `org.jetbrains.kotlin:kotlin-tooling-core:2.1.0`
5. `org.jetbrains.kotlin:kotlin-test:2.1.0`
6. `org.jetbrains.kotlin:kotlin-stdlib:2.1.0`
7. `com.google.code.gson:gson:2.8.9`
8. `org.jetbrains.kotlin:kotlin-util-io:2.1.0`
9. `org.jetbrains.kotlin:kotlin-util-klib:2.1.0`
10. `org.jetbrains.kotlin:kotlin-build-common:2.1.0`
11. `org.jetbrains.kotlin:kotlin-compiler-embeddable:2.1.0`
12. `org.jetbrains.kotlin:kotlin-daemon-client:2.1.0`
13. `org.jetbrains.kotlinx:kotlinx-coroutines-core-jvm:1.6.4`

Do not add any coordinate discovered during execution.
Do not inspect the 12 BOM constraint-only targets unless a coordinate above independently coincides with one of them; if so, authority comes only from this exact 13-coordinate allowlist.
Do not inspect primary or the previously reviewed nine direct coordinates.

## 5. Sandbox identity gate

Last accepted preserved Sandbox ID from the B-series evidence chain:

`0087a898-5221-41df-a33d-65045ffea69a`

After repository/FIRST/task-sheet gates pass and before relying on guest-local state, fresh-check that this exact Sandbox still exists and remains the unique applicable running Sandbox.

If the identity/uniqueness gate fails:

`STOP — FIXED GUEST STATE NOT RE-ESTABLISHED`

Do not substitute another Sandbox.
Do not create/reconstruct/materialize a replacement Sandbox.
Do not proceed to source research or acquisition.

## 6. Exact permitted guest root

Only the already-fixed guest Gradle user home is authorized:

`C:/M3A/cache/gradle`

The review is limited to exact coordinate-derived locations necessary to determine state for the 13 allowlisted coordinates.

No recursive or broad listing/search of the Gradle user home is authorized.
No enumeration of group directories, unrelated artifacts, unrelated versions, whole `files-2.1`, whole `metadata-*`, whole descriptor stores, cache-wide namespaces, drives, host, or repository.

Where immediate `metadata-*` format directories must be considered to probe an exact descriptor suffix, use only the minimum bounded method already accepted in B12-PRE; do not turn that into descriptor-store enumeration.

## 7. Permitted local-state method

For each exact coordinate, perform only the minimum exact-path read operations necessary to establish state.

Permitted object/location classes, where applicable:

- exact `files-2.1/<group>/<artifact>/<version>` location;
- exact POM filename under that exact coordinate/version location;
- exact `.module` filename under that exact coordinate/version location;
- exact base JAR filename under that exact coordinate/version location;
- exact coordinate descriptor/version suffix under applicable immediate `metadata-*` descriptor stores;
- exact `descriptor.bin` under that coordinate-specific descriptor/version subtree.

Use fail-closed execution sentinels if the Sandbox command channel does not return guest stdout, as in the previously accepted B12-PRE method.

Do not infer completeness from directory existence alone.
Do not infer absence from another repository namespace or another coordinate.
Do not inspect or interpret file bodies unless strictly necessary to establish local completeness and explicitly within the exact coordinate path.

If determining a coordinate's completeness would require broad enumeration, Gradle execution, network access, dependency resolution, source research, or expansion beyond this allowlist, classify:

`NOT DETERMINABLE WITHIN AUTHORIZED READ SCOPE`

and stop investigation for that coordinate.

## 8. Required per-coordinate classification

Assign exactly one classification to every coordinate:

- `LOCALLY COMPLETE`
- `LOCALLY INCOMPLETE`
- `ABSENT`
- `NOT DETERMINABLE WITHIN AUTHORIZED READ SCOPE`

For `LOCALLY INCOMPLETE`, identify only exact missing object classes proven within scope.

For `ABSENT`, record the exact coordinate locations checked that establish absence.

For `LOCALLY COMPLETE`, record the exact object/location evidence used; do not infer that local completeness equals production necessity or future acquisition exclusion without later review.

No classification creates source-read or download authority.

## 9. Exact evidence matrix

Produce a 13-row matrix containing at least:

- exact coordinate;
- exact files-2.1 coordinate/version location checked;
- exact descriptor/version location class checked;
- POM state where applicable;
- `.module` state where applicable;
- base JAR state where applicable;
- descriptor state where applicable;
- final classification;
- any bounded caveat.

Do not include unrequested transitive coordinates.

## 10. Explicit prohibitions

No Maven Central reads.
No Google Maven reads.
No alternate repository/mirror/search-engine reads.
No remote HEAD/GET/checksum/signature/sidecar/existence probes.
No source/object research.
No acquisition/download/persistence.
No guest network activity.
No package fill.
No local Maven repository creation/exposure.
No staging repository creation.
No Gradle native-cache write/fabrication/mutation.
No Gradle/gradlew/dependency resolution.
No Flutter/Dart.
No M3.
No AAR probe.
No M1 retry.
No M2.
No inspection of BOM constraint-only targets outside the literal 13-coordinate allowlist.
No denied classifier reads/probes.
No product implementation.
No LC-03 / LC-04 / Phase 36.
No README read.
FD02 remains excluded.
No default `git status`.
No protected staged/index inspection.
No repository-wide/directory-wide/cache-wide/drive-wide/host-wide search or enumeration.
No automatic helper agents.

## 11. Required result artifact

Create exactly one bounded review result document on a fresh review branch whose sole parent is the verified durable `main` authority.

Recommended result path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M3_POST_R7_B12_DEP13_LOCAL_STATE_REVIEW_RESULT_V0_1.md`

The result must contain:

1. repository/FIRST/task identity gates;
2. Sandbox identity/uniqueness gate;
3. exact bounded local-state method;
4. complete 13-row result matrix;
5. counts for each classification;
6. exact list of coordinates that still require production source/object contract based on accepted Atomic B12 Scope Review policy;
7. explicit statement that no source/network/acquisition/cache mutation/Gradle/M3/AAR/M2 occurred;
8. a single safest next bounded task recommendation.

Do not publish the downstream source/object-contract task in this execution.

## 12. Downstream recommendation rule

The accepted Atomic B12 Scope Review already requires production source/object-contract evidence for all 13 actual-edge targets before atomic B12 acquisition can be considered.

Therefore local presence does not cancel the need for the later bounded production source/object-contract evidence unless a future accepted governance decision explicitly changes that requirement.

However, the result must distinguish local-state outcomes so a later source/object-contract/acquisition design can avoid unnecessary acquisition for coordinates already locally complete.

## 13. Final classification

Use one of the following as applicable:

`DEP13 LOCAL STATE ESTABLISHED — READY FOR 13-COORDINATE PRODUCTION SOURCE/OBJECT CONTRACT`

or

`DEP13 SOURCE/OBJECT CONTRACT ENTRY BLOCKED — LOCAL STATE NOT DETERMINABLE WITHIN AUTHORIZED SCOPE`

If a mandatory repository/FIRST/Sandbox gate fails, use an exact blocker classification instead.

## 14. Candidate governance and stop rule

Create the result only on a fresh review branch from verified current `main`.

The executor must not self-accept.
Do not modify `main`.
Do not perform acceptance in the same task.
Do not publish or execute the downstream source/object-contract task in the same execution.
Do not execute B12 acquisition.

After publishing the bounded candidate result, stop and report exactly:

- review branch;
- candidate commit;
- sole parent;
- candidate tree;
- result path;
- result blob;
- result file SHA-256 if available;
- final classification;
- `LOCALLY_COMPLETE` count;
- `LOCALLY_INCOMPLETE` count;
- `ABSENT` count;
- `NOT_DETERMINABLE` count.

`DEP13 LOCAL-STATE COMPLETION != SOURCE-READ AUTHORITY`

`DEP13 LOCAL-STATE COMPLETION != B12 ACQUISITION AUTHORITY`

`DOWNSTREAM RECOMMENDATION != DOWNSTREAM TASK AUTHORITY`

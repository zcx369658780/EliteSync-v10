# EliteSync v10 H-01 BIB-03 U-15 TD-01 TLA-01 Host / Native Build Integration Owner Exact-Locator Intake Task Sheet v0.1

Status: `PROPOSED — TD-01 TLA-01 OWNER EXACT-LOCATOR INTAKE ONLY FOR DURABLY SELECTED HOST / NATIVE BUILD INTEGRATION EVIDENCE-GAP CLASS — ZERO EXISTENCE/TYPE/CONTENT CHECK — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-tla01-host-native-build-integration-owner-exact-locator-intake-v0-1`

Exact durable live-main authority required at task start:

`81dec98abc5764791bbf848a06eacaed410f7548`

This task authorizes one later fresh GPT L3/Codex candidate action for exactly one purpose: intake one explicit Owner-supplied EliteSync-v10 repository-relative candidate locator for the already durably selected TD-01 evidence-gap class `HOST / NATIVE BUILD INTEGRATION`, without checking whether the path exists, determining its type, reading its content, searching for alternatives, importing old-repository build-graph truth into V10, or making architecture, inheritance, implementation-planning, or implementation decisions.

## 1. Memory isolation and permanent boundaries

Global/cross-project memory is intentionally NOT an authority source for EliteSync-v10.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no new V10 technical-content read, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

## 2. Durable restart-gate basis

Durable TD-01 second-fact sufficiency / next-gap disposition:

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_SECOND_FACT_SUFFICIENCY_AND_NEXT_GAP_DISPOSITION_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`af4e0cde80dc2bbd8854c4a98f4dfc93f39cb2e4`

Preserve from that durable result:

- TD-01 remains `PARTIALLY DISCOVERED`;
- TD-01 has advanced to revision-scoped `manifest + generated / project toolchain metadata` evidence level;
- current TD-01 facts remain insufficient for architecture-entry determination;
- next priority bounded TD-01 evidence-gap class = `HOST / NATIVE BUILD INTEGRATION`;
- `PROJECT_TYPE=MODULE != HOST INTEGRATION ESTABLISHED`;
- `EVIDENCE-GAP PRIORITY != LOCATOR`;
- `EVIDENCE-GAP PRIORITY != PERMISSION TO SEARCH`;
- no next technical-evidence content-read task may be authored until an exact locator for this selected evidence-gap class is lawfully supplied through TLA-01 Owner input or separately predeclared TLA-02 governance.

## 3. Frozen Owner input

The Owner has now explicitly supplied the following input.

TD class:

`TD-01 — V10 TOOLCHAIN / BUILD / DEPENDENCY FACTS`

Selected evidence-gap class:

`HOST / NATIVE BUILD INTEGRATION`

Owner-supplied exact V10 repository-relative candidate locator:

`apps/android/app/build.gradle.kts`

Named future non-design question:

`WHAT HOST / NATIVE BUILD INTEGRATION FACTS — INCLUDING ANY EXPLICIT FLUTTER MODULE LOCATION, FLUTTER AAR GENERATION TASK, NATIVE PREBUILD DEPENDENCY, VARIANT-SPECIFIC AAR DEPENDENCY CONSUMPTION, OR OTHER DIRECTLY DECLARED ANDROID-HOST BUILD-INTEGRATION FACTS — ARE PRESENT IN THE CONTENT AT THIS EXACT OWNER-SUPPLIED V10 LOCATOR?`

The Owner supplied this locator string as an EliteSync-v10 TLA-01 candidate locator for the durably selected `HOST / NATIVE BUILD INTEGRATION` evidence-gap class.

Preserve exactly:

`EVIDENCE-GAP PRIORITY != LOCATOR`

`OLD ELITESYNC REPOSITORY LOCATOR != ELITESYNC-V10 REPOSITORY LOCATOR`

`OLD HOST/NATIVE INTEGRATION FACT != V10 ARCHITECTURE DECISION`

`OLD BUILD GRAPH != V10 INHERITANCE AUTHORITY`

`LOCATOR REVIEW != RECOMMENDATION TO INHERIT`

`OWNER-SUPPLIED V10 LOCATOR != VERIFIED EXISTING V10 PATH`

`OWNER-SUPPLIED V10 LOCATOR != TECHNICAL FACT`

`OWNER-SUPPLIED V10 LOCATOR != ARCHITECTURE CHOICE`

`SINGLE FILE HIT / CONTENT FACT != COMPLETE HOST BUILD GRAPH`

No claim is made yet that `apps/android/app/build.gradle.kts` exists in EliteSync-v10.

No claim is made yet that EliteSync-v10 uses an Android host, Flutter AAR integration, Gradle integration, or the old repository's build graph.

This Owner input satisfies the restart condition for one locator-intake action only.

It does NOT itself authorize existence checking, type checking, content reading, locator search, technical inference, build-graph inference, architecture selection, inheritance, implementation planning, or implementation.

## 4. Exact authorized substantive-content read budget

Exactly TWO substantive repository-content reads are authorized for the later candidate action:

1. this exact task sheet at this exact task-sheet commit;
2. the durable TD-01 second-fact sufficiency / next-gap disposition result above at exact durable `main`, required blob `af4e0cde80dc2bbd8854c4a98f4dfc93f39cb2e4`.

No third substantive repository-content read is authorized.

Minimum ref/commit metadata required to verify exact authority/topology is allowed only if it does not become tree/path discovery or content inspection.

Do not read `apps/android/app/build.gradle.kts`, `.metadata`, `pubspec.yaml`, `pubspec.lock`, any Gradle/settings/native-build file, CI/workflow, source/config/build files, README, FD02, or any adjacent technical file.

## 5. Sole intake question

Determine ONLY:

`MAY THE EXACT OWNER-SUPPLIED STRING apps/android/app/build.gradle.kts BE RECORDED AS THE SOLE TLA-01 CANDIDATE LOCATOR FOR THE DURABLY SELECTED TD-01 EVIDENCE-GAP CLASS HOST / NATIVE BUILD INTEGRATION AND THE NAMED FUTURE NON-DESIGN QUESTION, WHILE PRESERVING THAT INTAKE DOES NOT VERIFY EXISTENCE, TYPE, CONTENT, HOST INTEGRATION, BUILD-GRAPH FACT, TECHNICAL FACT, OR ARCHITECTURE SUITABILITY?`

This is locator intake only.

No repository operation may test the locator.

No technical evidence may be read.

## 6. Literal locator-form check permitted from frozen Owner input only

The candidate action may determine from the literal Owner-supplied string itself that it is syntactically a single repository-relative file-like locator because it is:

- one string;
- non-empty;
- not absolute;
- not directory-only;
- not basename-only;
- not a wildcard or glob;
- not an ellipsis or placeholder;
- not a list of alternatives.

This literal-form determination MUST NOT use repository operations.

Preserve:

`VALID LOCATOR FORM != VERIFIED PATH EXISTENCE`

`BUILD.GRADLE.KTS NAME != PROOF OF HOST/NATIVE BUILD INTEGRATION CONTENT`

## 7. Outcome A — Owner exact locator intake established

If current authority supports intake, the candidate MUST use exactly this classification:

`U-15 TD-01 TLA-01 HOST / NATIVE BUILD INTEGRATION OWNER EXACT-LOCATOR INTAKE = OWNER EXPLICITLY SUPPLIED apps/android/app/build.gradle.kts AS THE SOLE ELITESYNC-V10 REPOSITORY-RELATIVE CANDIDATE LOCATOR FOR THE DURABLY SELECTED TD-01 EVIDENCE-GAP CLASS HOST / NATIVE BUILD INTEGRATION AND THE NAMED FUTURE NON-DESIGN QUESTION — LOCATOR FORM ACCEPTED FOR INTAKE ONLY — OWNER RESTART CONDITION SATISFIED FOR ONE LATER SEPARATELY AUTHORED EXACT LOCATOR-EXISTENCE TASK — EVIDENCE-GAP PRIORITY != LOCATOR — OWNER-SUPPLIED V10 LOCATOR != VERIFIED EXISTING V10 PATH — OWNER-SUPPLIED V10 LOCATOR != TECHNICAL FACT — SINGLE FILE HIT / CONTENT FACT != COMPLETE HOST BUILD GRAPH — OWNER-SUPPLIED V10 LOCATOR != ARCHITECTURE CHOICE — OLD ELITESYNC REPOSITORY LOCATOR != ELITESYNC-V10 REPOSITORY LOCATOR — OLD BUILD GRAPH != V10 INHERITANCE AUTHORITY — ZERO EXISTENCE CHECK — ZERO TYPE CHECK — ZERO CONTENT READ — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Outcome A means only:

- the exact string is durably recorded as the sole Owner-supplied TLA-01 candidate locator for this selected TD-01 evidence-gap class and named non-design question;
- a later separately authored exact task may perform a bounded existence check against this exact V10 locator;
- no existence, type, content, Android-host, Flutter-AAR, Gradle-integration, native-build-graph, build-success, architecture, or inheritance fact is established now;
- no second locator such as a settings file is authorized now.

Candidate accounting:

- candidate host/native build integration TLA-01 intake reviews = `1`;
- accepted/durable intake reviews = `0`;
- exact locators tested = `0`;
- existence checks = `0`;
- type checks = `0`;
- technical-evidence content reads = `0`;
- repository search/enumeration/path discovery = `0/0/0`;
- technical-design actions = `0`;
- implementation-planning actions = `0`;
- implementation actions = `0`.

## 8. Outcome B / operational blockage

If the exact Owner input cannot truthfully be recorded under current authority, classify exactly:

`EXPECTED OUTCOME B — U-15 TD-01 TLA-01 HOST / NATIVE BUILD INTEGRATION OWNER EXACT-LOCATOR INTAKE NOT ESTABLISHED UNDER CURRENT AUTHORITY — FAIL CLOSED — ZERO EXISTENCE/TYPE/CONTENT CHECK — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

For exact main/ref/blob/topology failure use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 TLA-01 HOST / NATIVE BUILD INTEGRATION OWNER EXACT-LOCATOR INTAKE DETERMINATION MADE`

If `main` is not exactly `81dec98abc5764791bbf848a06eacaed410f7548` at task start, STOP before substantive content read except minimum ref metadata needed to detect mismatch.

If the durable second-fact disposition blob mismatches, STOP.

If the review branch advances beyond this exact task-sheet commit before candidate publication, fail closed rather than rewriting or substituting history.

## 9. Mandatory zero activity

The candidate intake action MUST perform ZERO:

- existence checking of `apps/android/app/build.gradle.kts`;
- type checking of that locator;
- content reading of that locator;
- repository-wide or directory-wide enumeration;
- repository-tree inspection;
- directory listing used for discovery;
- filename search;
- code search;
- GitHub code search;
- path discovery;
- guessed conventional Gradle/settings/native-build filename/path testing;
- alternate-locator testing;
- manifest/config/build/workflow/source locator testing beyond the frozen literal string;
- `git cat-file` object/path testing;
- `.metadata` reread;
- `pubspec.yaml` reread;
- `pubspec.lock` read;
- any Gradle/settings/native build content read;
- CI/workflow read;
- source repository access;
- old-repository access;
- source-content reads;
- Backend/API/Database/Flutter/infrastructure/deployment design;
- architecture selection;
- implementation planning;
- implementation;
- participant/data activity;
- README use;
- FD02 use;
- LC-03;
- LC-04;
- Phase 36.

## 10. Required candidate publication

For Outcome A or Outcome B create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_HOST_NATIVE_BUILD_INTEGRATION_OWNER_EXACT_LOCATOR_INTAKE_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_HOST_NATIVE_BUILD_INTEGRATION_OWNER_EXACT_LOCATOR_INTAKE_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: record H-01 BIB-03 U-15 TD-01 host native build integration locator intake`

Publish non-force only to:

`review/h01-bib03-u15-td01-tla01-host-native-build-integration-owner-exact-locator-intake-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent ACCEPT/REJECT.

## 11. Strict phase separation

Task-sheet authoring -> STOP.

Candidate locator intake -> STOP.

Acceptance/promotion -> STOP.

Exact locator existence check -> separately authored later task.

Any type check -> separately authorized only if specifically needed.

Any content read -> separately authored later task after a truthful locator-existence result.

Any second host/native integration locator -> separately governed later action after current single-file fact sufficiency is known.

Any architecture-entry determination -> separately authorized later governance task after sufficient bounded facts.

Technical design -> separately authorized later task.

Implementation planning / implementation -> separately authorized later task.
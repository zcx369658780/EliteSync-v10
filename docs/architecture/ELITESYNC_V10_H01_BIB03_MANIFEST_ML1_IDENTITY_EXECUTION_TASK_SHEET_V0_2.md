# EliteSync v10 H-01 BIB-03 Manifest ML-1 Identity Execution Task Sheet v0.2

## 1. Purpose and integrity disposition

This document defines exactly one possible later execution:

`H-01 BIB-03 MANIFEST-GUIDED STAGE ML-1 IDENTITY EXECUTION V0.2 — INTEGRITY REPLAY OF THE SAME EXACT HYPOTHESIS — EXACT MANIFEST IDENTITY ONLY — NO CONTENT READ`

Repository:

`zcx369658780/EliteSync-v10`

Publication parent authority:

`e0147231019a1251b50806b2a8ca21a888558b95`

The prior review-branch execution commit:

`b9a88c2d2eb7fd29caaa1243c23ab7868f989d41`

with artifact blob:

`60af1926c440df7fa2ece61960ca29985931df84`

is explicitly NOT accepted as a promotable ML-1 execution record.

It remains a historical unaccepted review-branch artifact only.

Independent review established:

1. its Git lineage was one commit ahead of the task-sheet publication authority and changed only the intended artifact path;
2. its recorded exact probe remained within the permitted identity-only operation and recorded a MISS with no manifest-content read;
3. its artifact incorrectly labeled the publication-authority path as the execution-artifact path rather than the controlling task-sheet path;
4. its artifact omitted multiple counters that the controlling v0.1 task sheet explicitly required the execution artifact to report;
5. a helper assigned a read-only/review role unexpectedly performed probe/write/commit/push actions in the shared workspace, creating a provenance anomaly;
6. the single-push budget of that invocation was consumed and no amendment or second push occurred.

Therefore preserve exactly:

`UNACCEPTED EXECUTION COMMIT != ACCEPTED ML-1 OUTCOME`

`RECORDED MISS IN NONCONFORMING ARTIFACT != ACCEPTED NEGATIVE PROBE FACT`

`REPLAY OF THE SAME EXACT HYPOTHESIS UNDER NEW EXPLICIT AUTHORITY != FALLBACK SEARCH`

`INTEGRITY REPLAY != NEW MANIFEST SEARCH`

No conclusion that `pubspec.yaml` is absent is accepted for forward governance until a conforming v0.2 execution is independently reviewed and promoted.

Do not amend, rewrite, force-move, delete, or promote the historical v0.1 review branch or commit.

## 2. Controlling accepted method authority

Controlling accepted method review:

`docs/architecture/ELITESYNC_V10_H01_BIB03_MANIFEST_GUIDED_BOUNDED_LOCATOR_METHOD_REVIEW_V0_1.md`

Accepted method-review Git blob:

`bba1f5d978d2dba9e240625ffe08c0d4ba40e3bb`

Controlling method model:

`BIB03-METHOD-02 — MANIFEST-GUIDED BOUNDED LOCATOR METHOD — STAGED EXACT-MANIFEST IDENTITY THEN SEPARATELY AUTHORIZED MINIMAL MANIFEST METADATA READ — NON-ENUMERATIVE — FAIL CLOSED`

Preserve:

`METHOD MODEL ACCEPTED != METHOD EXECUTED`

`MANIFEST IDENTITY ESTABLISHED != MANIFEST CONTENT READ AUTHORIZED`

## 3. Publication non-authority

Publication of this v0.2 task sheet does NOT execute ML-1 and does NOT authorize:

- manifest content reads;
- YAML parsing;
- package-root identification;
- Stage ML-2;
- source-path hypotheses;
- source-content reads;
- technical/config read budget;
- candidate source identification;
- candidate acceptance;
- bounded basis-instance acceptance;
- source-target binding;
- inspection;
- evidence collection;
- U-15 fact resolution;
- PUI reconsideration;
- design;
- implementation;
- Safety Operations;
- legal research;
- LC-03;
- LC-04;
- Phase 36.

## 4. Exact future execution contract

Future execution artifact:

`docs/architecture/ELITESYNC_V10_H01_BIB03_MANIFEST_ML1_IDENTITY_EXECUTION_V0_2.md`

Future execution branch:

`review/h01-bib03-manifest-ml1-identity-execution-v0.2`

Future exact commit subject:

`docs: execute H-01 BIB-03 manifest ML-1 identity replay v0.2`

The future execution branch must be created exactly from this v0.2 task-sheet publication commit.

Exactly one changed path is permitted: the v0.2 execution artifact above.

Do not promote `main` during execution.

## 5. Primary-agent-only execution integrity rule

For this integrity replay only:

`NO HELPER OR SUB-AGENT MAY EXECUTE THE PROBE, MODIFY THE WORKTREE, CREATE/EDIT THE EXECUTION ARTIFACT, COMMIT, OR PUSH`

The primary execution agent must perform the exact authorized probe and all Git mutations itself.

A helper/sub-agent may not be assigned an execution-capable role in this invocation.

Do not delegate the probe or Git mutation to a helper described as read-only, review-only, audit-only, or verification-only.

If a helper/sub-agent performs any probe, write, commit, or push operation anyway:

`OPERATIONAL BLOCKED — EXECUTION PROVENANCE VIOLATION`

and stop before any further commit or push if still possible.

Preserve:

`READ-ONLY REVIEW ROLE != EXECUTION AUTHORITY`

`INTERNAL DELEGATION MUST NOT EXPAND OWNER-DELEGATED AUTHORITY`

## 6. Protected-path and non-enumeration rule

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

Do NOT use or authorize:

- `rg --files`;
- `find`;
- `fd`;
- `git ls-files`;
- recursive `ls`, `dir`, or `tree`;
- repository/directory path globs;
- GitHub code search;
- broad filename search;
- `git grep`;
- repository source discovery;
- directory source discovery;
- neighboring-directory inspection.

FD02 remains protected and excluded.

Do not search for, reveal, open, read, hash, re-hash, inspect, cite, select, allowlist, modify, move, delete, stage, commit, or use FD02.

Do not independently re-prove its presence or uniqueness.

Historical incident remains historical only:

`PROTECTED-PATH ENUMERATION EXPOSURE — NO CONTENT ACCESS — STAGE A VOID`

## 7. Exact immutable ML-1 probe baseline

The integrity replay is pinned to exactly the same immutable baseline as v0.1:

`4ab7e22ee97dbbe726663fbd9dbe517be40deeb5`

Do not substitute the v0.2 task-sheet publication commit or any other commit.

Preserve:

`ML-1 PROBE COMMIT != ML-1 TASK-SHEET PUBLICATION COMMIT`

`SAME BASELINE REPLAY != NEW SEARCH SCOPE`

## 8. Exactly one replayed manifest hypothesis

Instantiate exactly:

### ML1-HYP-01-R

Origin class:

`MANIFEST-ORIGIN-01 — DETERMINISTIC STANDARD MANIFEST EXACT-PATH HYPOTHESIS PREDECLARED IN A SEPARATE EXECUTION TASK SHEET`

Exact manifest path hypothesis:

`pubspec.yaml`

Exact object hypothesis:

`4ab7e22ee97dbbe726663fbd9dbe517be40deeb5:pubspec.yaml`

This is exactly the same object hypothesis attempted by the nonconforming v0.1 record.

No second manifest hypothesis is instantiated.

No fallback manifest hypothesis is authorized.

No `melos.yaml`.

No nested manifest.

No path mutation.

Preserve:

`MANIFEST PATH HYPOTHESIS != MANIFEST EXISTS`

`MANIFEST EXISTS != MANIFEST RELEVANT`

`MANIFEST EXISTS != PACKAGE ROOT ACCEPTED`

`REPLAY OF SAME EXACT PATH != AUTHORITY TO TEST ANOTHER PATH`

## 9. Exact authorized identity-only replay

A separately authorized v0.2 execution may perform exactly one logical identity probe against:

`4ab7e22ee97dbbe726663fbd9dbe517be40deeb5:pubspec.yaml`

Allowed operation:

`git cat-file -e 4ab7e22ee97dbbe726663fbd9dbe517be40deeb5:pubspec.yaml`

If and only if existence is established:

`git rev-parse 4ab7e22ee97dbbe726663fbd9dbe517be40deeb5:pubspec.yaml`

and, if needed:

`git cat-file -t <resolved-object-sha>`

The resolved object must be a Git `blob`.

ML-1 may establish only:

1. exact path existence;
2. Git object type;
3. Git blob SHA.

Do NOT emit or inspect manifest bytes.

Do NOT use:

- `git show` on the manifest;
- `git cat-file -p`;
- `git cat-file blob`;
- manifest-content diff;
- `git grep`;
- YAML parsing;
- package/dependency/workspace parsing;
- line counting;
- any command or API returning manifest content.

Preserve:

`MANIFEST IDENTITY PROBE != MANIFEST READ`

`MANIFEST IDENTITY PROBE != SOURCE INSPECTION`

`MANIFEST PROBE BUDGET != TECHNICAL/SOURCE READ BUDGET`

## 10. Manifest identity output rule

If and only if ML1-HYP-01-R resolves to exactly one Git blob, emit exactly one manifest identity:

`zcx369658780/EliteSync-v10@4ab7e22ee97dbbe726663fbd9dbe517be40deeb5:pubspec.yaml#blob=<resolved-git-blob-sha>`

It is only a:

`MANIFEST IDENTITY`

It is NOT:

- manifest content;
- a package-root locator candidate;
- a source identifier;
- candidate source acceptance;
- a bounded basis instance;
- a source-target binding;
- inspection authority;
- ML-2 authority.

Preserve:

`MANIFEST IDENTITY != MANIFEST CONTENT`

`MANIFEST IDENTITY != PACKAGE-ROOT LOCATOR CANDIDATE`

`MANIFEST IDENTITY != SOURCE IDENTIFIER`

`MANIFEST IDENTITY != ML-2 READ AUTHORITY`

## 11. Fail-closed behavior

If the exact manifest path misses, cannot be resolved, is not a Git blob, or cannot be resolved without content exposure:

- emit no manifest identity;
- do not try another manifest path;
- do not mutate the hypothesis;
- do not infer a package root;
- do not search for manifests;
- do not inspect neighboring paths;
- do not enter ML-2.

A conforming MISS is:

`EXPECTED OUTCOME B`

not an operational failure.

Preserve:

`ZERO MANIFEST HITS != NO RELEVANT SOURCE EXISTS`

`FAILED MANIFEST PROBE != AUTHORITY TO SEARCH`

`NO MANIFEST IDENTITY FROM THIS INVOCATION != NO MANIFEST EXISTS ELSEWHERE`

## 12. Stage ML-2 remains separately gated

Even if exactly one manifest identity is emitted, Stage ML-2 remains unauthorized.

Preserve exactly:

`MANIFEST READ IS A TECHNICAL/CONFIG READ`

`MANIFEST METADATA-ONLY SEMANTICS != ZERO-BYTE CONTENT READ`

`MANIFEST IDENTITY ESTABLISHED != MANIFEST CONTENT READ AUTHORIZED`

`ANY FUTURE STAGE ML-2 MANIFEST CONTENT READ REQUIRES A SEPARATE TASK SHEET, EXPLICIT TECHNICAL/CONFIG READ BUDGET, AND INDEPENDENT GPT L3 REVIEW`

No ML-2 read budget is selected.

## 13. Frozen project state

Preserve:

`TP-SOURCE-CLASS-01`

as the sole abstract source-class envelope.

Preserve:

`TP-TARGET-01`

as the sole evidence-subject target.

Preserve:

`CURRENT ACCEPTED BOUNDED CONCRETE IDENTIFICATION BASIS INSTANCES = 0`

Preserve:

`U-12 accepted concrete targets = 0`

All three PUI entries remain deferred.

Preserve:

`PUI-PREREQ-12 threshold satisfaction = 0`

U-10 remains `UNRESOLVED`.

U-14 exclusion remains unchanged.

U-15 fact values remain unresolved.

Preserve exactly:

`D02-DURABLE-UNKNOWN-01`

D-02 remains `UNRESOLVED` and is not reopened.

Backend remains:

`0/10 NOT INSPECTED / NOT AUTHORIZED`

Database remains:

`0/8 NOT INSPECTED / NOT AUTHORIZED`

Cumulative accepted technical/source-content reads remain exactly:

`10`

The identity-only replay does not increment technical/source-content reads.

## 14. Mandatory complete execution counter ledger

The v0.2 execution artifact MUST contain every line below explicitly. No omission is allowed.

- manifest-guided method executions = `1` if probe attempted;
- Stage ML-1 executions = `1` if probe attempted;
- Stage ML-2 executions = `0`;
- manifest-path hypotheses instantiated = exactly `1`;
- manifest identity probes executed = exactly `1` if attempted;
- manifest hits = `0` or `1`;
- manifest identities emitted = `0` or `1`;
- manifest content reads = `0`;
- runtime/test/build/config/manifest reads = `0`;
- package-root locator candidates emitted = `0`;
- package-root locator candidates accepted = `0`;
- source-path hypotheses created = `0`;
- candidate source identifiers emitted = `0`;
- candidate source identifiers accepted = `0`;
- bounded basis instances accepted = `0`;
- concrete source-target bindings = `0`;
- repository-source metadata passes = `0` beyond the exact authorized manifest identity probe;
- repository-wide source discovery = `0`;
- directory-wide source discovery = `0`;
- technical-source reads = `0`;
- source-content reads = `0`;
- Flutter/client source reads = `0`;
- technical/platform inspection actions = `0`;
- future technical/source read budgets selected = `0`;
- evidence added/collected = `0`;
- U-15 fact values resolved = `0`;
- D-02 reopen actions = `0`;
- PUI-ENTRY verdict changes = `0`;
- PUI-ENTRY reconsideration actions = `0`;
- PUI-PREREQ-12 threshold satisfactions claimed = `0`;
- U-12 concrete targets added = `0`;
- technical-design decisions = `0`;
- implementation actions = `0`;
- telemetry/analytics/measurement operations = `0`;
- Safety Operations = `0`;
- legal-research operations = `0`;
- LC-03 / LC-04 / Phase 36 actions = `0 / 0 / 0`;
- DeepSeek calls = `0`;
- cumulative accepted technical/source-content reads = exactly `10`;
- current-execution repository/directory path-enumeration exposure = `0`;
- current-execution FD02 search/access = `0 / 0`.

Before commit, verify the artifact contains every required ledger entry.

Missing any required counter => `OPERATIONAL BLOCKED — EXECUTION ARTIFACT INCOMPLETE` before commit/push.

## 15. Mandatory publication-authority fields

The v0.2 artifact MUST record all of the following accurately:

- task-sheet publication commit = the exact v0.2 publication commit;
- controlling task-sheet path = `docs/architecture/ELITESYNC_V10_H01_BIB03_MANIFEST_ML1_IDENTITY_EXECUTION_TASK_SHEET_V0_2.md`;
- controlling task-sheet Git blob = the exact v0.2 task-sheet blob;
- immutable probe baseline = `4ab7e22ee97dbbe726663fbd9dbe517be40deeb5`;
- controlling accepted method-review path = `docs/architecture/ELITESYNC_V10_H01_BIB03_MANIFEST_GUIDED_BOUNDED_LOCATOR_METHOD_REVIEW_V0_1.md`;
- controlling method-review blob = `bba1f5d978d2dba9e240625ffe08c0d4ba40e3bb`.

Do not label the execution-artifact path as the task-sheet/publication-authority path.

Any mismatch => `OPERATIONAL BLOCKED — PUBLICATION-AUTHORITY RECORD MISMATCH` before commit/push.

## 16. Future terminal outcomes

Exactly one.

### Outcome A

`H-01 BIB-03 STAGE ML-1 V0.2 INTEGRITY REPLAY PRODUCED ONE MANIFEST IDENTITY — NO MANIFEST CONTENT READ — ML-2 REMAINS SEPARATELY GATED — NO PACKAGE-ROOT LOCATOR, SOURCE IDENTIFIER, BINDING, INSPECTION, EVIDENCE, PUI, DESIGN, OR IMPLEMENTATION AUTHORITY`

### Outcome B

`H-01 BIB-03 STAGE ML-1 V0.2 INTEGRITY REPLAY PRODUCED NO MANIFEST IDENTITY — FAIL CLOSED — NO FALLBACK MANIFEST SEARCH OR ADDITIONAL MANIFEST HYPOTHESIS AUTHORIZED`

No third outcome.

## 17. Publication terminal statement

`H-01 BIB-03 ML-1 V0.2 INTEGRITY-REPLAY TASK SHEET PUBLISHED — SAME EXACT ROOT PUBSPEC HYPOTHESIS AND SAME IMMUTABLE BASELINE ONLY — ORIGINAL V0.1 EXECUTION COMMIT UNACCEPTED AND NON-PROMOTABLE — NO PROBE OR CONTENT READ AUTHORITY CREATED BY PUBLICATION`

STOP AFTER ML-1 V0.2 INTEGRITY-REPLAY TASK-SHEET PUBLICATION — BEFORE MANIFEST IDENTITY PROBE, MANIFEST READ, ML-2, PACKAGE-ROOT LOCATOR IDENTIFICATION, SOURCE HYPOTHESIS, CANDIDATE ACCEPTANCE, BASIS-INSTANCE ACCEPTANCE, SOURCE-TARGET BINDING, INSPECTION, READ-BUDGET AUTHORIZATION, EVIDENCE COLLECTION, PUI RECONSIDERATION, DESIGN, OR IMPLEMENTATION
# EliteSync v10 H-01 BIB-03 U-15 TD-01 TLA-01 Settings-Level Flutter AAR Repository Registration Exact Content-Read Task Sheet v0.1

Status: `PROPOSED — TD-01 SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION EXACT CONTENT READ ONLY — ONE EXACT COMPLETE TARGET READ — REVISION-SCOPED SINGLE-FILE FACT EXTRACTION ONLY — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-tla01-settings-level-flutter-aar-repository-registration-exact-content-read-v0-1`

Exact durable live-main authority required at task start:

`9d6acf750fe2abeaedfcb4239e3ee356ca9e4c76`

This task authorizes one later fresh GPT L3/Codex candidate action for exactly one purpose: perform exactly one complete content read of the already HIT Owner-supplied V10 locator `apps/android/settings.gradle.kts` at the same predeclared V10 revision used by the accepted existence result, then extract only explicit revision-scoped single-file settings-level repository-registration / dependency-repository-source facts relevant to resolving the app-level Flutter AAR dependency coordinates. This task does not authorize any second target read, settings/build-graph locator check, search, enumeration, dependency-resolution execution, build execution, architecture decision, inheritance decision, implementation planning, or implementation.

## 1. Memory isolation and permanent boundaries

Global/cross-project memory is intentionally NOT an authority source for EliteSync-v10.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

Historical incidents, rejected candidates, stale tasks, and deviations remain historical truth and must not be rewritten as if they did not occur.

Preserve:

`PREMATURELY AUTHORED STALE TASK != CURRENT GOVERNANCE AUTHORITY`

The prior stale task `ad84587a8bd0d7fb5e373417aa94b33be936b58c` remains stale and is not current authority.

The rejected existence-result candidate `701e0ad03b5fc22694d8fcef18250580e4549e82` remains rejected and must not be promoted, reused, amended, rebased, cherry-picked, merged, or used as substantive basis.

## 2. Durable accepted HIT result

Durable settings-level exact-existence result-recording RETRY review:

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_SETTINGS_REPOSITORY_EXISTENCE_RESULT_RECORDING_RETRY_REVIEW_V0_1.md`

Required blob:

`78946d924395626514cf2e9a60e6511dc05efa98`

Preserve from that durable result:

`U-15 TD-01 TLA-01 SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION EXACT LOCATOR EXISTENCE CHECK = ONE AUTHORIZED NON-CONTENT EXISTENCE CHECK COMPLETED AGAINST apps/android/settings.gradle.kts AT PREDECLARED ELITESYNC-V10 REVISION b019b10e795080710d8486469202939120ef95ee — RESULT = HIT / ENTRY RESOLVES AT THIS EXACT REVISION+LOCATOR — HIT ESTABLISHES ONLY REVISION-SCOPED PATH EXISTENCE — HIT != FILE TYPE — HIT != CONTENT FACT — HIT != SETTINGS-LEVEL REPOSITORY REGISTRATION FACT — HIT != MAVEN REPOSITORY FACT — HIT != FLUTTER ARTIFACT REPOSITORY FACT — HIT != CONTENT-FILTER FACT — HIT != DEPENDENCY RESOLUTION FACT — HIT != ARTIFACT AVAILABILITY — HIT != COMPLETE HOST BUILD GRAPH — HIT != ARCHITECTURE CHOICE — HIT != INHERITANCE DECISION — HIT != LATESTNESS / CORRECTNESS DETERMINATION — ZERO TYPE CHECK — ZERO CONTENT READ — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Preserve:

`REJECTED RESULT DOCUMENT != INVALIDATED AUTHORIZED EXISTENCE-CHECK EXECUTION`

`RESULT-RECORDING RETRY != EXISTENCE-CHECK RE-EXECUTION`

`ONE AUTHORIZED HIT EXECUTION != PERMISSION TO RECHECK`

No existence recheck is authorized in this content-read action.

## 3. Durable locator-intake basis and exact non-design question

Durable settings-level TLA-01 intake result:

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_SETTINGS_LEVEL_FLUTTER_AAR_REPOSITORY_REGISTRATION_OWNER_EXACT_LOCATOR_INTAKE_REVIEW_V0_1.md`

Required blob:

`1e707b80fe2910a5d2541df9ebdba6a2a730cb0a`

TD class:

`TD-01 — V10 TOOLCHAIN / BUILD / DEPENDENCY FACTS`

Selected evidence-gap class:

`SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION`

Exact Owner-supplied V10 locator:

`apps/android/settings.gradle.kts`

Named non-design question:

`WHAT SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION / DEPENDENCY-REPOSITORY SOURCE FACTS — INCLUDING ANY EXPLICIT LOCAL FLUTTER AAR MAVEN REPOSITORY, FLUTTER ARTIFACT REPOSITORY, CONTENT FILTER, OR OTHER DIRECTLY DECLARED SETTINGS-LEVEL REPOSITORY CONFIGURATION RELEVANT TO RESOLVING THE APP-LEVEL FLUTTER AAR DEPENDENCY COORDINATES — ARE PRESENT IN THE CONTENT AT THIS EXACT OWNER-SUPPLIED V10 LOCATOR?`

Preserve exactly:

`EVIDENCE-GAP PRIORITY != LOCATOR`

`SETTINGS-LEVEL EVIDENCE CLASS != SETTINGS FILE LOCATOR`

`SETTINGS-LEVEL REPOSITORY DECLARATION != DEPENDENCY RESOLUTION SUCCESS`

`SETTINGS-LEVEL CONTENT FACT != COMPLETE HOST BUILD GRAPH`

`CONTENT FACT != ARCHITECTURE CHOICE`

`CONTENT FACT != INHERITANCE DECISION`

## 4. Exact target revision and locator

The only authorized target content source is exactly:

`b019b10e795080710d8486469202939120ef95ee:apps/android/settings.gradle.kts`

This is deliberately the same revision+locator for which the accepted bounded existence result recorded `HIT`.

Preserve:

`REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT`

`REVISION-SCOPED CONTENT FACT != LATESTNESS / CORRECTNESS DETERMINATION`

`SETTINGS-LEVEL REPOSITORY DECLARATION != DEPENDENCY RESOLUTION SUCCESS`

`DECLARED REPOSITORY SOURCE != ARTIFACT AVAILABILITY`

`DECLARED CONTENT FILTER != RESOLUTION SUCCESS`

`SINGLE SETTINGS FILE CONTENT FACT != COMPLETE HOST BUILD GRAPH`

`CONTENT FACT != ARCHITECTURE CHOICE`

`CONTENT FACT != INHERITANCE DECISION`

## 5. Exact authorized substantive-content read budget

Exactly FOUR substantive repository-content reads are authorized for the later candidate action:

1. this exact task sheet;
2. the durable settings-level exact-existence result-recording RETRY review, required blob `78946d924395626514cf2e9a60e6511dc05efa98`;
3. the durable settings-level TLA-01 intake result, required blob `1e707b80fe2910a5d2541df9ebdba6a2a730cb0a`;
4. exactly ONE complete target-content read of `b019b10e795080710d8486469202939120ef95ee:apps/android/settings.gradle.kts`.

No fifth substantive repository-content read is authorized.

Do NOT read the rejected existence-result candidate `701e0ad03b5fc22694d8fcef18250580e4549e82`.

Do NOT reread `apps/android/app/build.gradle.kts`.

Do NOT read any second Gradle/settings/native-build file, AndroidManifest, Kotlin/Java source, `.metadata`, `pubspec.yaml`, `pubspec.lock`, CI/workflow, README, FD02, source repository, or old repository.

Minimum exact ref/commit/compare metadata needed to verify authority/topology is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 6. Sole authorized target read

Read the exact target content once and only once:

`git show 'b019b10e795080710d8486469202939120ef95ee:apps/android/settings.gradle.kts'`

Do NOT use any second repository operation to reread any portion of the target.

Do NOT recheck existence.

Do NOT type-check.

Do NOT hash/re-hash or re-fetch the target for corroboration.

If the single exact target read unexpectedly fails, STOP without diagnostic repository follow-up.

## 7. Sole fact question and authorized extraction scope

Determine ONLY:

`WHAT SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION / DEPENDENCY-REPOSITORY SOURCE FACTS ARE EXPLICITLY PRESENT IN THE ONE AUTHORIZED COMPLETE CONTENT READ OF apps/android/settings.gradle.kts AT ELITESYNC-V10 REVISION b019b10e795080710d8486469202939120ef95ee?`

Record only facts explicitly present in that exact content and materially relevant to repository resolution, including when present:

- settings-level repository blocks that govern app dependency resolution;
- local Maven repository declarations and their exact declared path/URI expressions;
- remote Flutter artifact repository declarations and their exact declared URL forms;
- repository content filters, including exact group/include expressions relevant to Flutter/EliteSync artifacts;
- repository mode or settings-level repository policy that materially constrains dependency repository use;
- plugin-management repository declarations only where explicitly relevant to Flutter or the same build-integration chain;
- other directly declared settings-level repository configuration materially relevant to making the app-level Flutter AAR dependency coordinates resolvable.

The candidate may record ordinary repository declarations present in the same dependency-resolution block when needed to accurately describe the settings-level resolution context, but must distinguish generic repositories from declarations that explicitly target the Flutter/EliteSync AAR artifacts.

Do NOT infer successful dependency resolution, actual repository reachability, artifact presence, generated AAR existence, build success, runtime host embedding, complete host/native build graph, canonical architecture, or inheritance suitability.

## 8. Required Result A classification

If the one exact target read succeeds and supports a truthful bounded single-file settings fact register, reproduce exactly and verbatim:

`U-15 TD-01 TLA-01 SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION EXACT CONTENT READ = ONE AUTHORIZED COMPLETE CONTENT READ OF apps/android/settings.gradle.kts AT PREDECLARED ELITESYNC-V10 REVISION b019b10e795080710d8486469202939120ef95ee COMPLETED — REVISION-SCOPED SINGLE-FILE SETTINGS-LEVEL REPOSITORY-REGISTRATION / DEPENDENCY-REPOSITORY-SOURCE FACT REGISTER EXTRACTED FROM THIS EXACT CONTENT ONLY — EXPLICIT DECLARED REPOSITORY / CONTENT-FILTER / REPOSITORY-POLICY FACTS RELEVANT TO FLUTTER AAR RESOLUTION ONLY — NO APP BUILD FILE / OTHER GRADLE / MANIFEST / SOURCE / CI / ADJACENT-FILE / HISTORY / SOURCE-REPOSITORY CORROBORATION — SETTINGS-LEVEL REPOSITORY DECLARATION != DEPENDENCY RESOLUTION SUCCESS — DECLARED REPOSITORY SOURCE != ARTIFACT AVAILABILITY — DECLARED CONTENT FILTER != RESOLUTION SUCCESS — SINGLE SETTINGS FILE CONTENT FACT != COMPLETE HOST BUILD GRAPH — REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT — CONTENT FACT != ARCHITECTURE CHOICE — CONTENT FACT != INHERITANCE DECISION — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Do not shorten, paraphrase, translate, split, normalize, or semantically rewrite that classification.

Outcome A means only:

- one complete read of the exact settings file at the exact predeclared V10 revision occurred;
- explicit settings-level repository/dependency-source declarations in that content may be recorded as revision-scoped single-file facts;
- generic settings repository declarations must be distinguished from Flutter/EliteSync-specific repository or content-filter declarations;
- no repository reachability, artifact availability, successful dependency resolution, build success, complete build graph, architecture, or inheritance determination is established;
- no second technical file is read or authorized.

## 9. Operational blockage

If exact authority/ref/blob validation fails before the target read, use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION EXACT CONTENT FACT REGISTER DETERMINATION MADE`

If the single target read fails, use only:

`OPERATIONAL BLOCKED — AUTHORIZED SINGLE SETTINGS-LEVEL TARGET CONTENT READ FAILED — NO SETTINGS-LEVEL CONTENT FACT REGISTER DETERMINATION MADE — ZERO DIAGNOSTIC FOLLOW-UP`

If the review branch advances beyond this exact task-sheet commit before candidate execution/publication, fail closed rather than rewriting or substituting history.

## 10. Mandatory zero activity

The candidate action MUST perform ZERO:

- existence recheck of `apps/android/settings.gradle.kts`;
- type check of that locator;
- second target read or partial reread;
- target hash/re-hash/re-fetch;
- read of rejected candidate `701e0ad03b5fc22694d8fcef18250580e4549e82`;
- reread of `apps/android/app/build.gradle.kts`;
- any second Gradle/settings/native-build content read;
- AndroidManifest/Kotlin/Java source read;
- `.metadata` reread;
- `pubspec.yaml` reread;
- `pubspec.lock` read;
- CI/workflow read;
- alternate locator/revision testing;
- repository-wide or directory-wide enumeration;
- repository-tree inspection;
- directory listing used for discovery;
- filename search;
- code search;
- GitHub code search;
- path discovery;
- source repository access;
- old-repository access;
- README use;
- FD02 use;
- dependency-resolution execution;
- Gradle build/task execution;
- network/repository reachability tests;
- artifact-availability tests;
- technical design;
- architecture selection;
- implementation planning;
- implementation;
- participant/data activity;
- LC-03;
- LC-04;
- Phase 36.

## 11. Required candidate publication

For successful Result A create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_SETTINGS_LEVEL_FLUTTER_AAR_REPOSITORY_REGISTRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_SETTINGS_LEVEL_FLUTTER_AAR_REPOSITORY_REGISTRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: read H-01 BIB-03 U-15 TD-01 settings repository content`

Publish non-force only to:

`review/h01-bib03-u15-td01-tla01-settings-level-flutter-aar-repository-registration-exact-content-read-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent `ACCEPT` / `REJECT`.

## 12. Strict phase separation

Task-sheet authoring -> STOP.

Candidate exact-content read / fact register -> STOP.

Fresh GPT L3 ACCEPT/REJECT -> STOP.

If accepted, normal non-force promotion -> separate acceptance action.

Settings-level fact sufficiency / next-gap disposition -> separately authorized later governance task.

Architecture-entry determination -> separately authorized later governance task after sufficient bounded technical facts.

Technical design -> separately authorized later task.

Implementation planning / implementation -> separately authorized later task.
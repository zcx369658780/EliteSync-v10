# EliteSync v10 H-01 BIB-03 U-15 TD-01 M1 Owner-Root Intake and Binding-Validation Governance Task Sheet v0.1

Status: `PROPOSED — TD-01 M1 OWNER-SUPPLIED EVIDENCE-WORKTREE ROOT INTAKE / BINDING VALIDATION ONLY — ONE EXACT OWNER ROOT STRING FROZEN — THREE EXACT NON-CONTENT GIT METADATA COMMANDS MAY BE EXECUTED IN A LATER CANDIDATE — ZERO ARTIFACT PROBE — ZERO ARTIFACT EXISTENCE/TYPE/CONTENT/HASH CHECK — ZERO DIRECTORY / GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO Gradle/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-m1-owner-root-intake-binding-validation-governance-v0-1`

Exact durable live-main authority required at task start:

`823a2f039b2ccabfbeecede6220f705db41ff4ff`

This task authorizes one later fresh GPT L3/Codex governance candidate action for exactly one purpose: accept the Owner-supplied exact absolute local root string `D:\EliteSync-v10` as the sole M1 evidence-worktree root candidate, then perform only the minimum exact non-content Git metadata validation needed to determine whether that exact root is the top-level working-tree root of repository `zcx369658780/EliteSync-v10`, while recording local `HEAD` only as state context and without probing the materialized AAR locator or inspecting protected local staged state.

This task does NOT execute M1.

This task does NOT establish artifact presence or absence.

This task does NOT require the Owner-selected worktree to be attached to `refs/heads/main`.

This task does NOT establish cleanliness.

## 1. Owner-supplied exact root input

Current explicit Owner input:

`OWNER-SUPPLIED EXACT EXISTING LOCAL ELITESYNC-V10 EVIDENCE-WORKTREE ROOT = D:\EliteSync-v10`

The exact literal root candidate for this task is therefore:

`D:\EliteSync-v10`

Preserve exactly:

`OWNER-SUPPLIED EVIDENCE ROOT != VERIFIED FILESYSTEM PATH`

`OWNER-SUPPLIED EVIDENCE ROOT != VERIFIED ELITESYNC-V10 WORKTREE`

`OWNER-SUPPLIED EVIDENCE ROOT != refs/heads/main WORKTREE`

`OWNER-SUPPLIED EVIDENCE ROOT != CLEAN WORKTREE`

`OWNER-SUPPLIED EVIDENCE ROOT != ARTIFACT PRESENCE`

`ROOT INTAKE != ROOT VALIDATION`

`ROOT VALIDATION != ARTIFACT PROBE`

No other local root candidate is authorized.

## 2. Permanent governance boundaries

Global/cross-project memory is NOT EliteSync-v10 governance authority.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no new V10 technical-target read, no generated-output inspection, no Maven-directory inspection, no cache inspection, no Gradle/build execution, no dependency-resolution execution, no repository/network artifact test, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

Protected unrelated local staged state remains protected. Do NOT inspect, modify, unstage, overwrite, discard, reset, clean, commit, cite, summarize, or use it.

Do NOT use `git status`, `git status --short`, untracked checks, staged/unstaged diff inspection, directory listing, cache listing, generated-output listing, filename search, code search, GitHub code search, repository-tree inspection, filesystem discovery, globbing, or path search.

Do NOT rerun `git worktree list --porcelain` or use another worktree-listing command.

Historical incidents, rejected candidates, stale tasks, and deviations remain historical truth and must not be rewritten as if they did not occur.

## 3. Exact durable basis A — accepted evidence-worktree recovery disposition

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_EVIDENCE_WORKTREE_RECOVERY_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`d174230edcac33565769b50bedf6d3b5ea985c33`

Preserve from Basis A:

- the prior environment/method action found `0` registered worktrees whose branch line was exactly `refs/heads/main`;
- that result was an environment-binding failure only;
- it did not establish that no EliteSync-v10 worktree exists;
- it did not establish artifact absence;
- another registered worktree may not be auto-selected;
- the accepted next restart gate was Owner supply of one exact existing local EliteSync-v10 evidence-worktree root;
- no root had yet been supplied, validated, or authorized in that prior action;
- root intake, root validation, and artifact probing remain distinct phases.

Preserve exactly:

`REGISTERED refs/heads/main WORKTREE COUNT = 0 != NO ELITESYNC-V10 WORKTREE EXISTS`

`REGISTERED refs/heads/main WORKTREE COUNT = 0 != ARTIFACT ABSENCE`

`REGISTERED refs/heads/main WORKTREE COUNT = 0 != PERMISSION TO AUTO-SELECT ANOTHER WORKTREE`

`ROOT INTAKE != ROOT VALIDATION`

`ROOT VALIDATION != ARTIFACT PROBE`

## 4. Exact durable basis B — accepted materialized M1 locator

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_L3_ARTIFACT_LOCATOR_MATERIALIZATION_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`b80d0d05065003ccd1585d4e3d2f7a00286dca3f`

Preserve exactly:

`apps/flutter_elitesync_module/build/host/outputs/repo/com/elitesync/flutter_elitesync_module/flutter_release/1.0/flutter_release-1.0.aar`

This remains the sole materialized repository-relative generated-AAR locator for the selected M1 lane.

Preserve exactly:

`MATERIALIZED ARTIFACT LOCATOR != VERIFIED ARTIFACT PRESENCE`

`MATERIALIZED ARTIFACT LOCATOR != ARTIFACT FRESHNESS`

`MATERIALIZED ARTIFACT LOCATOR != BUILD SUCCESS`

`MATERIALIZED ARTIFACT LOCATOR != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`MATERIALIZED ARTIFACT LOCATOR != M1 EXECUTION AUTHORITY`

`LOCAL GENERATED-ARTIFACT LOCATOR != GIT-TRACKED PATH CLAIM`

## 5. Exact substantive V10 repository-content read budget

Exactly THREE substantive EliteSync-v10 repository-content reads are authorized for the later candidate action:

1. this exact task sheet;
2. Basis A at required blob `d174230edcac33565769b50bedf6d3b5ea985c33`;
3. Basis B at required blob `b80d0d05065003ccd1585d4e3d2f7a00286dca3f`.

No fourth substantive EliteSync-v10 repository-content read is authorized.

Do NOT reread any V10 technical target, external web documentation, rejected candidate, old/source repository content, README, FD02, generated output, Maven metadata, `.aar`, `.pom`, checksum, cache, or build-output content.

Minimum exact GitHub ref/commit/compare metadata needed to verify authority/topology is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 6. Sole root-binding validation commands

After exact authority/blob validation, the later candidate may execute exactly ONCE each of the following commands and no other local Git/filesystem command:

1. Top-level root validation:

`git -C 'D:\EliteSync-v10' rev-parse --show-toplevel`

2. Repository-origin identity validation:

`git -C 'D:\EliteSync-v10' remote get-url origin`

3. Local HEAD context:

`git -C 'D:\EliteSync-v10' rev-parse HEAD`

These are non-content Git metadata commands only.

Do NOT use `git status`.

Do NOT use `git branch`, `git symbolic-ref`, `git worktree list`, `git remote -v`, `git config --list`, `git log`, `git show`, `git diff`, or any additional Git command.

Do NOT perform any filesystem `Test-Path`, `Get-Item`, `Resolve-Path`, directory listing, canonicalization probe, file metadata read, or artifact check.

## 7. Exact validation rules

### V1 — exact root binding

The output of `git -C 'D:\EliteSync-v10' rev-parse --show-toplevel` must identify the same Windows path as the Owner-supplied root `D:\EliteSync-v10`.

For this comparison only, the following non-filesystem string normalization is permitted:

- `/` and `\` are treated as equivalent Windows path separators;
- drive-letter case may be normalized;
- a single trailing path separator may be ignored.

No other canonicalization is authorized.

No `..` resolution, symlink resolution, environment-variable expansion, short-name expansion, or filesystem path probing is authorized.

Preserve:

`SHOW-TOPLEVEL STRING MATCH != CLEAN WORKTREE`

`SHOW-TOPLEVEL STRING MATCH != ARTIFACT PRESENCE`

### V2 — repository identity binding

The output of `git -C 'D:\EliteSync-v10' remote get-url origin` must identify repository:

`zcx369658780/EliteSync-v10`

Allowed URL-form normalization for identity comparison only:

- HTTPS form with or without terminal `.git`;
- SSH/scp-like GitHub form with or without terminal `.git`;
- case-sensitive repository owner/name must remain `zcx369658780/EliteSync-v10`.

No network access or remote contact is authorized or required.

Preserve:

`ORIGIN URL IDENTITY != REMOTE REACHABILITY`

`ORIGIN URL IDENTITY != LOCAL HEAD EQUALITY WITH REMOTE MAIN`

### V3 — local HEAD context

`git -C 'D:\EliteSync-v10' rev-parse HEAD` must succeed and return one commit object identity string.

Record exactly:

`M1_EVIDENCE_LOCAL_HEAD = <returned SHA>`

This task does NOT require local HEAD to equal durable GitHub `main`.

This task does NOT authorize changing local HEAD.

Preserve exactly:

`LOCAL WORKTREE HEAD != REMOTE MAIN UNLESS SEPARATELY VERIFIED`

`LOCAL HEAD CONTEXT != CLEAN WORKTREE`

`LOCAL HEAD CONTEXT != ARTIFACT PRESENCE`

## 8. Root-binding Outcome A requirements

Outcome A is available only if V1, V2, and V3 all succeed under the exact commands above.

If Outcome A applies, freeze exactly:

`M1_EVIDENCE_WORKTREE_ROOT = D:\EliteSync-v10`

and form by literal string/path joining only:

`M1_EVIDENCE_ABSOLUTE_TARGET = D:\EliteSync-v10\apps\flutter_elitesync_module\build\host\outputs\repo\com\elitesync\flutter_elitesync_module\flutter_release\1.0\flutter_release-1.0.aar`

Literal joining is NOT an existence check.

Do NOT execute `Test-Path` against that target.

Preserve exactly:

`BOUND EVIDENCE ROOT != CLEAN WORKTREE`

`BOUND EVIDENCE ROOT != refs/heads/main WORKTREE`

`BOUND EVIDENCE ROOT != ARTIFACT PRESENCE`

`ABSOLUTE TARGET STRING != TARGET PRESENCE`

`LITERAL PATH JOIN != FILESYSTEM PROBE`

`ROOT VALIDATION != ARTIFACT PROBE`

## 9. Required Outcome A classification

If V1, V2, and V3 all succeed, record exactly and verbatim:

`U-15 TD-01 M1 OWNER-SUPPLIED EVIDENCE-WORKTREE ROOT INTAKE / BINDING VALIDATION = OWNER-SUPPLIED EXACT ROOT D:\EliteSync-v10 ACCEPTED AS THE SOLE M1 EVIDENCE-ROOT CANDIDATE — EXACT NON-CONTENT GIT METADATA VALIDATION CONFIRMS THE OWNER ROOT STRING MATCHES THE GIT SHOW-TOPLEVEL WORKING-TREE ROOT AND origin IDENTIFIES zcx369658780/EliteSync-v10 — LOCAL HEAD RECORDED ONLY AS STATE CONTEXT — M1_EVIDENCE_WORKTREE_ROOT = D:\EliteSync-v10 — THE ACCEPTED REPOSITORY-RELATIVE GENERATED-AAR LOCATOR MAY BE LITERALLY JOINED TO THIS ROOT TO FREEZE M1_EVIDENCE_ABSOLUTE_TARGET = D:\EliteSync-v10\apps\flutter_elitesync_module\build\host\outputs\repo\com\elitesync\flutter_elitesync_module\flutter_release\1.0\flutter_release-1.0.aar — ROOT BINDING DOES NOT REQUIRE refs/heads/main AND DOES NOT ESTABLISH CLEANLINESS — BOUND EVIDENCE ROOT != ARTIFACT PRESENCE — ABSOLUTE TARGET STRING != TARGET PRESENCE — ROOT VALIDATION != ARTIFACT PROBE — NO Test-Path AUTHORIZED OR PERFORMED — ZERO ARTIFACT PROBE — ZERO ARTIFACT EXISTENCE/TYPE/CONTENT/HASH CHECK — ZERO DIRECTORY / GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO Gradle/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Do not shorten, paraphrase, translate, split, normalize, or semantically rewrite this classification.

Outcome A means only:

- the Owner-supplied exact local root is bound to the expected EliteSync-v10 Git working tree/repository identity using the three exact non-content metadata commands;
- local HEAD is contextual metadata only;
- one exact absolute future M1 target string is frozen by literal joining;
- no artifact existence result exists yet;
- future one-shot M1 execution still requires a separate task.

## 10. Outcome B / operational blockage

If any of V1, V2, or V3 fails, classify exactly:

`EXPECTED OUTCOME B — U-15 TD-01 M1 OWNER-SUPPLIED EVIDENCE-WORKTREE ROOT INTAKE / BINDING VALIDATION DOES NOT BIND D:\EliteSync-v10 TO THE REQUIRED ELITESYNC-V10 LOCAL EVIDENCE WORKTREE UNDER THE EXACT AUTHORIZED NON-CONTENT METADATA CHECKS — FAIL CLOSED — NO EVIDENCE ROOT OR ABSOLUTE M1 TARGET AUTHORIZED — NO Test-Path AUTHORIZED — ZERO ARTIFACT PROBE — ZERO ARTIFACT EXISTENCE/TYPE/CONTENT/HASH CHECK — ZERO DIRECTORY / GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO Gradle/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

For exact GitHub main/ref/blob/topology failure before determination use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 M1 OWNER-SUPPLIED EVIDENCE-WORKTREE ROOT BINDING DETERMINATION MADE`

If `main` is not exactly `823a2f039b2ccabfbeecede6220f705db41ff4ff` at task start, STOP before substantive repository-content reads or local metadata commands except minimum GitHub ref metadata needed to detect mismatch.

If the review branch advances beyond this exact task-sheet commit before candidate publication, fail closed rather than rewriting or substituting history.

## 11. Mandatory zero activity

The candidate action MUST perform ZERO:

- `git worktree list --porcelain` or any worktree-listing command;
- worktree create/add/remove/move/repair/lock/unlock operations;
- branch checkout/switch/attach/detach operations;
- `git status`, `git status --short`, untracked checks;
- staged/unstaged diff inspection of protected local state;
- any Git command except the three exact commands authorized in Section 6;
- `Test-Path`, `Get-Item`, `Resolve-Path`, file existence/type/content/hash checks;
- artifact probes;
- directory listing or filesystem discovery;
- generated-output inspection;
- Maven-directory inspection;
- cache inspection;
- wildcard/glob testing;
- filename search;
- code search;
- GitHub code search;
- repository-tree inspection;
- repository-wide or directory-wide enumeration;
- path discovery;
- Gradle/build execution;
- dependency-resolution execution;
- `syncFlutterAar` execution;
- `flutter build aar` execution;
- network/repository reachability tests;
- artifact downloads/uploads;
- V10 technical-target reads;
- external web research;
- source repository access;
- old-repository access;
- README use;
- FD02 use;
- architecture selection;
- technical design;
- implementation planning;
- implementation;
- LC-03;
- LC-04;
- Phase 36.

## 12. Required candidate publication

For Outcome A or Outcome B create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_OWNER_ROOT_INTAKE_AND_BINDING_VALIDATION_GOVERNANCE_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to this task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_OWNER_ROOT_INTAKE_AND_BINDING_VALIDATION_GOVERNANCE_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: validate H-01 BIB-03 U-15 TD-01 M1 owner evidence root`

Publish non-force only to:

`review/h01-bib03-u15-td01-m1-owner-root-intake-binding-validation-governance-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent `ACCEPT` / `REJECT`.

## 13. Strict phase separation

Task-sheet authoring -> STOP.

Candidate Owner-root intake/binding validation -> STOP.

Fresh GPT L3 ACCEPT/REJECT -> STOP.

If accepted, normal non-force promotion -> separate acceptance action.

M1 one-shot `Test-Path -LiteralPath` execution -> separately authored later task only.

No later phase is implied by this task.

# EliteSync v10｜H01-BIB03-U15-TD01 M3 Post-R7 Fixed-Guest Recovery Contract Review Task v0.1

Status: `TASK AUTHORIZED — GOVERNANCE / RECOVERY-CONTRACT REVIEW ONLY — NO SANDBOX CREATION — NO GUEST ACCESS — NO DEP13 PROBES — NO ACQUISITION — NO GRADLE/M3 — NO M2`

Date: 2026-09-11

Repository: `zcx369658780/EliteSync-v10`

## 1. Purpose

Establish the exact bounded recovery/replacement contract required to create and verify a new Windows Sandbox evidence environment after the previously preserved Sandbox disappeared.

This task is governance/recovery-contract review only. It must decide whether the already accepted repository evidence is sufficient to define a reproducible replacement Sandbox creation/materialization/verification task, and if so, define that downstream task's exact inputs, identity gates, allowed source roots, guest roots, verification identities, write boundaries, and stop rules.

This task does **not** create, start, stop, close, reset, kill, share, materialize, substitute, or otherwise modify any Sandbox.

It does not resume DEP13 local-state probes.
It does not authorize source/object research, Maven/network activity, acquisition, Gradle/M3, AAR probing, or M2.

`RECOVERY CONTRACT != RECOVERY EXECUTION AUTHORITY`

## 2. Mandatory repository startup gate

Fresh-fetch GitHub `main` first.

Require exact current durable authority:

`d07471ae79b9709815ab057d2d43d36ad23426f3`

Require exact tree:

`4dc2d4b2260ad7aac5caca4e8bde48ae55f1c1ba`

Read FIRST:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M3_POST_R7_B12_DEP13_LOCAL_STATE_REVIEW_ACCEPTANCE_V0_1.md`

Require FIRST blob:

`91b4b1a009c3e619816fa9675e4a7a75903cb1a4`

Also read the exact previously accepted durable handoff that records the fixed guest environment:

`docs/architecture/ELITESYNC_V10_POST_R7_M3_GRADLE_ACQUISITION_CURRENT_SESSION_CLOSEOUT_AND_NEXT_SESSION_HANDOFF_V0_1.md`

Preserve fixed transition FIRST blob:

`18485c95262051e43892436fb52413132c5ee626`

Conversation text and memory are context/navigation only, not execution authority.

If any required repository/FIRST identity gate fails, stop before substantive recovery review.

## 3. Accepted facts to preserve

Do not reopen accepted B-series technical findings unless contradictory fresh repository evidence exists.

Preserve:

- the old Sandbox ID `0087a898-5221-41df-a33d-65045ffea69a` is no longer present and must never be treated as current;
- DEP13 local-state remains `NOT DETERMINABLE = 13/13` because no authorized guest was available;
- primary + nine direct coordinates remain only a future acquisition candidate subset;
- the 13 actual-edge targets still require local-state plus production source/object-contract evidence before atomic B12 acquisition;
- B12 remains NOT AUTHORIZED;
- manual fabrication/mutation of Gradle native `modules-2` cache metadata remains rejected;
- Windows Sandbox is temporary isolated build-chain evidence infrastructure, not the permanent development environment;
- no automatic helper agents;
- no README read; FD02 excluded; no protected staged/index inspection; no broad repository/host search.

## 4. Accepted fixed guest roots and identities

Use the accepted post-R7 handoff only to preserve already established fixed guest expectations, including:

- Flutter: `C:/M3A/tools/flutter`
- Dart: `C:/M3A/tools/flutter/bin/cache/dart-sdk/bin/dart.exe`
- Flutter-tools package config: `C:/M3A/tools/flutter/packages/flutter_tools/.dart_tool/package_config.json`
- Flutter-tools snapshot: `C:/M3A/tools/flutter/bin/cache/flutter_tools.snapshot`
- Android SDK: `C:/M3A/tools/android`
- JDK: `C:/M3A/tools/jdk`
- Git: `C:/M3A/tools/git`
- Pub cache: `C:/M3A/cache/pub`
- Gradle user home: `C:/M3A/cache/gradle`
- guest module: `C:/M3A/work/apps/flutter_elitesync_module`

Preserve accepted identities:

- Dart SHA-256: `44328B98B6BF981D8307F72EF38ACBB37B8067394916953CAA48B8B444525152`
- Flutter-tools pubspec SHA-256: `6E31FF00AC4441294B60F20A71DCE2777CC80EB33663E6621DAE38C2391DB0EA`
- Flutter-tools snapshot SHA-256: `E494C265BA9C38A6A5679842703D3041DF0D2D86C83BFC55DF56C3BD0DF8E808`
- corrected Flutter-tools package facts: `coverage: 1.15.0`, `code_builder: 4.11.1`

These are target verification identities only. Their presence in this task does not authorize any host path discovery or copying.

## 5. Exact recovery-contract questions

The review must answer all questions below using accepted repository evidence only.

### Q1 — Source locator sufficiency

Determine whether accepted evidence contains exact, non-discovered host-side source locators sufficient to reconstruct the six previously materialized source classes needed for the fixed guest environment:

1. Flutter/toolchain source material;
2. Android SDK;
3. JDK;
4. Git;
5. Pub cache;
6. Gradle cache and module source material as previously bounded.

Do not search the host, repository, filesystem, drives, worktrees, branches, or directories to find missing locators.

For each class classify:

- `EXACT SOURCE LOCATOR ESTABLISHED IN ACCEPTED EVIDENCE`
- `SOURCE CLASS ESTABLISHED BUT EXACT CURRENT LOCATOR NOT ESTABLISHED`
- `NOT DETERMINABLE FROM AUTHORIZED EVIDENCE`

### Q2 — Recovery method sufficiency

Determine whether accepted evidence is sufficient to define, without invention, the exact replacement Sandbox creation and materialization method, including:

- how a new Sandbox is created/launched;
- how its new exact identity is captured;
- how uniqueness is proven;
- which host sources are exposed read-only;
- what is copied/materialized into each fixed guest root;
- what writes are expected/allowed;
- how completion and failure are detected;
- how partial recovery is cleaned up or quarantined if a gate fails.

Do not execute any method.

### Q3 — Verification contract

Define the minimum downstream verification contract that a replacement Sandbox must pass before it can become the new fixed guest authority for DEP13, including at minimum:

- exact new Sandbox ID recorded fresh;
- exactly one applicable running Sandbox;
- all required fixed guest roots present;
- required accepted identity hashes match;
- no silent toolchain/version drift;
- no guest network requirement for recovery unless separately justified and authorized;
- Gradle user home restored/materialized only through an accepted supported mechanism;
- protected module state and previously accepted generated `.android` state are not silently altered beyond any explicitly authorized recovery scope.

### Q4 — Downstream execution readiness

Choose one classification:

`RECOVERY EXECUTION CONTRACT COMPLETE — READY FOR OWNER-DELEGATED BOUNDED RECOVERY EXECUTION TASK`

or

`RECOVERY EXECUTION CONTRACT BLOCKED — EXACT SOURCE/METHOD LOCATORS MISSING`

If blocked, list only the exact missing locators/evidence items. Do not search for them.

## 6. No silent historical substitution

Historical fact that A1-A7 or B-series recovery once succeeded does not by itself authorize replay.

Do not treat:

- old Sandbox ID;
- old temporary share handles;
- inferred drive paths;
- guessed source directories;
- conversation-only paths;
- host-wide search results;

as current recovery authority.

An exact locator must be supported by accepted repository evidence or explicitly supplied by Owner in a later task.

## 7. Allowed repository reads

Read only the exact documents explicitly named in this task and exact documents directly identified by path/blob inside those documents when strictly necessary to answer Q1-Q4.

Do not perform repository-wide or directory-wide enumeration, filename search, code search, `git ls-tree`, `git ls-files`, `rg --files`, `find`, `fd`, or glob-based discovery.

If a required historical source/method record is referenced only descriptively but without an exact path/blob, classify the relevant recovery element as not established rather than searching for it.

## 8. Explicit prohibitions

No Sandbox create/start/stop/close/reset/kill/share/materialize/substitute/reconstruct.
No `wsb` execution except no runtime execution is needed for this governance task.
No guest access.
No host-wide or drive-wide source discovery.
No guest cache reads.
No DEP13 probes.
No source/object research.
No Maven Central or Google Maven reads.
No artifact acquisition/download/persistence.
No package fill.
No local Maven repository creation/exposure.
No Gradle cache mutation/fabrication.
No Gradle/gradlew/dependency resolution.
No Flutter/Dart execution.
No M3.
No AAR probe.
No M1 retry.
No M2.
No product implementation.
No LC-03 / LC-04 / Phase 36.
No README read.
FD02 remains excluded.
No default `git status`.
No protected staged/index inspection.
No automatic helper agents.

## 9. Required result artifact

Create exactly one bounded result document on a fresh review branch whose sole parent is verified current durable `main`.

Recommended result path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M3_POST_R7_FIXED_GUEST_RECOVERY_CONTRACT_REVIEW_RESULT_V0_1.md`

The result must contain:

1. repository/FIRST gates;
2. accepted evidence read list with exact paths/blobs where available;
3. Q1 source-locator matrix;
4. Q2 recovery-method sufficiency decision;
5. Q3 exact downstream verification contract;
6. Q4 readiness classification;
7. exact missing locator/evidence list if blocked;
8. explicit NOT_RUN / NOT_AUTHORIZED section;
9. one safest next bounded task recommendation.

Do not publish or execute that downstream task in this execution.

## 10. Candidate governance and stop rule

Create the result only on a fresh review branch from verified current `main`.

The executor must not self-accept.
Do not modify `main`.
Do not execute recovery.
Do not resume DEP13.
Do not publish a downstream recovery execution task in the same execution.

After publishing the candidate, stop and report exactly:

- review branch;
- candidate commit;
- sole parent;
- candidate tree;
- result path;
- result blob;
- result SHA-256 if available;
- final classification;
- count of exact source classes with established locators;
- count of missing source/method locator items.

`RECOVERY CONTRACT COMPLETION != SANDBOX RECOVERY EXECUTION AUTHORITY`

`RECOVERY CONTRACT BLOCKER != PERMISSION TO SEARCH FOR LOCATORS`

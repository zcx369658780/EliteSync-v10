# EliteSync v10 H-01 BIB-03 U-15 TD-01 TLA-01 Generated / Project Toolchain Metadata Exact Content-Read Task Sheet v0.1

Status: `PROPOSED — TD-01 GENERATED / PROJECT TOOLCHAIN METADATA EXACT CONTENT READ ONLY — ONE EXACT TARGET CONTENT READ — REVISION-SCOPED FACT EXTRACTION ONLY — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-tla01-generated-toolchain-metadata-exact-content-read-v0-1`

Exact durable live-main authority required at task start:

`f463361338f7933e9b03dab7d58e895691733106`

This task authorizes one later fresh GPT L3/Codex candidate action for exactly one purpose: read exactly once the complete content at the already durably accepted and revision-scoped HIT EliteSync-v10 locator `apps/flutter_elitesync_module/.metadata` at exact revision `13ed14cd644ddb769e50cb3d41ee0a425f130f21`, extract only explicit `GENERATED / PROJECT TOOLCHAIN METADATA` facts from that exact content, and record those facts without reading adjacent files, searching for corroboration, deciding architecture, deciding inheritance, or making implementation decisions.

## 1. Memory isolation and permanent boundaries

Global/cross-project memory is intentionally NOT an authority source for EliteSync-v10.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

## 2. Durable exact locator and HIT basis

Durable generated/project toolchain metadata TLA-01 intake result:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_GENERATED_TOOLCHAIN_METADATA_OWNER_EXACT_LOCATOR_INTAKE_REVIEW_V0_1.md`

Required blob:

`2bfb6ddb05c7f6964ff04c0b68a39e2ee0bdacd0`

Durable generated/project toolchain metadata exact-existence HIT result:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_GENERATED_TOOLCHAIN_METADATA_EXACT_EXISTENCE_REVIEW_V0_1.md`

Required blob:

`450f24fadb0f01f22e7bb53f63eb3b2da61d51f6`

Exact content-read revision:

`13ed14cd644ddb769e50cb3d41ee0a425f130f21`

Exact content-read locator:

`apps/flutter_elitesync_module/.metadata`

Durably selected evidence-gap class:

`GENERATED / PROJECT TOOLCHAIN METADATA`

Named non-design question:

`WHAT GENERATED / PROJECT TOOLCHAIN METADATA FACTS — INCLUDING PROJECT TYPE, TOOL REVISION, CHANNEL, OR OTHER EXPLICITLY RECORDED FLUTTER PROJECT-METADATA FACTS — ARE PRESENT IN THE CONTENT AT THIS EXACT OWNER-SUPPLIED V10 LOCATOR?`

The prior accepted HIT establishes only that this exact locator resolves at this exact revision.

Preserve:

`EXACT REVISION+LOCATOR HIT != FILE TYPE`

`EXACT REVISION+LOCATOR HIT != CONTENT FACT`

`.metadata NAME != PROOF OF FLUTTER TOOLING CONTENT`

`RECORDED GENERATED TOOL REVISION != INSTALLED TOOL REVISION`

`RECORDED CHANNEL != CURRENT INSTALLED CHANNEL`

`GENERATED TOOLCHAIN METADATA != INSTALLED TOOLCHAIN STATE`

`REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT`

`CONTENT FACT != ARCHITECTURE CHOICE`

`CONTENT FACT != INHERITANCE DECISION`

## 3. Exact authorized substantive-content read budget

Exactly FOUR substantive repository-content reads are authorized for the candidate action:

1. this exact task sheet at this exact task-sheet commit;
2. the durable exact-existence HIT result at durable `main`, required blob `450f24fadb0f01f22e7bb53f63eb3b2da61d51f6`;
3. the durable TLA-01 intake result at durable `main`, required blob `2bfb6ddb05c7f6964ff04c0b68a39e2ee0bdacd0`;
4. exactly one complete content read of `apps/flutter_elitesync_module/.metadata` at exact revision `13ed14cd644ddb769e50cb3d41ee0a425f130f21`.

No fifth substantive repository-content read is authorized.

The target content read may occur exactly once. Do not reread any portion of the target through a second repository operation.

Minimum ref/commit metadata needed to verify exact authority/topology is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 4. Sole authorized target-content read

Preferred exact read command:

`git show '13ed14cd644ddb769e50cb3d41ee0a425f130f21:apps/flutter_elitesync_module/.metadata'`

An equivalent exact read is allowed only if it retrieves the complete content of that same exact revision+locator once and does not retrieve type metadata, directory listings, adjacent files, history, blame, diffs, or other repository content.

Do NOT perform an existence check again before or after the content read.

Do NOT perform a type check.

Do NOT hash or re-fetch the target through another repository operation.

If the one exact content read unexpectedly fails despite the durable HIT, STOP and classify operational blockage; do not diagnose through another repository operation.

## 5. Authorized fact extraction

From the one exact target content only, record only facts explicitly present and directly relevant to the selected `GENERATED / PROJECT TOOLCHAIN METADATA` evidence-gap class.

The fact register may include, when explicitly present:

### A. Project-type metadata

Record exact semantic values for fields that explicitly identify generated project type or project form, including a `project_type` field or equivalent if present.

Do not infer project type from the filename, directory name, prior `pubspec.yaml`, or old-repository knowledge.

### B. Recorded Flutter/tool revision metadata

Record exact semantic values for fields that explicitly record a Flutter/tool revision, framework revision, generation revision, or equivalent generated-tool identifier if present.

A recorded revision is metadata stored in this exact content only.

Do not claim it is the currently installed Flutter revision, the latest Flutter revision, or the revision that should be used for V10 design.

### C. Recorded channel metadata

Record exact semantic values for fields that explicitly record a Flutter/tool channel or equivalent if present.

A recorded channel is metadata stored in this exact content only.

Do not claim it is the currently installed channel or the required V10 channel.

### D. Explicit migration/platform metadata

If the content explicitly contains generated migration/platform records, record their structure and values relevant to project/toolchain provenance, such as:

- platform identifier;
- creation revision;
- base revision;
- other directly declared migration provenance fields.

Do not use those records to infer that a platform currently builds, runs, is supported in production, or should remain in the V10 architecture.

### E. Other explicit generated-project metadata

Record other directly declared generated-project/tooling metadata only when it materially describes generation state, project/tool provenance, or migration state.

If unmanaged-file metadata or similar generated-tool bookkeeping is explicitly present, it may be recorded only as literal metadata state; do not inspect the referenced files and do not infer their content or role.

Do not convert comments, conventional expectations, or absent fields into facts beyond what is explicitly present.

## 6. Explicitly prohibited inference

Do NOT claim from this content alone:

- the currently installed Flutter SDK version or revision;
- the currently installed Flutter channel;
- the currently installed Dart SDK version;
- that recorded revision/channel values are current, latest, correct, recommended, or supported;
- that the project builds, tests, runs, or deploys successfully;
- that Flutter/Dart is the final V10 architecture choice;
- that this Flutter module is the canonical or preferred V10 client;
- that any recorded platform is production-supported or required;
- that old EliteSync implementation should be inherited;
- that another V10 Flutter project does or does not exist;
- any fact from `pubspec.yaml`, `pubspec.lock`, Gradle, CI/workflow, source files, or adjacent files not contained literally in this target;
- any Backend/API/Database/Flutter/infrastructure design conclusion.

Preserve exactly:

`RECORDED GENERATED TOOL REVISION != INSTALLED TOOL REVISION`

`RECORDED CHANNEL != CURRENT INSTALLED CHANNEL`

`GENERATED TOOLCHAIN METADATA != INSTALLED TOOLCHAIN STATE`

`PROJECT-TYPE METADATA != CANONICAL V10 CLIENT`

`MIGRATION/PLATFORM METADATA != BUILD SUCCESS`

`REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT`

`CONTENT FACT != ARCHITECTURE CHOICE`

`CONTENT FACT != INHERITANCE DECISION`

## 7. Sole execution question

Determine ONLY:

`WHAT GENERATED / PROJECT TOOLCHAIN METADATA FACTS ARE EXPLICITLY PRESENT IN THE ONE AUTHORIZED CONTENT READ OF apps/flutter_elitesync_module/.metadata AT ELITESYNC-V10 REVISION 13ed14cd644ddb769e50cb3d41ee0a425f130f21?`

## 8. Result A — revision-scoped generated/project toolchain metadata fact register established

If the one exact target content read succeeds and supports a truthful bounded fact register, use exactly this classification:

`U-15 TD-01 GENERATED / PROJECT TOOLCHAIN METADATA EXACT CONTENT READ = ONE AUTHORIZED COMPLETE CONTENT READ OF apps/flutter_elitesync_module/.metadata AT PREDECLARED ELITESYNC-V10 REVISION 13ed14cd644ddb769e50cb3d41ee0a425f130f21 COMPLETED — REVISION-SCOPED GENERATED / PROJECT TOOLCHAIN METADATA FACT REGISTER EXTRACTED FROM THIS EXACT CONTENT ONLY — EXPLICIT RECORDED METADATA ONLY — NO PUBSPEC / LOCKFILE / GRADLE / CI / ADJACENT-FILE / HISTORY / SOURCE-REPOSITORY CORROBORATION — GENERATED TOOLCHAIN METADATA != INSTALLED TOOLCHAIN STATE — REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT — CONTENT FACT != ARCHITECTURE CHOICE — CONTENT FACT != INHERITANCE DECISION — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

The candidate must then include a concise structured fact register covering only the authorized categories in Section 5 actually supported by the target content.

Candidate accounting:

- candidate generated/project toolchain metadata exact-content-read reviews = `1`;
- accepted/durable exact-content-read reviews = `0`;
- target `.metadata` content reads = `1`;
- additional target rereads = `0`;
- adjacent implementation/config/build/workflow reads = `0`;
- `pubspec.yaml` rereads = `0`;
- `pubspec.lock` reads = `0`;
- Gradle reads = `0`;
- CI/workflow reads = `0`;
- type checks = `0`;
- existence rechecks = `0`;
- repository search/enumeration/path discovery = `0/0/0`;
- technical-design actions = `0`;
- implementation-planning actions = `0`;
- implementation actions = `0`.

## 9. Operational blockage

If exact main/ref/blob/topology validation fails before the target content read, or if the one exact target content read unexpectedly fails, use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 GENERATED / PROJECT TOOLCHAIN METADATA CONTENT FACT REGISTER DETERMINATION MADE`

Do not diagnose target-read failure through another repository operation.

If the review branch advances beyond this exact task-sheet commit before candidate execution/publication, fail closed rather than rewriting or substituting history.

If the target is accidentally read more than once, do not conceal it; stop and report the deviation for fresh GPT L3 disposition.

## 10. Mandatory zero activity beyond the authorized reads

The candidate action MUST perform ZERO:

- second read or partial reread of `.metadata` through repository operations;
- `pubspec.yaml` reread;
- `pubspec.lock` read;
- Gradle read;
- CI/workflow read;
- source/config/build content read;
- any adjacent Flutter file read;
- type check of `.metadata`;
- existence recheck of `.metadata`;
- alternate locator or revision test;
- repository-wide or directory-wide enumeration;
- repository-tree inspection;
- filename search;
- code search;
- GitHub code search;
- path discovery;
- source repository access;
- old-repository access;
- source-content reads;
- Backend/API/Database/Flutter/infrastructure/deployment design;
- architecture selection;
- implementation planning;
- implementation;
- participant/data activity;
- README use;
- FD02 use;
- LC-03;
- LC-04;
- Phase 36.

## 11. Required candidate publication

Create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_GENERATED_TOOLCHAIN_METADATA_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_GENERATED_TOOLCHAIN_METADATA_EXACT_CONTENT_READ_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: read H-01 BIB-03 U-15 TD-01 generated toolchain metadata content`

Publish non-force only to:

`review/h01-bib03-u15-td01-tla01-generated-toolchain-metadata-exact-content-read-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent ACCEPT/REJECT.

## 12. Strict phase separation

Task-sheet authoring -> STOP.

Candidate exact-content-read / fact extraction -> STOP.

Acceptance/promotion -> STOP.

Any fact-sufficiency / next-gap disposition -> separately authorized later governance task.

Any follow-up through `pubspec.yaml`, `pubspec.lock`, Gradle, CI/workflow, or another file -> separately authored exact task with its own exact locator and read budget.

Any architecture-entry determination -> separately authorized later governance task after sufficient bounded technical facts.

Technical design -> separately authorized later task.

Implementation planning / implementation -> separately authorized later task.
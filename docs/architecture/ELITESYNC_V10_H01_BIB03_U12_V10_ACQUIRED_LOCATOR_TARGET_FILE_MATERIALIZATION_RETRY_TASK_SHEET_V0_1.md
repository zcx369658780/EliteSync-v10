# EliteSync v10 H-01 BIB-03 U-12 V10 Acquired-Locator Target-File Materialization Retry Task Sheet v0.1

Status: `PROPOSED — CLEAN RETRY OF EXACT GOVERNANCE TARGET-FILE MATERIALIZATION ONLY — PRIOR CANDIDATE REJECTED FOR ZERO-ACTIVITY VIOLATION — ZERO IMPLEMENTATION — ZERO MDM-01 EXECUTION`

Review branch:

`review/h01-bib03-u12-v10-acquired-locator-target-file-materialization-retry-v0-1`

Exact durable live-main authority required at task start:

`c009a376b4130f4afb4f30b00ac8c32b592d36f7`

This task sheet authorizes one fresh GPT L3/Codex candidate action for exactly one purpose: cleanly retry materialization of the already durably acquired `TP-TARGET-01` target-side locator as one governance-only Markdown file at the exact acquired repository-relative locator.

The sole acquired locator is:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U12_TP-TARGET-01_TARGET_SIDE_LOCATOR_V0_1.md`

This retry exists because the prior candidate materialization action produced structurally correct candidate content but violated a mandatory zero-activity boundary through one unauthorized target-path metadata probe before the exact create operation. That prior candidate is rejected and MUST NOT be promoted or reused as execution evidence.

## 1. Memory-isolation and project-operating rules

Preserve for this and later handoffs:

- Global/cross-project memory is intentionally NOT an authority source for EliteSync-v10.
- Do not reconstruct or import legacy EliteSync project context from memory.
- The memory restriction is intentional isolation against legacy EliteSync contamination.
- Governance authority comes from fresh GitHub durable state plus exact predeclared task sheets/artifacts/commits/blobs only.
- Complete each authorized phase, then continue directly to the next separately legal task-sheet publication and provide a launch prompt when the Owner has not imposed a stop; this acceleration rule never overrides phase separation or fail-closed gates.
- `task authoring -> STOP`, `candidate execution -> STOP`, and `acceptance/promotion -> STOP` remain distinct governance actions.

## 2. Durable gate-start state

Require and preserve:

- durable `main` = `c009a376b4130f4afb4f30b00ac8c32b592d36f7`
- accepted U-12 v10 LAM-01 locator-acquisition execution reviews = `1`
- locator-acquisition executions = `1 durable`
- LAM-01 executions = `1 durable`
- acquired target-side locators = `1 durable`
- exact durable acquired locator = `docs/architecture/ELITESYNC_V10_H01_BIB03_U12_TP-TARGET-01_TARGET_SIDE_LOCATOR_V0_1.md`
- acquired locator status = `FUTURE-CREATED / EXISTENCE NOT ASSERTED`
- target-file creations = `0 durable`
- accepted target-file materialization reviews = `0`
- MDM-01 executions = `0`
- source reads = `0`
- accepted source-path hits = `0`
- implementation actions = `0`
- old-repository access = `0`
- source-repository access = `0`
- accepted / unauthorized content-read ledger = `13 / 1`
- cumulative unauthorized enumeration incidents = `2`
- cumulative unauthorized target-path metadata-probe incidents = `1`
- `POST-E3 PROMOTION PUSH-OPERATION COUNT = UNKNOWN / NOT INDEPENDENTLY VERIFIABLE`
- `OWNER LIMITATION — CURRENT SHA IS THE LATEST VERSION TO INHERIT = UNPROVEN`

Implementation remains unauthorized.

LC-03 remains unauthorized.

LC-04 remains unauthorized.

Phase 36 remains unauthorized.

## 3. Rejected prior candidate and incident non-use rule

Prior rejected candidate:

`c57556473bf578944e0ee1840b40319455e8c5d0`

Prior task-sheet parent:

`24b8a74278c680442aa73eab296558fbe2ea3d69`

Prior candidate path/content topology was otherwise exact, but the candidate action violated the prior task sheet's mandatory `ZERO pre-existence target-path testing / existing-path verification` requirement because an auxiliary structure agent performed one unauthorized target-path metadata probe.

Preserve exactly:

`INCIDENT-BIB03-U12-MATERIALIZATION-PROBE-01 — ONE UNAUTHORIZED TARGET-PATH METADATA PROBE OCCURRED DURING THE PRIOR MATERIALIZATION CANDIDATE ACTION — PROBE OUTPUT IS NOT AUTHORIZED FOR USE — ZERO TARGET-FILE CONTENT READS — ZERO SOURCE/OLD-REPOSITORY ACCESS — PRIOR CANDIDATE REJECTED — MAIN NOT PROMOTED`

Do not inspect, reconstruct, summarize, search within, rely on, or reuse the probe output.

The prior successful create operation does NOT cure the zero-activity violation.

The prior candidate commit MUST NOT be promoted, cherry-picked, merged, rebased, amended, rewritten, or used as the retry base.

The clean retry branch MUST descend only from exact durable `main = c009a376b4130f4afb4f30b00ac8c32b592d36f7` through this retry task sheet.

## 4. Exact authorized governance basis

The retry candidate action may read ONLY this retry task sheet plus the following exact governance artifacts at exact durable `main = c009a376b4130f4afb4f30b00ac8c32b592d36f7`.

### Basis A — durable LAM-01 acquisition result

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U12_V10_LAM01_LOCATOR_ACQUISITION_EXECUTION_REVIEW_V0_1.md`

Required blob:

`0fc7e55980cd8b3d2efe88182ec2d9ef8daac6f5`

Require exact durable acquired locator field:

`CANDIDATE LAM-01 ACQUIRED V10 TARGET-SIDE LOCATOR = docs/architecture/ELITESYNC_V10_H01_BIB03_U12_TP-TARGET-01_TARGET_SIDE_LOCATOR_V0_1.md`

Require exact inherited status field:

`ACQUIRED LOCATOR STATUS = FUTURE-CREATED / EXISTENCE NOT ASSERTED`

### Basis B — durable owner-locator-unavailable / MDM-01 boundary

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U12_OWNER_LOCATOR_UNAVAILABLE_ACQUISITION_ENTRY_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`2f4e8c3a5d943e1e181fc2e6341d138a4beab3b3`

Preserve exactly:

`MDM-01 EXECUTION PRECONDITION — EXACT OWNER-SUPPLIED CANDIDATE LOCATOR = UNSATISFIED`

Preserve:

`GOVERNANCE-ACQUIRED LOCATOR != OWNER-SUPPLIED LOCATOR`

`LAM-01 SUCCESS != MDM-01 PRECONDITION SATISFIED`

No other repository content is authorized as basis.

## 5. Sole retry materialization authority

The retry candidate action is authorized to make exactly one repository write beyond its retry task-sheet parent:

Create exactly one new UTF-8 Markdown file at:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U12_TP-TARGET-01_TARGET_SIDE_LOCATOR_V0_1.md`

The exact create operation itself is the ONLY authorized target-path-sensitive or existence-sensitive action.

Before that create operation, perform ZERO target-path probes of any kind.

Do NOT use:

- GitHub contents reads on the target path;
- filesystem stat/existence APIs on the target path;
- `git cat-file` on any target-path-derived object;
- tree inspection;
- path listing;
- directory listing;
- filename search;
- code search;
- repository enumeration;
- object metadata probes;
- API `HEAD`/metadata checks;
- helper/auxiliary agents with repository/filesystem/GitHub access for path verification.

No auxiliary agent may invoke repository, filesystem, or GitHub tools during the retry candidate action. If an auxiliary agent is used for pure reasoning, it may receive only already-authorized text and no repository/tool access. The primary agent must perform the sole exact create operation itself.

If the exact create operation reports that the path already exists, conflicts, or cannot be created exactly, classify as operational blockage and STOP. Do not inspect the path and do not switch to update/overwrite.

No alternate path is permitted.

No normalization, separator change, filename change, version change, directory change, alias, fallback locator, overwrite, update, deletion, or rename is permitted.

## 6. Exact candidate target-file content

If and only if the exact create operation succeeds, the created candidate file MUST contain exactly the following substantive governance content, with ordinary final-newline formatting permitted:

```markdown
# EliteSync v10 H-01 BIB-03 U-12 TP-TARGET-01 Target-Side Locator v0.1

Status: `CANDIDATE MATERIALIZED GOVERNANCE TARGET — DURABLE ONLY AFTER FRESH GPT L3 ACCEPTANCE AND PROMOTION — ZERO IMPLEMENTATION`

`TP-TARGET-01 TARGET-SIDE LOCATOR = docs/architecture/ELITESYNC_V10_H01_BIB03_U12_TP-TARGET-01_TARGET_SIDE_LOCATOR_V0_1.md`

`LOCATOR ORIGIN = DURABLE LAM-01 GOVERNANCE ACQUISITION`

`LAM-01 ACQUISITION COMMIT = c009a376b4130f4afb4f30b00ac8c32b592d36f7`

`LAM-01 ACQUISITION BLOB = 0fc7e55980cd8b3d2efe88182ec2d9ef8daac6f5`

`MATERIALIZATION STATUS = 1 CANDIDATE / 0 DURABLE`

This file is a governance-only materialization of the exact durably acquired `TP-TARGET-01` target-side locator. It contains no implementation, source content, evidence/PUI, Backend/Database design, runtime design, API/schema/storage/process logic, or implementation-suitability determination.

`MATERIALIZED GOVERNANCE TARGET != IMPLEMENTATION`

`MATERIALIZED GOVERNANCE TARGET != SOURCE CONTENT`

`MATERIALIZED GOVERNANCE TARGET != MDM-01 EXECUTION`

`GOVERNANCE-ACQUIRED LOCATOR != OWNER-SUPPLIED LOCATOR`

Implementation remains unauthorized. MDM-01 remains unexecuted. LC-03, LC-04, and Phase 36 remain unauthorized.
```

No additional substantive sections, implementation notes, inferred architecture, source-path references, TODO implementation content, compatibility claims, or incident/probe-derived statements may be added to the target file.

## 7. Sole retry question

Determine ONLY:

`CAN THE EXACT DURABLY LAM-01-ACQUIRED TP-TARGET-01 LOCATOR BE CLEANLY MATERIALIZED AS EXACTLY ONE NEW GOVERNANCE-ONLY MARKDOWN FILE AT ITS OWN EXACT REPOSITORY-RELATIVE LOCATOR, USING THE PREDECLARED CONTENT ABOVE, WITH THE EXACT CREATE OPERATION AS THE SOLE TARGET-PATH-SENSITIVE ACTION AND WITH ZERO USE OF THE PRIOR UNAUTHORIZED PROBE OUTPUT?`

No broader post-LAM-01 use decision may be made.

## 8. Candidate Outcome A — clean exact target file materialized

Outcome A applies only if:

1. fresh durable authority matches exactly;
2. authorized basis blobs match exactly;
3. no target-path probe or equivalent action occurs before the create operation;
4. no auxiliary agent invokes repository/filesystem/GitHub tools;
5. exactly one create operation is attempted at the exact locator;
6. the create succeeds;
7. exactly the prescribed target-file content is produced;
8. no prior probe output is used.

Candidate accounting:

- candidate U-12 v10 acquired-locator target-file materialization retry reviews = `1`
- accepted materialization reviews = `0`
- target-file creations = `1 candidate / 0 durable`
- exact materialized target-side locator = `docs/architecture/ELITESYNC_V10_H01_BIB03_U12_TP-TARGET-01_TARGET_SIDE_LOCATOR_V0_1.md`
- LAM-01 executions = `1 durable`
- MDM-01 executions = `0`
- source reads = `0`
- implementation actions = `0`

Outcome A does NOT authorize implementation.

Outcome A does NOT satisfy or rewrite the MDM-01 Owner-supplied-locator precondition.

## 9. Operational blockage / fail-closed conditions

Use only:

`OPERATIONAL BLOCKED — NO CLEAN U-12 V10 ACQUIRED-LOCATOR TARGET-FILE MATERIALIZATION RETRY CANDIDATE CREATED`

for any prerequisite or create failure, including:

- `main` mismatch;
- Basis A/B blob mismatch;
- retry review branch advanced beyond the exact retry task-sheet commit before execution;
- any pre-create target-path probe occurs;
- any auxiliary agent invokes repository/filesystem/GitHub tools;
- any prior probe output is used;
- exact create reports target already exists;
- exact create conflicts or fails;
- prescribed content cannot be written exactly.

If any prohibited target-path probe occurs, STOP BEFORE CREATE. Do not continue merely because the probe output is ignored.

Operational blockage MUST NOT trigger inspection, overwrite, update, deletion, rename, alternate locator selection, search, or repository discovery.

## 10. Mandatory zero activity

The retry candidate action MUST perform ZERO:

- pre-existence target-path testing;
- existing-path verification;
- target-path metadata probing;
- `git cat-file` locator/object testing;
- repository-tree inspection;
- repository-wide or directory-wide file/path enumeration;
- directory listing used as discovery;
- GitHub code search;
- filename search;
- code search;
- source-path discovery;
- implementation-content read;
- source-content read;
- old `D:\EliteSync` access;
- source repository `zcx369658780/EliteSync` access;
- source commit dereference/read;
- MDM-01 execution;
- technical design beyond the already fixed governance content;
- Backend inspection/design;
- Database inspection/design;
- interface/schema/data-model/API/storage/process/algorithm/dependency design;
- evidence/PUI processing;
- implementation;
- LC-03;
- LC-04;
- Phase 36;
- README use;
- FD02 access/search/hash/re-hash/inspection/read/modify/move/delete/stage/commit/citation/selection/allowlisting/use.

## 11. Protected local state and permanent boundaries

The Owner previously reported unrelated staged local state in the main worktree. Treat it as protected local state.

Do NOT inspect, read, modify, unstage, overwrite, discard, reset, clean, commit, cite, summarize, or use it as evidence.

Do not use `git status`, `git status --short`, or untracked-file checks.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

Do not use prior unauthorized enumeration output or the prior unauthorized target-path probe output.

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No old/source repository access is authorized.

Preserve all accepted ADRs, durable UNKNOWN dispositions, D-02 unresolved / `D02-DURABLE-UNKNOWN-01`, U-15 unresolved, U-14 exclusion, every accepted U-12 exact-scope rule, `TP-SOURCE-CLASS-01`, `TP-TARGET-01`, Backend `0/10 NOT INSPECTED / NOT AUTHORIZED`, Database `0/8 NOT INSPECTED / NOT AUTHORIZED`, deferred PUI/PUI-PREREQ-12=`0`, all legal boundaries, all Safety boundaries, and all no-processing boundaries.

## 12. Required candidate publication topology

Later clean retry candidate publication MUST satisfy relative to this exact retry task-sheet commit:

- `ahead_by = 1`
- `behind_by = 0`
- changed-path count = `1`
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U12_TP-TARGET-01_TARGET_SIDE_LOCATOR_V0_1.md`

Required candidate commit subject:

`docs: retry materialize H-01 BIB-03 U-12 TP-TARGET-01 target-side locator`

Publish non-force only to:

`review/h01-bib03-u12-v10-acquired-locator-target-file-materialization-retry-v0-1`

Do NOT promote `main` in the retry candidate action.

After candidate publication, STOP for fresh GPT L3 independent ACCEPT/REJECT.

## 13. Strict phase separation

Retry task-sheet authoring -> STOP.

Clean retry candidate materialization -> STOP.

Acceptance/promotion -> STOP.

Any later use of the materialized target file, any MDM-01 precondition change or execution, source-side binding, source read, implementation design, implementation, LC-03, LC-04, or Phase 36 action requires a later separately authored exact task.

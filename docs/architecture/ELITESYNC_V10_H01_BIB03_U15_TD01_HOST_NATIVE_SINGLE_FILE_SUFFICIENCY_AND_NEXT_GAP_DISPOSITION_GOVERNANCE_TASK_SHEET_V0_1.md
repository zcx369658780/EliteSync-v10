# EliteSync v10 H-01 BIB-03 U-15 TD-01 Host / Native Single-File Sufficiency and Next-Gap Disposition Governance Task Sheet v0.1

Status: `PROPOSED — TD-01 HOST / NATIVE SINGLE-FILE FACT-SUFFICIENCY / NEXT-GAP DISPOSITION GOVERNANCE ONLY — ZERO NEW TECHNICAL CONTENT READ — ZERO LOCATOR ACQUISITION — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-host-native-single-file-sufficiency-next-gap-disposition-v0-1`

Exact durable live-main authority required at task start:

`8b118261f2e4ec1b698bd25ce35db935034074f0`

This task authorizes one later fresh GPT L3/Codex governance candidate action for exactly one purpose: determine what portion of the previously selected `HOST / NATIVE BUILD INTEGRATION` evidence gap is now established by the durable single-file `apps/android/app/build.gradle.kts` fact register, what material host/native build-integration facts remain unestablished, whether current TD-01 evidence is now sufficient for architecture-entry determination, and, if not, identify exactly one next bounded evidence-gap class without acquiring, guessing, searching for, or testing any locator.

## 1. Memory isolation and permanent boundaries

Global/cross-project memory is intentionally NOT an authority source for EliteSync-v10.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no new V10 implementation/config/build/workflow content read, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

## 2. Exact durable basis

### Basis A — durable host/native single-file fact register

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_HOST_NATIVE_BUILD_INTEGRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Required blob:

`b201348a6a62f7bd2c7d09d1f20b20554cdabef3`

Preserve from Basis A:

- exact revision+locator = `5ed1c04b822b72327864e9c6de63a80a77e88eb4:apps/android/app/build.gradle.kts`;
- declared Flutter module reference = `../flutter_elitesync_module`;
- declared module-local Flutter SDK properties reference = `../flutter_elitesync_module/.android/local.properties`;
- declared Flutter SDK selection order = module-local `flutter.sdk`, then host `local.properties` `flutter.sdk`, then `FLUTTER_HOME`;
- declared `syncFlutterAar` task is an `Exec` task with working directory `flutterModuleDir`;
- declared command = `flutterExecutable build aar --no-debug --no-profile` plus declared dart-define arguments;
- declared output location = module-relative `build/host/outputs/repo/com/elitesync/flutter_elitesync_module/flutter_release/1.0`;
- declared `preBuild -> syncFlutterAar` task dependency;
- declared artifact consumption includes debug `flutter_release:1.0`, profile `flutter_profile:1.0`, and release `flutter_release:1.0` coordinates;
- no settings-level, other-Gradle, manifest, source, CI, adjacent-file, history, or source-repository corroboration occurred.

Preserve exactly:

`DECLARED FLUTTER MODULE PATH != VERIFIED MODULE PATH`

`DECLARED AAR GENERATION TASK != AAR BUILD SUCCESS`

`DECLARED TASK DEPENDENCY != SUCCESSFUL TASK EXECUTION`

`DECLARED AAR DEPENDENCY != RESOLVED AAR DEPENDENCY`

`SINGLE FILE CONTENT FACT != SETTINGS-LEVEL REPOSITORY REGISTRATION`

`SINGLE FILE CONTENT FACT != COMPLETE HOST BUILD GRAPH`

`REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT`

`CONTENT FACT != ARCHITECTURE CHOICE`

`CONTENT FACT != INHERITANCE DECISION`

### Basis B — durable second-fact sufficiency / next-gap disposition

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_SECOND_FACT_SUFFICIENCY_AND_NEXT_GAP_DISPOSITION_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`af4e0cde80dc2bbd8854c4a98f4dfc93f39cb2e4`

Preserve from Basis B:

- TD-01 remains `PARTIALLY DISCOVERED`;
- TD-01 had advanced to revision-scoped `manifest + generated / project toolchain metadata` evidence level;
- architecture-entry determination remained unsupported;
- the next priority evidence-gap class was `HOST / NATIVE BUILD INTEGRATION`;
- `PROJECT_TYPE=MODULE != HOST INTEGRATION ESTABLISHED`;
- settings-level repository registration, dependency resolution, build success, installed toolchain state, and other gaps were not established by that disposition.

No other repository artifact is authorized as substantive basis.

## 3. Exact authorized substantive-content read budget

Exactly THREE substantive repository-content reads are authorized for the later candidate action:

1. this exact task sheet;
2. Basis A at exact durable `main`;
3. Basis B at exact durable `main`.

No fourth substantive repository-content read is authorized.

Do not reread `apps/android/app/build.gradle.kts`.

Do not check or read `apps/android/settings.gradle.kts`.

Do not reread `.metadata` or `pubspec.yaml`.

Do not read `pubspec.lock`, any Gradle/settings/native-build file, AndroidManifest, Kotlin/Java source, CI/workflow, README, FD02, or any adjacent file.

Minimum ref/commit metadata needed for exact authority/topology validation is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 4. Sole governance question

Determine ONLY:

`GIVEN THE DURABLE SINGLE-FILE apps/android/app/build.gradle.kts HOST / NATIVE BUILD INTEGRATION FACT REGISTER, WHAT PORTION OF HOST / NATIVE BUILD INTEGRATION IS NOW ESTABLISHED, WHAT MATERIAL HOST/NATIVE BUILD-GRAPH FACTS REMAIN UNESTABLISHED, IS TD-01 NOW SUFFICIENTLY DISCOVERED FOR AN ARCHITECTURE-ENTRY DETERMINATION, AND, IF NOT, WHAT SINGLE NEXT BOUNDED EVIDENCE-GAP CLASS SHOULD BE PRIORITIZED WITHOUT ACQUIRING OR TESTING A LOCATOR?`

This task does not authorize architecture selection.

This task does not authorize deciding that Android, Gradle, Flutter AAR integration, or the old build graph is the canonical or preferred V10 architecture.

This task does not authorize any new locator acquisition, existence test, type test, or technical content read.

## 5. Authorized disposition dimensions

### A. Host/native integration scope now established

The candidate may determine only whether Basis A establishes revision-scoped single-file declarations for:

- a Flutter module reference used by the Android app build script;
- Flutter SDK/executable selection logic used by that script;
- an AAR-generation task and declared output path;
- a native `preBuild` dependency on that AAR-generation task;
- variant-specific Flutter AAR dependency coordinates consumed by the app build script.

The candidate must keep all such facts source- and revision-scoped.

### B. Remaining host/native build-graph gaps

The candidate may identify only evidence classes, not repository paths, including:

- settings-level repository registration proving where the declared Flutter AAR coordinates are intended to resolve from;
- actual dependency resolution / artifact availability;
- successful AAR generation and build execution;
- runtime host/launcher/embedding facts, which are distinct from build-graph facts and must not be treated as required for this immediate settings-level gap unless separately justified;
- any broader native host/build graph facts not established by the single file.

Do not infer any settings filename, path, repository block, manifest path, activity path, or other locator.

### C. Architecture-entry sufficiency

Determine only whether the currently authorized TD-01 fact set is sufficient to make an architecture-entry determination.

Preserve exactly:

`FACT SUFFICIENCY DETERMINATION != ARCHITECTURE DESIGN`

`PARTIALLY DISCOVERED != DESIGN-READY`

`DECLARED AAR CONSUMPTION != RESOLVABLE AAR DEPENDENCY`

`APP BUILD-SCRIPT INTEGRATION FACT != COMPLETE HOST BUILD GRAPH`

### D. Next bounded evidence-gap priority

If more TD-01 evidence is needed, select exactly ONE evidence-gap class as the next priority.

The expected next priority is:

`SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION`

because Basis A declares Flutter AAR coordinates and a module-local generated-repository output location, while no durable authorized evidence establishes the Gradle settings/repository-registration layer through which those coordinates are intended to be resolved.

Do not name, infer, normalize, guess, search for, or test a repository path.

Do not authorize TLA-01 or TLA-02 intake in this candidate action.

## 6. Expected Outcome A — host/native integration partially established at app-build-script declaration level; settings-level registration is next priority

Outcome A applies if Basis A materially advances host/native build-integration facts but still does not establish a complete enough bounded build graph for architecture-entry determination.

If supported, use exactly this classification:

`U-15 TD-01 HOST / NATIVE SINGLE-FILE SUFFICIENCY / NEXT-GAP DISPOSITION = HOST / NATIVE BUILD INTEGRATION ADVANCED FROM UNESTABLISHED TO PARTIALLY ESTABLISHED AT REVISION-SCOPED APP BUILD-SCRIPT DECLARATION LEVEL — FLUTTER MODULE REFERENCE, DECLARED FLUTTER AAR GENERATION TASK / OUTPUT, DECLARED PREBUILD TASK DEPENDENCY, AND DECLARED VARIANT-SPECIFIC FLUTTER AAR CONSUMPTION ARE ESTABLISHED FOR THE EXACT AUTHORIZED REVISION+LOCATOR ONLY — SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION, ACTUAL DEPENDENCY RESOLUTION / ARTIFACT AVAILABILITY, SUCCESSFUL AAR GENERATION / BUILD EXECUTION, AND COMPLETE HOST BUILD GRAPH REMAIN UNESTABLISHED — TD-01 REMAINS PARTIALLY DISCOVERED AND IS NOT YET SUFFICIENT FOR ARCHITECTURE-ENTRY DETERMINATION — NEXT PRIORITY BOUNDED TD-01 EVIDENCE-GAP CLASS = SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION — NO LOCATOR ACQUIRED OR AUTHORIZED — ZERO NEW TECHNICAL CONTENT READ — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Outcome A means only:

- host/native build integration is no longer wholly unestablished;
- the durable app build script establishes an explicit declared chain from Flutter module reference to AAR generation to preBuild dependency to variant-specific AAR consumption;
- that declared chain does not establish repository registration, resolution, artifact availability, successful build execution, runtime embedding, or a complete host build graph;
- TD-01 remains `PARTIALLY DISCOVERED` and architecture-entry remains unsupported;
- the next bounded evidence-gap class is `SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION`;
- no exact locator is declared, acquired, inferred, authorized, tested, or searched for;
- no architecture, inheritance, technical-design, implementation-planning, or implementation decision is made.

Preserve exactly:

`REVISION-SCOPED APP BUILD-SCRIPT FACT != CURRENT/LATEST FACT`

`DECLARED FLUTTER MODULE PATH != VERIFIED MODULE PATH`

`DECLARED AAR GENERATION TASK != AAR BUILD SUCCESS`

`DECLARED TASK DEPENDENCY != SUCCESSFUL TASK EXECUTION`

`DECLARED AAR DEPENDENCY != RESOLVED AAR DEPENDENCY`

`APP BUILD-SCRIPT INTEGRATION FACT != SETTINGS-LEVEL REPOSITORY REGISTRATION`

`APP BUILD-SCRIPT INTEGRATION FACT != COMPLETE HOST BUILD GRAPH`

`PARTIALLY ESTABLISHED HOST INTEGRATION != ARCHITECTURE-ENTRY SUFFICIENT`

`EVIDENCE-GAP PRIORITY != LOCATOR`

`EVIDENCE-GAP PRIORITY != PERMISSION TO SEARCH`

`FACT SUFFICIENCY DETERMINATION != ARCHITECTURE DESIGN`

## 7. Outcome B / operational blockage

If the durable basis supports a different bounded truth — including if selecting `SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION` as the single next evidence-gap class would overreach — use exactly:

`EXPECTED OUTCOME B — U-15 TD-01 HOST / NATIVE SINGLE-FILE SUFFICIENCY / NEXT-GAP DISPOSITION DOES NOT SUPPORT THE EXPECTED SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION PRIORITY UNDER CURRENT AUTHORITY — FAIL CLOSED — NO LOCATOR ACQUIRED — ZERO NEW TECHNICAL CONTENT READ — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

For exact main/ref/blob/topology failure use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 HOST / NATIVE SINGLE-FILE SUFFICIENCY / NEXT-GAP DISPOSITION DETERMINATION MADE`

If the review branch advances beyond this exact task-sheet commit before candidate publication, fail closed rather than rewriting or substituting history.

## 8. Mandatory zero activity

The candidate action MUST perform ZERO:

- new locator acquisition/intake;
- TLA-01 execution;
- TLA-02 selection/import;
- `apps/android/app/build.gradle.kts` reread;
- `apps/android/settings.gradle.kts` check or read;
- any other Gradle/settings/native-build read;
- AndroidManifest read;
- Kotlin/Java source read;
- CI/workflow read;
- `.metadata` reread;
- `pubspec.yaml` reread;
- `pubspec.lock` read;
- repository-wide or directory-wide enumeration;
- repository-tree inspection;
- directory listing used for discovery;
- filename search;
- code search;
- GitHub code search;
- path discovery;
- guessed filename/path testing;
- existence/type checks;
- `git cat-file` path/object testing;
- old/source repository access;
- README use;
- FD02 use;
- Backend/API/Database/Flutter/infrastructure/deployment design;
- architecture selection;
- implementation planning;
- implementation;
- participant/data activity;
- LC-03;
- LC-04;
- Phase 36.

## 9. Required candidate publication

For Outcome A or Outcome B create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_HOST_NATIVE_SINGLE_FILE_SUFFICIENCY_AND_NEXT_GAP_DISPOSITION_GOVERNANCE_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_HOST_NATIVE_SINGLE_FILE_SUFFICIENCY_AND_NEXT_GAP_DISPOSITION_GOVERNANCE_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: review H-01 BIB-03 U-15 TD-01 host native single-file sufficiency disposition`

Publish non-force only to:

`review/h01-bib03-u15-td01-host-native-single-file-sufficiency-next-gap-disposition-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent ACCEPT/REJECT.

## 10. Strict phase separation and future restart gate

Task-sheet authoring -> STOP.

Candidate single-file sufficiency / next-gap disposition -> STOP.

Acceptance/promotion -> STOP.

If durable Outcome A is later accepted, no settings-level technical-evidence content-read task may be authored until an exact locator for the selected `SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION` evidence-gap class is lawfully supplied through TLA-01 Owner input or separately predeclared TLA-02 governance.

Any architecture-entry determination beyond the bounded sufficiency conclusion -> separately authorized later governance task.

Technical design -> separately authorized later task.

Implementation planning / implementation -> separately authorized later task.
# EliteSync v10 H-01 BIB-03 U-15 TD-01 M3 Flutter AAR Generation / Build Execution Method / Side-Effect Governance Task v0.1

Status: `TASK SHEET ONLY — GOVERNANCE AUTHORITY ONLY — M3 MUST NOT EXECUTE — NO COMMAND / BUILD / GRADLE / FLUTTER / NETWORK / GENERATED-OUTPUT INSPECTION AUTHORIZED`

## 1. Governing authority

This task sheet is authored only from the promoted durable session-entry authority:

- durable `main`: `c3b27a49c41e17e8b8138c8ce563eaa89a23044f`
- handoff path: `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M3_ENTRY_CURRENT_SESSION_CLOSEOUT_AND_NEXT_SESSION_HANDOFF_V0_1.md`
- required handoff blob: `2399b1cb0c8bbfa88bf57f74da8303bfc717af12`

The promoted handoff is already durable session-entry authority. Its acceptance must not be repeated by the later M3 governance candidate.

This task authorizes only the production of a bounded governance review candidate for:

`M3 — FLUTTER AAR GENERATION / BUILD EXECUTION METHOD / SIDE-EFFECT GOVERNANCE`

It does not authorize M3 execution.

## 2. Preserved current technical state

Preserve all of the following without expansion:

- M1 one-shot exact local artifact lane is exhausted for the frozen target.
- M1 result = `False / WORKTREE-STATE NON-HIT ONLY`.
- Evidence root = `D:\EliteSync-v10`.
- M1 NON-HIT does not establish global artifact absence, build failure, invalid integration, or Gradle dependency-resolution failure.
- M2 targeted Gradle dependency-resolution remains a later separate deferred gate.
- Next priority evidence gap = `ACTUAL FLUTTER AAR GENERATION SUCCESS / RELEASE ARTIFACT PRODUCTION STATE`.
- Next method-governance class = `M3 FLUTTER AAR GENERATION / BUILD EXECUTION, METHOD CLASS ONLY`.

Preserve exactly:

`M3 METHOD-CLASS PRIORITY != flutter build aar AUTHORITY`

`M3 METHOD-CLASS PRIORITY != syncFlutterAar AUTHORITY`

`M3 METHOD-CLASS PRIORITY != GRADLE EXECUTION AUTHORITY`

`M3 METHOD-CLASS PRIORITY != NETWORK AUTHORITY`

`M3 METHOD-CLASS PRIORITY != GENERATED-OUTPUT WRITE AUTHORITY`

`NEXT EVIDENCE-GAP PRIORITY != EXECUTION AUTHORITY`

`NEXT METHOD-GOVERNANCE CLASS != COMMAND AUTHORITY`

`GENERATION SUCCESS != DEPENDENCY-RESOLUTION SUCCESS`

`GENERATION FAILURE != INVALID INTEGRATION`

Also preserve:

`WORKTREE-STATE NON-HIT != GLOBAL ARTIFACT ABSENCE`

`WORKTREE-STATE NON-HIT != BUILD FAILURE`

`WORKTREE-STATE NON-HIT != INVALID INTEGRATION`

`WORKTREE-STATE NON-HIT != GRADLE DEPENDENCY-RESOLUTION FAILURE`

`M1 NON-HIT != PERMISSION TO RETRY M1`

## 3. Permanent boundaries inherited unchanged

All permanent boundaries in the promoted handoff remain in force. In particular:

- README documentary-read budget remains exhausted.
- FD02 remains permanently excluded.
- No repository-wide or directory-wide file/path enumeration is authorized.
- No old `D:\EliteSync` access and no source repository `zcx369658780/EliteSync` access.
- No new technical-source read, generated-output read, cache read, build-output read, `.aar` / `.pom` / checksum read, or external-documentation read is authorized by this task.
- No `git status`, untracked inspection, recursive listing, globbing, `rg --files`, `find`, `fd`, `git ls-files`, `git ls-tree`, GitHub code search, filename search, or `git grep`.
- Protected unrelated staged state must not be inspected, modified, un-staged, overwritten, discarded, reset, cleaned, committed, cited, summarized, or used.
- D-02 unresolved / `D02-DURABLE-UNKNOWN-01`, U-14 exclusion, U-12 exact-scope target rules, `TP-SOURCE-CLASS-01`, `TP-TARGET-01`, Backend `0/10 NOT INSPECTED / NOT AUTHORIZED`, Database `0/8 NOT INSPECTED / NOT AUTHORIZED`, deferred PUI, PUI-PREREQ-12=`0`, accepted legal and Safety boundaries, no-processing boundaries, implementation unauthorized, LC-03 unauthorized, LC-04 unauthorized, and Phase 36 unauthorized remain unchanged.
- Old-repository implementation is not V10 inheritance authority.
- No DB/API/Backend/Flutter architecture design, implementation planning, or implementation is authorized.

Historical incident and deviation records from the promoted handoff remain preserved and must not be reinterpreted or used as authority.

## 4. Current-session prohibition

The authoring/publishing of this task sheet is the only substantive action authorized in this session.

Do not execute or probe any of the following while authoring or publishing this task:

- Gradle or Gradle wrapper;
- Flutter build or Flutter tool execution;
- `syncFlutterAar`;
- dependency resolution;
- network/repository access;
- dependency download;
- generated-output inspection;
- cache inspection;
- Maven-directory inspection;
- M1 retry or broadened artifact probe;
- toolchain prerequisite command;
- technical design;
- implementation planning;
- implementation.

Task-sheet authoring -> STOP.

## 5. Later governance candidate: exact mandate

A later Codex candidate may read this task sheet and the promoted handoff only. It must govern M3 without executing M3. It must produce an answer-first governance review that separately determines all thirteen classes below.

If an exact determination cannot be supported from the accepted recorded facts without a newly forbidden read, command, probe, network action, or inference, the candidate must record that item as bounded `UNRESOLVED — FRESH AUTHORITY REQUIRED`; it must not guess and must not execute to resolve the uncertainty.

### 5.1 Bounded method choice

Determine exactly one bounded M3 execution method class for a future separately authorized execution, choosing among:

1. the declared Gradle `syncFlutterAar` task;
2. direct `flutter build aar` using the already recorded release-only command family;
3. another exact bounded method only if it can be justified solely from the accepted recorded facts without new technical reads.

The review must explain why the chosen method is the narrowest method that can answer the generation-success / release-artifact-production evidence gap, and why the non-selected methods are not selected at this gate.

No method choice itself grants command authority.

### 5.2 Exact local root and working directory

The evidence root remains exactly:

`D:\EliteSync-v10`

The review must state the exact working directory required by the chosen future method. It must not infer a directory not supported by accepted recorded facts. If the exact working directory cannot be established from the task plus promoted handoff, classify it `UNRESOLVED — FRESH AUTHORITY REQUIRED` and keep execution blocked.

No directory listing, path discovery, or target probe may be used to resolve it.

### 5.3 Local HEAD / state-context contract

Define the exact future execution precondition for local Git identity using non-content Git metadata only.

At minimum, the contract must:

- bind the future execution to the Owner-approved `D:\EliteSync-v10` root;
- require an exact declared local HEAD before execution;
- distinguish local HEAD context from remote `main` authority;
- not equate a bound root with a clean worktree;
- not inspect or require inspection of protected unrelated staged state;
- not use `git status` or untracked checks;
- stop before execution on a HEAD mismatch or root mismatch.

The governance review must say whether any branch-name check is actually necessary. It must not invent a clean-worktree requirement.

### 5.4 Source reads inherently implied by build execution

Define a strict distinction between:

- human/agent content inspection, which remains unauthorized absent separate authority; and
- source/configuration reads that a future authorized build tool would inherently perform as part of executing the exact chosen method.

The review must bound tool-inherent reads to the minimum project/module scope inherently required by the chosen method and must prohibit using those implicit tool reads as a backdoor for agent inspection, summarization, search, enumeration, or architecture inference.

### 5.5 Generated-output write scope

Define an exact allowlist of generated-output write locations or a deterministically bounded generated-output subtree required by the chosen method.

The review must distinguish:

- project/module generated build outputs;
- local host-output Maven/release artifact production;
- incidental tool metadata writes, if any;
- prohibited writes to tracked/protected unrelated source state.

If the chosen method can write outside a safely bounded allowlist and the exact scope cannot be established from accepted facts, keep execution blocked pending fresh authority.

No generated-output write is authorized by this governance candidate itself.

### 5.6 Cache read/write policy

Separately govern cache reads and cache writes.

The review must state, for each cache class inherently implicated by the chosen method, whether future execution would permit:

- read only;
- read/write;
- no access;
- or `UNRESOLVED — FRESH AUTHORITY REQUIRED`.

It must distinguish project-local generated/tool state from user/global Flutter/Dart/Gradle caches where applicable. It must not inspect caches while making the governance decision.

A method that requires unknown or unbounded cache mutation must remain blocked until separately authorized.

### 5.7 Network and dependency-download policy

Network authority is not inherited.

The review must make network policy explicit and separate from method selection. Default governance posture is:

`NO NETWORK / NO NEW DEPENDENCY DOWNLOAD UNLESS A LATER FRESH AUTHORITY EXPLICITLY GRANTS IT`

The review must determine whether the selected method can be meaningfully attempted under that no-network boundary. If not determinable from accepted facts, classify the network requirement as unresolved and keep execution blocked rather than testing connectivity or repositories.

No repository reachability test, package download, remote metadata lookup, or dependency-resolution probe is authorized.

### 5.8 Flutter / Gradle / toolchain prerequisite checks

Define the smallest non-destructive prerequisite check set for a future separately authorized M3 execution.

The review must classify each proposed check by:

- exact command, if known;
- working directory;
- whether it is read-only or may mutate local state;
- whether it may use network;
- success criterion;
- stop condition.

Potential checks must not be run by the governance candidate. Any check with unknown mutation or network behavior must stay unauthorized until separately resolved.

A prerequisite failure must be classified separately from generation failure.

### 5.9 Protected unrelated staged-state interaction

Protected unrelated staged state remains opaque and protected.

The future method must not require inspection, citation, mutation, un-staging, reset, cleanup, overwrite, or commit of unrelated staged state. The review must define a stop rule if the chosen execution method cannot be safely bounded away from protected unrelated staged state without inspecting that state.

No cleanliness claim may be inferred.

### 5.10 Failure / timeout / partial-output handling

Define exact future handling for:

- prerequisite failure;
- command start failure;
- non-zero exit;
- timeout;
- interruption;
- network-attempt violation;
- write-scope violation;
- partial generated output;
- indeterminate result.

The review must define a finite timeout or a bounded timeout policy before future execution. It must state that partial outputs are not to be inspected unless a later execution task expressly authorizes an exact bounded post-run evidence read.

No failure category may be escalated into `INVALID INTEGRATION` without separate evidence and authority.

### 5.11 Cleanup versus non-cleanup policy

Default to non-destructive preservation.

The review must explicitly choose between:

- no cleanup, preserving any bounded partial generated state for later governed evidence review; or
- a precisely enumerated cleanup action set that would itself require explicit later authority.

`git clean`, `git reset`, recursive deletion, cache purge, broad build-directory deletion, or removal of protected staged state must not be silently authorized.

If safe cleanup cannot be exactly bounded, select `NO CLEANUP`.

### 5.12 Exact success/failure evidence classifications

The review must define mutually distinct evidence classifications for a future M3 run. At minimum it must include bounded classes equivalent to:

- `M3 PRECONDITION BLOCKED` — root/HEAD/prerequisite/authority gate not satisfied; no generation attempt conclusion;
- `M3 GENERATION COMMAND SUCCESS` — the exact authorized generation command completed with its defined success exit condition;
- `M3 GENERATION COMMAND FAILURE` — the exact authorized generation command ran and failed or exited non-zero;
- `M3 TIMEOUT / INTERRUPTED` — no success/failure overreach beyond the observed termination state;
- `M3 SIDE-EFFECT / AUTHORITY VIOLATION BLOCKED` — execution must stop when a required side effect exceeds authority;
- `M3 RESULT INDETERMINATE` — the evidence obtained is insufficient for a narrower conclusion.

The review must separately state what exact post-run evidence, if any, would be required to elevate `M3 GENERATION COMMAND SUCCESS` into `ACTUAL FLUTTER AAR GENERATION SUCCESS / RELEASE ARTIFACT PRODUCTION STATE`.

Preserve exactly:

`GENERATION SUCCESS != DEPENDENCY-RESOLUTION SUCCESS`

`GENERATION FAILURE != INVALID INTEGRATION`

M2 targeted Gradle dependency-resolution remains a later separate gate even if M3 generation succeeds.

### 5.13 Candidate publication and fresh ACCEPT / REJECT boundary

The later governance candidate must publish only a governance review document and must not execute M3.

Required candidate branch:

`review/h01-bib03-u15-td01-m3-method-side-effect-governance-v0-1`

Required candidate path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M3_FLUTTER_AAR_GENERATION_BUILD_EXECUTION_METHOD_SIDE_EFFECT_GOVERNANCE_REVIEW_V0_1.md`

Required candidate commit subject:

`docs: add H-01 BIB-03 U-15 TD-01 M3 method governance review`

Required topology:

- candidate branch starts from this exact task-sheet commit;
- exactly one candidate commit;
- candidate commit has this task-sheet commit as its sole parent;
- candidate commit changes exactly the required candidate review path and no other path;
- no merge commit;
- no rebase/cherry-pick onto another parent;
- no modification of this task sheet in the candidate commit.

The governance candidate must STOP immediately after publishing that one candidate commit. It must not promote itself to `main`.

A fresh GPT L3 review must independently verify, before any acceptance or execution authority is considered:

1. fresh remote `main` authority;
2. this task commit identity and sole-parent relationship to its durable base;
3. task path/blob;
4. candidate branch, sole parent, subject, changed-path topology, and candidate blob;
5. candidate content against all thirteen governance requirements and permanent boundaries;
6. absence of any M3 execution claim or unauthorized scope expansion.

The fresh reviewer must return explicit `ACCEPT` or `REJECT`. Acceptance of the governance candidate is not itself M3 execution authority. A separate later execution task sheet would still be required.

## 6. Explicit non-authorities

This task sheet does not authorize any of the following:

`TASK SHEET != M3 EXECUTION AUTHORITY`

`GOVERNANCE REVIEW != BUILD AUTHORITY`

`METHOD CHOICE != COMMAND AUTHORITY`

`WORKING-DIRECTORY DETERMINATION != SOURCE-READ AUTHORITY`

`TOOL-INHERENT SOURCE READ GOVERNANCE != AGENT SOURCE-INSPECTION AUTHORITY`

`WRITE-SCOPE GOVERNANCE != GENERATED-OUTPUT WRITE AUTHORITY`

`CACHE POLICY GOVERNANCE != CACHE ACCESS AUTHORITY`

`NETWORK POLICY GOVERNANCE != NETWORK AUTHORITY`

`PREREQUISITE-CHECK DESIGN != PREREQUISITE-CHECK EXECUTION AUTHORITY`

`FAILURE-HANDLING GOVERNANCE != CLEANUP AUTHORITY`

`SUCCESS CLASSIFICATION != DEPENDENCY-RESOLUTION SUCCESS`

`FRESH ACCEPTANCE OF M3 GOVERNANCE != M3 EXECUTION AUTHORITY`

## 7. Stop condition

Publish this task sheet as the sole change on its task branch, record its exact commit and blob, and STOP.

Do not author the M3 governance review in the same session.

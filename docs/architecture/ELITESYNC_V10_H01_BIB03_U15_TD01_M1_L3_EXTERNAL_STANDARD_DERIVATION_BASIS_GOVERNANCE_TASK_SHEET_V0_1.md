# EliteSync v10 H-01 BIB-03 U-15 TD-01 M1 L3 External-Standard Derivation-Basis Governance Task Sheet v0.1

Status: `PROPOSED — TD-01 M1 L3 EXTERNAL-STANDARD DERIVATION-BASIS GOVERNANCE ONLY — OFFICIAL EXTERNAL DOCUMENTATION REVIEW AUTHORIZED WITHIN EXACT BOUNDS — ZERO ARTIFACT LOCATOR MATERIALIZATION — ZERO ARTIFACT PROBE — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO GRADLE/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-m1-l3-external-standard-derivation-basis-governance-v0-1`

Exact durable live-main authority required at task start:

`4b7e55243abb63d183c0ba59a17b5b0fe5f5f8dd`

This task authorizes one later fresh GPT L3/Codex governance candidate action for exactly one purpose: determine whether a minimal authoritative official external-standard/documentation basis can be established under Route L3 so that a later separately authorized derivation-materialization task could deterministically map the already durable V10 Maven repository / coordinate facts to one exact local Maven artifact file locator without repository search, generated-output inspection, guessed filename/layout conventions, Gradle execution, dependency resolution, network artifact probing, technical design, or implementation.

This task does NOT materialize the artifact locator.

This task does NOT authorize M1 execution.

This task does NOT authorize an artifact existence/type/content/hash check.

## 1. Permanent governance boundaries

Global/cross-project memory is NOT EliteSync-v10 governance authority.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no generated-output inspection, no local Maven-directory inspection, no cache inspection, no Gradle/build execution, no dependency-resolution execution, no repository/network artifact availability test, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

Historical incidents, rejected candidates, stale tasks, and deviations remain historical truth and must not be rewritten as if they did not occur.

The stale task `ad84587a8bd0d7fb5e373417aa94b33be936b58c` remains stale and not current authority.

The rejected existence-result candidate `701e0ad03b5fc22694d8fcef18250580e4549e82` remains rejected and must not be promoted, reused, amended, rebased, cherry-picked, merged, or used as substantive basis.

## 2. Exact durable basis A — accepted M1 artifact-locator governance disposition

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_ARTIFACT_LOCATOR_DERIVATION_AND_ACQUISITION_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`d2907585f53806224b2febb9b04d346998c0ecfe`

Preserve from Basis A:

- M1 remains the selected first evidence-method class;
- current durable V10 declarations identify the Flutter-module host-output Maven repository source, a declared release-artifact directory, and app-level Maven coordinates;
- current durable V10 declarations do NOT explicitly identify one final local artifact file locator;
- silent Maven-layout / artifact-filename convention import is not authorized;
- M1 cannot yet be executed;
- restart gate = either Owner-supplied exact artifact locator under L1 or a later separately authorized external-standard derivation basis under L3;
- no artifact locator is materialized, acquired, tested, or authorized.

Preserve exactly:

`EXACT REPOSITORY DIRECTORY != EXACT ARTIFACT FILE LOCATOR`

`MAVEN COORDINATE != EXACT LOCAL ARTIFACT FILE LOCATOR UNDER CURRENT AUTHORITY`

`EXPLICIT DIRECTORY + COORDINATE != EXPLICIT ARTIFACT FILENAME`

`DECLARED MAVEN COORDINATE != AUTHORITY TO INVENT MAVEN FILE LAYOUT`

`NO EXACT ARTIFACT FILE LOCATOR != ARTIFACT DOES NOT EXIST`

`LOCATOR ACQUISITION != METHOD EXECUTION AUTHORITY`

## 3. Exact durable basis B — app-level AAR declaration facts

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_HOST_NATIVE_BUILD_INTEGRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Required blob:

`b201348a6a62f7bd2c7d09d1f20b20554cdabef3`

Preserve only these exact revision-scoped declaration facts:

- Flutter module directory reference = `rootProject.file("../flutter_elitesync_module")`;
- declared release AAR output location = `File(flutterModuleDir, "build/host/outputs/repo/com/elitesync/flutter_elitesync_module/flutter_release/1.0")`;
- `syncFlutterAar` is declared as an `Exec` task using `flutter build aar --no-debug --no-profile` plus recorded dart-define arguments;
- `debugImplementation` declares `com.elitesync.flutter_elitesync_module:flutter_release:1.0`;
- `profileImplementation` declares `com.elitesync.flutter_elitesync_module:flutter_profile:1.0`;
- `releaseImplementation` declares `com.elitesync.flutter_elitesync_module:flutter_release:1.0`.

Preserve exactly:

`DECLARED AAR GENERATION TASK != AAR BUILD SUCCESS`

`DECLARED AAR DEPENDENCY != RESOLVED AAR DEPENDENCY`

`DECLARED ARTIFACT OUTPUT PATH != ARTIFACT AVAILABILITY`

The durable basis does NOT explicitly name a final `.aar` file locator.

## 4. Exact durable basis C — settings-level repository-source facts

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_SETTINGS_LEVEL_FLUTTER_AAR_REPOSITORY_REGISTRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Required blob:

`9ad03334b39779879124c4463d70b684480fc625`

Preserve only these revision-scoped declaration facts:

- `dependencyResolutionManagement` declares `RepositoriesMode.PREFER_SETTINGS`;
- dependency-resolution repositories declare `maven(url = uri("../flutter_elitesync_module/build/host/outputs/repo"))`;
- settings-level repository declarations do not establish artifact presence or dependency-resolution success.

Preserve exactly:

`SETTINGS-LEVEL REPOSITORY DECLARATION != DEPENDENCY RESOLUTION SUCCESS`

`DECLARED REPOSITORY SOURCE != ARTIFACT AVAILABILITY`

## 5. Exact substantive V10 repository-content read budget

Exactly FOUR substantive EliteSync-v10 repository-content reads are authorized for the later candidate action:

1. this exact task sheet;
2. Basis A at required blob `d2907585f53806224b2febb9b04d346998c0ecfe`;
3. Basis B at required blob `b201348a6a62f7bd2c7d09d1f20b20554cdabef3`;
4. Basis C at required blob `9ad03334b39779879124c4463d70b684480fc625`.

No fifth substantive EliteSync-v10 repository-content read is authorized.

Do NOT reread any V10 technical target, including:

- `apps/android/app/build.gradle.kts`;
- `apps/android/settings.gradle.kts`;
- any generated Maven repository output;
- any `.aar`, `.pom`, metadata, cache, build-output, or repository directory;
- `.metadata`;
- `pubspec.yaml`;
- `pubspec.lock`;
- AndroidManifest;
- Kotlin/Java source;
- CI/workflows;
- any other Gradle/settings/native-build file.

Do NOT read README, FD02, source repository, old repository, or rejected candidate content.

Minimum exact ref/commit/compare metadata required to verify current authority/topology is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 6. External-source authority and read budget

External web/documentation research IS authorized in this task solely to evaluate Route L3 derivation authority.

The candidate may use search/navigation only to locate authoritative official documentation.

Substantive external evidence may come only from official documentation or standards published by one or more of:

- Apache Maven / Maven official documentation under `maven.apache.org`;
- Flutter official documentation under `docs.flutter.dev`;
- Gradle official documentation under `docs.gradle.org`;
- Android Developers official documentation under `developer.android.com`.

No blog, Stack Overflow answer, Reddit post, forum answer, package registry listing, random GitHub repository/source file, unofficial mirror, AI-generated page, cached snippet, or legacy EliteSync source may serve as derivation authority.

Exactly up to SIX substantive external official-documentation page reads are authorized.

Search-result snippets do NOT count as derivation authority and must not be used as substantive evidence.

For each external source actually used, record:

- publisher / authority;
- exact page title;
- exact canonical URL;
- retrieval date;
- the specific rule the source supports;
- whether the source is normative/specification-like, product documentation, or explanatory documentation.

Use concise paraphrase. Do not reproduce long copyrighted passages.

If authoritative official documentation cannot establish the required rule set within this budget, fail closed.

## 7. Sole L3 governance question

Determine ONLY:

`IS THERE A MINIMAL AUTHORITATIVE OFFICIAL EXTERNAL-STANDARD / DOCUMENTATION BASIS SET THAT, WHEN LATER COMBINED WITH THE EXACT DURABLE V10 REPOSITORY ROOT / DECLARED RELEASE DIRECTORY / MAVEN COORDINATE FACTS, WOULD FULLY AND DETERMINISTICALLY DEFINE THE FINAL LOCAL MAVEN ARTIFACT FILE LOCATOR WITHOUT ANY REPOSITORY SEARCH, GENERATED-OUTPUT INSPECTION, GUESSED PATH, OR UNSTATED CONVENTION; AND IF SO, WHAT EXACT EXTERNAL RULE SET MUST BE FROZEN FOR A LATER SEPARATE LOCATOR-MATERIALIZATION TASK?`

This task does not apply that rule set to materialize a V10 artifact locator.

This task does not test any locator.

## 8. Required external rule dimensions

A sufficient L3 basis MUST explicitly and authoritatively cover every dimension needed to eliminate ambiguity.

### D1 — Maven repository path/layout mapping

The basis must establish the applicable mapping from Maven coordinates and repository root to repository path components, including any group-id-to-path conversion actually required.

### D2 — artifact filename construction

The basis must establish the applicable final artifact filename construction rule, including artifact identifier and version placement.

### D3 — artifact file type / extension

The basis must establish why the relevant generated/consumed Flutter artifact in this exact integration class has the file type/extension required for deterministic locator derivation.

A generic statement that “Flutter can build AARs” is insufficient unless it authoritatively binds the relevant output artifact class to `.aar` or equivalent exact file-type semantics.

### D4 — classifier / variant ambiguity

The basis must establish whether any classifier or additional filename component applies to the relevant release artifact, or establish that no classifier component applies for the selected release coordinate/output class.

If classifier state remains ambiguous, the L3 basis is insufficient.

### D5 — coordinate/output binding

The basis must be compatible with the durable V10 release coordinate `com.elitesync.flutter_elitesync_module:flutter_release:1.0` and declared release repository directory. It must not silently substitute a different artifact identity or variant.

If any D1–D5 dimension remains unresolved, do NOT declare the L3 basis sufficient.

## 9. Source-combination principles

A single official source is preferred if it authoritatively covers D1–D5.

If no single source covers all dimensions, the candidate may establish a minimal multi-source basis set, but each source must cover a non-duplicative required dimension and all sources must be mutually compatible.

Prefer more directly authoritative sources in this order where applicable:

1. formal/official Maven repository-layout or artifact-format documentation;
2. official Flutter documentation specifically describing `flutter build aar` / add-to-app AAR outputs;
3. official Gradle documentation for dependency/artifact notation only when needed to resolve an ambiguity not covered by Maven/Flutter docs;
4. Android Developers documentation for AAR artifact semantics only when needed to resolve exact file-type/extension meaning.

Do NOT combine unrelated documents merely to manufacture certainty.

Do NOT use inference from common practice where the official text does not support the exact transformation.

Preserve exactly:

`OFFICIAL DOCUMENTATION != V10 ARTIFACT PRESENCE`

`EXTERNAL STANDARD DERIVATION BASIS != ARTIFACT LOCATOR`

`EXTERNAL STANDARD DERIVATION BASIS != METHOD EXECUTION AUTHORITY`

`MAVEN LAYOUT RULE != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`STANDARD-SUPPORTED FILENAME RULE != ARTIFACT AVAILABILITY`

`EXTERNAL WEB RESEARCH != V10 REPOSITORY CONTENT READ`

## 10. Expected Outcome A — sufficient L3 derivation basis established

Use Outcome A only if the authoritative external basis set fully resolves D1–D5 without unstated convention.

If supported, classify exactly:

`U-15 TD-01 M1 L3 EXTERNAL-STANDARD DERIVATION-BASIS GOVERNANCE = A MINIMAL AUTHORITATIVE OFFICIAL EXTERNAL-DOCUMENTATION BASIS SET HAS BEEN ESTABLISHED THAT IS SUFFICIENT, WHEN LATER COMBINED WITH THE DURABLE V10 REPOSITORY ROOT / DECLARED RELEASE DIRECTORY / RELEASE MAVEN COORDINATE FACTS, TO DETERMINISTICALLY DERIVE ONE FINAL LOCAL MAVEN ARTIFACT FILE LOCATOR WITHOUT REPOSITORY SEARCH OR GENERATED-OUTPUT INSPECTION — THE FROZEN BASIS SET EXPLICITLY COVERS APPLICABLE MAVEN REPOSITORY PATH/LAYOUT MAPPING, FINAL ARTIFACT FILENAME CONSTRUCTION, THE RELEVANT FLUTTER AAR FILE TYPE/EXTENSION, CLASSIFIER/VARIANT AMBIGUITY, AND BINDING TO THE DURABLE RELEASE COORDINATE/OUTPUT CLASS — EXTERNAL STANDARD DERIVATION BASIS != ARTIFACT LOCATOR — NO V10 ARTIFACT LOCATOR MATERIALIZED / ACQUIRED / TESTED / AUTHORIZED — NO ARTIFACT PRESENCE CLAIM — NO COMMAND AUTHORIZED — ZERO ARTIFACT PROBE — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO GRADLE/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Outcome A means only:

- one minimal official external derivation-basis set is selected and durably recorded;
- D1–D5 are each explicitly supported;
- the basis may be used only by a later separately authored L3 locator-materialization governance task;
- no final V10 artifact locator is materialized in this action;
- no artifact existence/presence/freshness/build/resolution fact is established;
- M1 remains unexecuted;
- Gradle dependency-resolution state remains unproven;
- no architecture-entry, design, or implementation authority is created.

Preserve exactly:

`EXTERNAL STANDARD DERIVATION BASIS != ARTIFACT LOCATOR`

`STANDARD AUTHORITY != ARTIFACT PRESENCE`

`STANDARD AUTHORITY != FRESH GENERATION`

`STANDARD AUTHORITY != BUILD SUCCESS`

`STANDARD AUTHORITY != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`L3 BASIS ACCEPTANCE != M1 EXECUTION AUTHORITY`

## 11. Outcome B — sufficient L3 basis not established

If one or more D1–D5 dimensions cannot be authoritatively resolved within the allowed official-source budget, classify exactly:

`EXPECTED OUTCOME B — U-15 TD-01 M1 L3 EXTERNAL-STANDARD DERIVATION-BASIS GOVERNANCE DOES NOT ESTABLISH A SUFFICIENT AUTHORITATIVE RULE SET FOR DETERMINISTIC FINAL ARTIFACT-FILE LOCATOR DERIVATION UNDER CURRENT AUTHORITY — ONE OR MORE REQUIRED DIMENSIONS D1-D5 REMAIN UNRESOLVED — FAIL CLOSED — L1 OWNER-SUPPLIED EXACT ARTIFACT LOCATOR REMAINS AVAILABLE AS A SEPARATE RESTART ROUTE — NO V10 ARTIFACT LOCATOR MATERIALIZED / ACQUIRED / TESTED / AUTHORIZED — NO COMMAND AUTHORIZED — ZERO ARTIFACT PROBE — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO GRADLE/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Outcome B does NOT prove that the artifact does not exist.

Outcome B does NOT authorize repository inspection or a guessed path.

## 12. Operational blockage

For exact main/ref/blob/topology failure before substantive work, use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 M1 L3 EXTERNAL-STANDARD DERIVATION-BASIS GOVERNANCE DETERMINATION MADE`

If `main` is not exactly `4b7e55243abb63d183c0ba59a17b5b0fe5f5f8dd` at task start, STOP before substantive repository-content read except minimum exact ref metadata needed to detect mismatch.

If the review branch advances beyond this exact task-sheet commit before candidate publication, fail closed rather than rewriting or substituting history.

## 13. Mandatory zero activity

The candidate action MUST perform ZERO:

- V10 artifact locator materialization;
- artifact locator testing;
- artifact existence/type/content/hash checks;
- generated-output inspection;
- local Maven directory inspection;
- Gradle cache inspection;
- repository-tree inspection;
- directory listing used for discovery;
- wildcard/glob testing;
- filename search in V10;
- code search in V10;
- GitHub code search in V10;
- repository-wide or directory-wide enumeration;
- V10 path discovery;
- Gradle/build execution;
- dependency-resolution execution;
- `syncFlutterAar` execution;
- `flutter build aar` execution;
- repository/network artifact reachability tests;
- artifact download/upload;
- source repository access;
- old-repository access;
- README use;
- FD02 use;
- architecture selection;
- Backend/API/Database/Flutter/infrastructure/deployment design;
- implementation planning;
- implementation;
- participant/data activity;
- LC-03;
- LC-04;
- Phase 36.

External web search/navigation is authorized only within Section 6 and only for official documentation needed for L3 rule-basis evaluation.

## 14. Required candidate artifact

For Outcome A or Outcome B create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_L3_EXTERNAL_STANDARD_DERIVATION_BASIS_GOVERNANCE_REVIEW_V0_1.md`

The candidate must include:

1. exact task authority and durable `main` authority at task start;
2. exact V10 substantive read accounting;
3. external source accounting with title / publisher / canonical URL / retrieval date / rule supported;
4. D1–D5 support matrix;
5. minimal-basis-set determination;
6. exact Outcome A or Outcome B classification;
7. all preserved boundaries;
8. zero-activity accounting.

## 15. Candidate publication constraints

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_L3_EXTERNAL_STANDARD_DERIVATION_BASIS_GOVERNANCE_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: govern H-01 BIB-03 U-15 TD-01 M1 L3 derivation basis`

Publish non-force only to:

`review/h01-bib03-u15-td01-m1-l3-external-standard-derivation-basis-governance-v0-1`

Do NOT modify or promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent `ACCEPT / REJECT`.

## 16. Strict phase separation

L3 task-sheet authoring -> STOP.

Candidate L3 external-standard derivation-basis governance -> STOP.

Fresh GPT L3 ACCEPT/REJECT -> STOP.

If Outcome A is accepted and promoted: actual L3 locator derivation/materialization -> separately authored later task.

Any M1 artifact existence probe -> separately authored later task after one exact locator becomes durable.

Any M2 Gradle dependency-resolution governance/execution -> separately authorized later task if still required after M1 disposition.

Architecture-entry determination -> separately authorized later governance task.

Technical design -> separately authorized later task.

Implementation planning / implementation -> separately authorized later task.

# EliteSync v10 H-01 BIB-03 U-15 TD-01 TLA-01 Host / Native Build Integration Exact Content-Read Task Sheet v0.1

Status: `PROPOSED — TD-01 HOST / NATIVE BUILD INTEGRATION EXACT CONTENT READ ONLY — ONE EXACT TARGET CONTENT READ — REVISION-SCOPED SINGLE-FILE FACT EXTRACTION ONLY — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-tla01-host-native-build-integration-exact-content-read-v0-1`

Exact durable live-main authority required at task start:

`90f3b52917c96871722ac58b017800645608bded`

This task authorizes one later fresh GPT L3/Codex candidate action for exactly one purpose: read exactly once the complete content at the already durably accepted and revision-scoped HIT EliteSync-v10 locator `apps/android/app/build.gradle.kts` at exact revision `5ed1c04b822b72327864e9c6de63a80a77e88eb4`, extract only explicit single-file `HOST / NATIVE BUILD INTEGRATION` facts from that exact content, and record those facts without reading settings-level repository registration, adjacent Gradle/native files, source files, manifests, CI/workflows, old/source repository content, or making architecture, inheritance, implementation-planning, or implementation decisions.

## 1. Memory isolation and permanent boundaries

Global/cross-project memory is intentionally NOT an authority source for EliteSync-v10.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

## 2. Durable exact locator and HIT basis

Durable host/native build integration TLA-01 intake result:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_HOST_NATIVE_BUILD_INTEGRATION_OWNER_EXACT_LOCATOR_INTAKE_REVIEW_V0_1.md`

Required blob:

`71a2d58de4c063046956d98a7854ee22809117c1`

Durable host/native build integration exact-existence HIT result:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_HOST_NATIVE_BUILD_INTEGRATION_EXACT_EXISTENCE_REVIEW_V0_1.md`

Required blob:

`1cbedcc298fcb108476c8225886350460c4451ba`

Exact content-read revision:

`5ed1c04b822b72327864e9c6de63a80a77e88eb4`

Exact content-read locator:

`apps/android/app/build.gradle.kts`

Durably selected evidence-gap class:

`HOST / NATIVE BUILD INTEGRATION`

Named non-design question:

`WHAT HOST / NATIVE BUILD INTEGRATION FACTS — INCLUDING ANY EXPLICIT FLUTTER MODULE LOCATION, FLUTTER AAR GENERATION TASK, NATIVE PREBUILD DEPENDENCY, VARIANT-SPECIFIC AAR DEPENDENCY CONSUMPTION, OR OTHER DIRECTLY DECLARED ANDROID-HOST BUILD-INTEGRATION FACTS — ARE PRESENT IN THE CONTENT AT THIS EXACT OWNER-SUPPLIED V10 LOCATOR?`

The prior accepted HIT establishes only that this exact locator resolves at this exact revision.

Preserve exactly:

`EXACT REVISION+LOCATOR HIT != FILE TYPE`

`EXACT REVISION+LOCATOR HIT != CONTENT FACT`

`EXACT REVISION+LOCATOR HIT != ANDROID HOST FACT`

`EXACT REVISION+LOCATOR HIT != FLUTTER AAR FACT`

`EXACT REVISION+LOCATOR HIT != GRADLE INTEGRATION FACT`

`SINGLE FILE HIT / CONTENT FACT != COMPLETE HOST BUILD GRAPH`

`REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT`

`CONTENT FACT != ARCHITECTURE CHOICE`

`CONTENT FACT != INHERITANCE DECISION`

## 3. Exact authorized substantive-content read budget

Exactly FOUR substantive repository-content reads are authorized for the candidate action:

1. this exact task sheet at this exact task-sheet commit;
2. the durable exact-existence HIT result at exact durable `main`, required blob `1cbedcc298fcb108476c8225886350460c4451ba`;
3. the durable TLA-01 intake result at exact durable `main`, required blob `71a2d58de4c063046956d98a7854ee22809117c1`;
4. exactly one complete content read of `apps/android/app/build.gradle.kts` at exact revision `5ed1c04b822b72327864e9c6de63a80a77e88eb4`.

No fifth substantive repository-content read is authorized.

The target content read may occur exactly once. Do not reread any portion of the target through a second repository operation.

Minimum ref/commit metadata needed to verify exact authority/topology is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 4. Sole authorized target-content read

Preferred exact read command:

`git show '5ed1c04b822b72327864e9c6de63a80a77e88eb4:apps/android/app/build.gradle.kts'`

An equivalent exact read is allowed only if it retrieves the complete content of that same exact revision+locator once and does not retrieve type metadata, directory listings, adjacent files, history, blame, diffs, or other repository content.

Do NOT perform an existence check again before or after the content read.

Do NOT perform a type check.

Do NOT hash or re-fetch the target through another repository operation.

If the one exact content read unexpectedly fails despite the durable HIT, STOP and classify operational blockage; do not diagnose through another repository operation.

## 5. Authorized fact extraction

From the one exact target content only, record only facts explicitly present and directly relevant to the selected `HOST / NATIVE BUILD INTEGRATION` evidence-gap class.

The fact register may include, when explicitly present:

### A. Explicit Flutter module location/reference

Record literal or semantically equivalent values that directly identify a Flutter module directory/path/reference used by this build script.

Do not inspect the referenced path and do not infer its existence beyond what this file declares.

### B. Explicit Flutter AAR generation task

Record task name, working directory, executable/command, arguments, outputs only when directly declared and materially relevant to AAR generation or host integration.

A declared task does not prove successful execution or that outputs exist.

### C. Explicit native build/task dependency

Record direct task dependency relationships such as a pre-build task depending on an AAR-generation task, if explicitly declared.

A declared task edge does not prove that the task graph has executed successfully.

### D. Explicit variant-specific Flutter artifact consumption

Record exact configuration names and dependency coordinates/source forms for Flutter/AAR artifacts consumed by the app/module when explicitly declared, including debug/profile/release distinctions when present.

Declared dependency coordinates do not prove repository registration, resolution, artifact availability, successful linkage, or build success.

### E. Other direct single-file host/native integration facts

Record other statements from this exact file only when they materially establish how this Android/Gradle app/module declares integration with Flutter-produced artifacts.

Do not use ordinary Android build configuration unrelated to Flutter/native integration as evidence merely because it is present.

## 6. Explicitly prohibited inference

Do NOT claim from this single file alone:

- that `apps/android/settings.gradle.kts` exists or registers any repository;
- that a Flutter AAR repository is available or correctly registered;
- that any AAR has actually been generated;
- that any declared dependency has resolved;
- that preBuild/build/test/run/deploy succeeded;
- that an Android host runtime actually launches Flutter;
- that a launcher activity, manifest, FlutterActivity, FlutterEngine, MethodChannel, or runtime embedding exists;
- that this file establishes the complete native host/build graph;
- that Android/Gradle/Flutter AAR integration is the final or preferred V10 architecture;
- that old EliteSync build-graph behavior should be inherited;
- that another host/native integration path exists or does not exist;
- any fact from settings, manifests, Kotlin/Java source, adjacent Gradle files, CI/workflows, or other files not literally present in this target;
- any Backend/API/Database/Flutter/infrastructure design conclusion.

Preserve exactly:

`DECLARED FLUTTER MODULE PATH != VERIFIED MODULE PATH`

`DECLARED AAR GENERATION TASK != AAR BUILD SUCCESS`

`DECLARED TASK DEPENDENCY != SUCCESSFUL TASK EXECUTION`

`DECLARED AAR DEPENDENCY != RESOLVED AAR DEPENDENCY`

`SINGLE FILE CONTENT FACT != SETTINGS-LEVEL REPOSITORY REGISTRATION`

`SINGLE FILE CONTENT FACT != COMPLETE HOST BUILD GRAPH`

`REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT`

`CONTENT FACT != ARCHITECTURE CHOICE`

`CONTENT FACT != INHERITANCE DECISION`

## 7. Sole execution question

Determine ONLY:

`WHAT HOST / NATIVE BUILD INTEGRATION FACTS ARE EXPLICITLY PRESENT IN THE ONE AUTHORIZED CONTENT READ OF apps/android/app/build.gradle.kts AT ELITESYNC-V10 REVISION 5ed1c04b822b72327864e9c6de63a80a77e88eb4?`

## 8. Result A — revision-scoped single-file host/native integration fact register established

If the one exact target content read succeeds and supports a truthful bounded fact register, use exactly this classification:

`U-15 TD-01 HOST / NATIVE BUILD INTEGRATION EXACT CONTENT READ = ONE AUTHORIZED COMPLETE CONTENT READ OF apps/android/app/build.gradle.kts AT PREDECLARED ELITESYNC-V10 REVISION 5ed1c04b822b72327864e9c6de63a80a77e88eb4 COMPLETED — REVISION-SCOPED SINGLE-FILE HOST / NATIVE BUILD INTEGRATION FACT REGISTER EXTRACTED FROM THIS EXACT CONTENT ONLY — EXPLICIT DECLARED BUILD-INTEGRATION FACTS ONLY — NO SETTINGS / OTHER GRADLE / MANIFEST / SOURCE / CI / ADJACENT-FILE / HISTORY / SOURCE-REPOSITORY CORROBORATION — SINGLE FILE CONTENT FACT != COMPLETE HOST BUILD GRAPH — REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT — CONTENT FACT != ARCHITECTURE CHOICE — CONTENT FACT != INHERITANCE DECISION — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

The candidate must then include a concise structured fact register covering only the authorized categories in Section 5 actually supported by the target content.

Candidate accounting:

- candidate host/native build integration exact-content-read reviews = `1`;
- accepted/durable exact-content-read reviews = `0`;
- target `apps/android/app/build.gradle.kts` content reads = `1`;
- additional target rereads = `0`;
- settings-level reads = `0`;
- other Gradle/native-build reads = `0`;
- manifest/source/CI/adjacent-file reads = `0/0/0/0`;
- `.metadata` rereads = `0`;
- `pubspec.yaml` rereads = `0`;
- `pubspec.lock` reads = `0`;
- type checks = `0`;
- existence rechecks = `0`;
- repository search/enumeration/path discovery = `0/0/0`;
- technical-design actions = `0`;
- implementation-planning actions = `0`;
- implementation actions = `0`.

## 9. Operational blockage

If exact main/ref/blob/topology validation fails before the target content read, or if the one exact target content read unexpectedly fails, use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 HOST / NATIVE BUILD INTEGRATION CONTENT FACT REGISTER DETERMINATION MADE`

Do not diagnose target-read failure through another repository operation.

If the review branch advances beyond this exact task-sheet commit before candidate execution/publication, fail closed rather than rewriting or substituting history.

If the target is accidentally read more than once, do not conceal it; stop and report the deviation for fresh GPT L3 disposition.

## 10. Mandatory zero activity beyond the authorized reads

The candidate action MUST perform ZERO:

- second read or partial reread of `apps/android/app/build.gradle.kts` through repository operations;
- `apps/android/settings.gradle.kts` check or read;
- any other Gradle/settings/native-build content read;
- AndroidManifest read;
- Kotlin/Java source read;
- CI/workflow read;
- `.metadata` reread;
- `pubspec.yaml` reread;
- `pubspec.lock` read;
- type check of target;
- existence recheck of target;
- alternate locator or revision test;
- repository-wide or directory-wide enumeration;
- repository-tree inspection;
- filename search;
- code search;
- GitHub code search;
- path discovery;
- source repository access;
- old-repository access;
- source-content reads;
- Backend/API/Database/Flutter/infrastructure/deployment design;
- architecture selection;
- implementation planning;
- implementation;
- participant/data activity;
- README use;
- FD02 use;
- LC-03;
- LC-04;
- Phase 36.

## 11. Required candidate publication

Create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_HOST_NATIVE_BUILD_INTEGRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_HOST_NATIVE_BUILD_INTEGRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: read H-01 BIB-03 U-15 TD-01 host native build integration content`

Publish non-force only to:

`review/h01-bib03-u15-td01-tla01-host-native-build-integration-exact-content-read-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent ACCEPT/REJECT.

## 12. Strict phase separation

Task-sheet authoring -> STOP.

Candidate exact-content-read / fact extraction -> STOP.

Acceptance/promotion -> STOP.

Single-file host/native integration fact sufficiency / next-gap disposition -> separately authorized later governance task.

Any second host/native integration locator, including `apps/android/settings.gradle.kts`, -> separately governed only after durable single-file fact sufficiency is known.

Architecture-entry determination -> separately authorized later governance task after sufficient bounded technical facts.

Technical design -> separately authorized later task.

Implementation planning / implementation -> separately authorized later task.

# EliteSync v10 H-01 BIB-03 Manifest-Guided Bounded Locator Method Review Task Sheet v0.1

## 1. Purpose and publication non-authority

This document defines exactly one possible later governance review:

`H-01 BIB-03 MANIFEST-GUIDED BOUNDED LOCATOR METHOD REVIEW — GOVERNANCE ONLY`

Owner selected this direction after:

- accepted `BIB03-HYP-01` convention probe = `MISS`;
- accepted `BIB03-HYP-02` convention probe = `MISS`;
- accepted bounded `HYP-ORIGIN-02` documentary-basis result = qualifying exact source-path count `0`.

These results remain bounded negative records only.

Preserve:

`TWO CONVENTION HYPOTHESIS MISSES != TP-SOURCE-CLASS-01 EMPTY`

`ZERO HYP-ORIGIN-02 DOCUMENTARY PATHS != NO RELEVANT SOURCE EXISTS`

Publication of this task sheet does NOT execute the proposed method and does NOT authorize:

- any manifest path hypothesis;
- any exact-path repository probe;
- any manifest existence check;
- any manifest read;
- any config/build/runtime read;
- repository-wide or directory-wide discovery;
- repository path enumeration;
- source filename search;
- repository-source metadata inspection;
- source-content reads;
- technical-source reads;
- Flutter/client source reads;
- package-root identification;
- candidate source identification;
- candidate acceptance;
- bounded basis-instance acceptance;
- source-target binding;
- inspection;
- technical/source read-budget authorization;
- evidence collection;
- U-15 fact resolution;
- PUI reconsideration;
- design;
- implementation;
- Safety Operations;
- legal research;
- LC-03;
- LC-04;
- Phase 36.

Repository:

`zcx369658780/EliteSync-v10`

Publication parent authority:

`7b7cc723d855b2922375a6d6cfbe44a826092b39`

## 2. Proposed method model

Review only:

`BIB03-METHOD-02 — MANIFEST-GUIDED BOUNDED LOCATOR METHOD — STAGED EXACT-MANIFEST IDENTITY THEN SEPARATELY AUTHORIZED MINIMAL MANIFEST METADATA READ — NON-ENUMERATIVE — FAIL CLOSED`

This is a new method model. It is NOT an extension, retry, or automatic successor execution of `BIB03-METHOD-01`.

Preserve:

`NEW METHOD MODEL != OLD METHOD RETRY`

`METHOD MODEL ACCEPTED != METHOD EXECUTED`

`METHOD EXECUTED != MANIFEST READ AUTHORIZED`

`MANIFEST IDENTITY ESTABLISHED != MANIFEST CONTENT READ AUTHORIZED`

The method is staged so that exact manifest identity and manifest content reading can never be conflated.

## 3. Stage ML-1 — future exact-manifest identity gate

Future Stage ML-1 may exist only under a separate task sheet and independent GPT L3 review.

Stage ML-1 may permit only a finite predeclared set of exact manifest-path hypotheses and metadata-only identity probes.

Stage ML-1 may establish only:

1. exact manifest path existence at one immutable commit;
2. Git object type;
3. Git blob SHA.

Stage ML-1 must NOT read manifest bytes.

Stage ML-1 must NOT parse YAML, JSON, TOML, XML, Gradle, Dart, package-manager, build-system, workspace, dependency, or runtime configuration content.

Stage ML-1 must NOT identify a package root merely because a manifest-like filename exists.

Preserve:

`MANIFEST PATH HYPOTHESIS != MANIFEST EXISTS`

`MANIFEST EXISTS != MANIFEST RELEVANT`

`MANIFEST EXISTS != PACKAGE ROOT ACCEPTED`

`MANIFEST IDENTITY PROBE != MANIFEST READ`

`MANIFEST IDENTITY PROBE != SOURCE INSPECTION`

`MANIFEST PROBE BUDGET != TECHNICAL/SOURCE READ BUDGET`

No Stage ML-1 path is instantiated in the current review.

Current manifest-path hypotheses instantiated = `0`.

Current manifest identity probes executed = `0`.

## 4. Eligible future manifest-hypothesis origin classes

Review exactly these three future origin classes:

`MANIFEST-ORIGIN-01 — DETERMINISTIC STANDARD MANIFEST EXACT-PATH HYPOTHESIS PREDECLARED IN A SEPARATE EXECUTION TASK SHEET`

`MANIFEST-ORIGIN-02 — EXACT MANIFEST PATH EXPLICITLY PRESENT IN ALREADY ACCEPTED NON-SOURCE DOCUMENTARY TEXT AND ACCEPTED FOR REUSE`

`MANIFEST-ORIGIN-03 — EXACT OWNER-SUPPLIED MANIFEST PATH USED AS A BOUNDED FUTURE INPUT`

No fourth class.

No origin class is instantiated in this review.

Do not create any actual manifest filename or path hypothesis here.

Preserve:

`ORIGIN CLASS ELIGIBLE != ORIGIN INSTANCE AVAILABLE`

`ORIGIN INSTANCE AVAILABLE != MANIFEST EXISTS`

`MANIFEST FILENAME CONVENTION != SOURCE PATH`

## 5. Stage ML-2 — future bounded manifest metadata-read gate

Stage ML-2 may be proposed only after:

1. a separate Stage ML-1 execution has established exactly one manifest identity under accepted authority;
2. that Stage ML-1 outcome has passed independent GPT L3 review and promotion;
3. a separate Stage ML-2 task sheet explicitly requests a bounded technical/config/manifest read authority;
4. that Stage ML-2 task sheet independently defines the exact read budget and allowed fields/semantics.

The current review creates no Stage ML-2 read authority.

A future manifest content read is a technical/config/manifest read and MUST NOT be hidden inside metadata-only terminology.

If later authorized, the read must increment the applicable technical/source-content governance counters according to the separately accepted read-budget decision.

The current cumulative accepted technical/source-content reads remain exactly:

`10`

Preserve:

`MANIFEST READ IS A TECHNICAL/CONFIG READ`

`MANIFEST METADATA-ONLY SEMANTICS != ZERO-BYTE CONTENT READ`

`MANIFEST READ AUTHORITY != SOURCE READ AUTHORITY`

`MANIFEST READ AUTHORITY != PACKAGE ROOT ACCEPTED`

`MANIFEST READ AUTHORITY != SOURCE TARGET IDENTIFIED`

`MANIFEST READ AUTHORITY != SOURCE-TARGET BINDING`

## 6. Future ML-2 allowed semantic objective

If Stage ML-2 is ever separately authorized, its only eligible objective is to obtain the minimum explicit package/workspace locator metadata needed to identify at most one package-root locator candidate relevant to the accepted H-01 Flutter/client boundary.

The future read may not be used for:

- dependency analysis;
- implementation analysis;
- architecture discovery;
- feature discovery;
- source import discovery;
- runtime-behavior inference;
- security review;
- legal review;
- telemetry review;
- unrelated package discovery;
- backend discovery;
- database discovery.

A future ML-2 output may be at most one:

`PACKAGE-ROOT LOCATOR CANDIDATE`

It is not yet a source identifier.

Preserve:

`PACKAGE-ROOT LOCATOR CANDIDATE != SOURCE IDENTIFIER`

`PACKAGE-ROOT LOCATOR CANDIDATE != CANDIDATE SOURCE ACCEPTED`

`PACKAGE-ROOT LOCATOR CANDIDATE != TP-SOURCE-CLASS-01 MEMBERSHIP ESTABLISHED`

`PACKAGE-ROOT LOCATOR CANDIDATE != TP-TARGET-01 RELEVANCE ESTABLISHED`

`PACKAGE-ROOT LOCATOR CANDIDATE != SOURCE-TARGET BINDING`

## 7. Future package-root to source-hypothesis separation

Even after a package-root locator candidate is separately established and accepted for reuse, a later exact source hypothesis requires its own separate BIB-03 task sheet and review.

The manifest-guided method MUST NOT automatically append conventional source suffixes such as source directories, entrypoints, or filenames.

Preserve:

`PACKAGE ROOT IDENTIFIED != SOURCE PATH INFERRED`

`PACKAGE ROOT IDENTIFIED != AUTHORITY TO APPEND CONVENTIONAL SOURCE PATH`

`PACKAGE ROOT IDENTIFIED != SOURCE IDENTIFIED`

`SOURCE IDENTIFIED != CANDIDATE ACCEPTED`

`CANDIDATE ACCEPTED != SOURCE-TARGET BINDING`

`SOURCE-TARGET BINDING != INSPECTION AUTHORITY`

`SOURCE-TARGET BINDING != READ BUDGET`

## 8. Finite/fail-closed requirements

The proposed method must remain finite and fail closed.

A future Stage ML-1 task sheet must predeclare its exact finite manifest hypothesis set before execution.

A future Stage ML-1 task sheet may authorize no more than:

`3 EXACT MANIFEST IDENTITY PROBES`

This is a maximum ceiling, not a requirement and not a retry entitlement.

Stage ML-1 rules:

- zero hits => fail closed;
- multiple hits => fail closed;
- exactly one hit => at most one manifest identity may be carried forward for a later Stage ML-2 authority request;
- no fallback search;
- no second hypothesis set in the same invocation;
- no directory inspection;
- no repo-wide search.

Stage ML-2 rules, if separately authorized later:

- exactly one accepted manifest identity input;
- exactly one bounded read budget specified in advance;
- only explicitly allowed locator semantics;
- zero usable package-root locator candidates => fail closed;
- multiple usable package-root locator candidates => fail closed;
- no ranking or broadening.

Preserve:

`ZERO MANIFEST HITS != NO RELEVANT SOURCE EXISTS`

`MULTIPLE MANIFEST HITS != AUTHORITY TO RANK OR SEARCH`

`ZERO PACKAGE-ROOT LOCATORS != NO RELEVANT SOURCE EXISTS`

`MULTIPLE PACKAGE-ROOT LOCATORS != AUTHORITY TO CHOOSE OR EXPAND`

## 9. Protected-path rule

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

Do not use or authorize:

- `rg --files`;
- `find`;
- `fd`;
- `git ls-files`;
- recursive `ls`, `dir`, or `tree`;
- repository/directory path globs;
- GitHub code search;
- broad filename search;
- `git grep`;
- repository source discovery;
- directory source discovery.

FD02 remains protected and excluded.

Do not search for, reveal, open, read, hash, re-hash, inspect, cite, select, allowlist, modify, move, delete, stage, commit, or use FD02.

Do not independently re-prove its presence or uniqueness.

Historical incident remains historical only:

`PROTECTED-PATH ENUMERATION EXPOSURE — NO CONTENT ACCESS — STAGE A VOID`

## 10. Exact eight decisions

Create exactly `BIB03-MANIFEST-GOV-01..08`.

No ninth decision.

### BIB03-MANIFEST-GOV-01

Use exactly one:

`BIB03-METHOD-02 ELIGIBLE AS THE SOLE CURRENT MANIFEST-GUIDED BOUNDED LOCATOR METHOD MODEL`

or:

`NO MANIFEST-GUIDED BIB-03 METHOD MODEL ESTABLISHED — FAIL CLOSED`

### BIB03-MANIFEST-GOV-02

Use exactly one:

`MANIFEST IDENTITY PROBE AND MANIFEST CONTENT READ MUST REMAIN SEPARATE STAGES`

or:

`STAGE SEPARATION NOT ESTABLISHED — FAIL CLOSED`

### BIB03-MANIFEST-GOV-03

Use exactly one:

`MANIFEST-ORIGIN-01 / 02 / 03 ARE THE ONLY CURRENT ELIGIBLE FUTURE MANIFEST-HYPOTHESIS ORIGIN CLASSES`

or:

`MANIFEST-HYPOTHESIS ORIGIN CLASSES NOT ESTABLISHED — FAIL CLOSED`

### BIB03-MANIFEST-GOV-04

Use exactly one:

`FUTURE STAGE ML-1 MAY USE AT MOST THREE PREDECLARED EXACT MANIFEST IDENTITY PROBES — ZERO/MULTIPLE HITS FAIL CLOSED`

or:

`FINITE MANIFEST PROBE CEILING NOT ESTABLISHED — FAIL CLOSED`

### BIB03-MANIFEST-GOV-05

Use exactly one:

`STAGE ML-1 SUCCESS MAY CARRY FORWARD AT MOST ONE MANIFEST IDENTITY — NO MANIFEST CONTENT READ AUTHORITY`

or:

`MANIFEST IDENTITY OUTPUT BOUNDARY NOT ESTABLISHED — FAIL CLOSED`

### BIB03-MANIFEST-GOV-06

Use exactly one:

`ANY FUTURE STAGE ML-2 MANIFEST CONTENT READ REQUIRES A SEPARATE TASK SHEET, EXPLICIT TECHNICAL/CONFIG READ BUDGET, AND INDEPENDENT GPT L3 REVIEW`

or:

`MANIFEST READ-AUTHORITY BOUNDARY NOT ESTABLISHED — FAIL CLOSED`

### BIB03-MANIFEST-GOV-07

Use exactly one:

`FUTURE STAGE ML-2 MAY PRODUCE AT MOST ONE PACKAGE-ROOT LOCATOR CANDIDATE — NOT A SOURCE IDENTIFIER OR SOURCE-TARGET BINDING`

or:

`PACKAGE-ROOT LOCATOR OUTPUT BOUNDARY NOT ESTABLISHED — FAIL CLOSED`

### BIB03-MANIFEST-GOV-08

Use exactly one:

`AFTER ACCEPTANCE AND PROMOTION, A SEPARATE STAGE ML-1 MANIFEST-IDENTITY EXECUTION TASK SHEET MAY BE PROPOSED — NO MANIFEST HYPOTHESIS, PROBE, OR READ AUTHORITY CREATED BY THIS REVIEW`

or:

`NO LAWFUL MANIFEST-GUIDED SUCCESSOR PATH ESTABLISHED`

Do not draft the Stage ML-1 execution task sheet in this review.

## 11. Frozen project state

Preserve:

`TP-SOURCE-CLASS-01`

as the sole abstract source-class envelope.

Preserve:

`TP-TARGET-01`

as the sole evidence-subject target.

Preserve:

`CURRENT ACCEPTED BOUNDED CONCRETE IDENTIFICATION BASIS INSTANCES = 0`

Preserve:

`U-12 accepted concrete targets = 0`

All three PUI entries remain deferred.

Preserve:

`PUI-PREREQ-12 threshold satisfaction = 0`

U-10 remains `UNRESOLVED`.

U-14 exclusion remains unchanged.

U-15 fact values remain unresolved.

Preserve exactly:

`D02-DURABLE-UNKNOWN-01`

D-02 remains `UNRESOLVED`.

Backend remains:

`0/10 NOT INSPECTED / NOT AUTHORIZED`

Database remains:

`0/8 NOT INSPECTED / NOT AUTHORIZED`

Cumulative accepted technical/source-content reads remain exactly:

`10`

## 12. Required zero counters

Require and report:

- manifest-guided method executions = `0`;
- Stage ML-1 executions = `0`;
- Stage ML-2 executions = `0`;
- manifest-path hypotheses instantiated = `0`;
- manifest identity probes executed = `0`;
- manifest content reads = `0`;
- runtime/test/build/config/manifest reads = `0`;
- package-root locator candidates emitted = `0`;
- package-root locator candidates accepted = `0`;
- source-path hypotheses created = `0`;
- candidate source identifiers emitted = `0`;
- candidate source identifiers accepted = `0`;
- bounded basis instances accepted = `0`;
- concrete source-target bindings = `0`;
- repository-source metadata passes = `0`;
- repository-wide source discovery = `0`;
- directory-wide source discovery = `0`;
- technical-source reads = `0`;
- source-content reads = `0`;
- Flutter/client source reads = `0`;
- technical/platform inspection actions = `0`;
- future technical/source read budgets selected = `0`;
- evidence added/collected = `0`;
- U-15 fact values resolved = `0`;
- D-02 reopen actions = `0`;
- PUI-ENTRY verdict changes = `0`;
- PUI-ENTRY reconsideration actions = `0`;
- PUI-PREREQ-12 threshold satisfactions claimed = `0`;
- U-12 concrete targets added = `0`;
- technical-design decisions = `0`;
- implementation actions = `0`;
- telemetry/analytics/measurement operations = `0`;
- Safety Operations = `0`;
- legal-research operations = `0`;
- LC-03 / LC-04 / Phase 36 actions = `0 / 0 / 0`;
- DeepSeek calls = `0`;
- cumulative accepted technical/source-content reads = exactly `10`;
- current-review repository/directory path-enumeration exposure = `0`;
- current-review FD02 search/access = `0 / 0`.

## 13. Review artifact and branch

Create only:

`docs/architecture/ELITESYNC_V10_H01_BIB03_MANIFEST_GUIDED_BOUNDED_LOCATOR_METHOD_REVIEW_V0_1.md`

on exactly:

`review/h01-bib03-manifest-guided-bounded-locator-method-review-v0.1`

If the exact branch does not exist, create it exactly from the task-sheet publication commit.

Commit exactly one changed path with subject:

`docs: review H-01 BIB-03 manifest-guided bounded locator method`

Push only the review branch, exactly once, non-force.

Do not promote `main`.

## 14. Terminal outcomes

Exactly one terminal outcome.

### Outcome A

`H-01 BIB-03 MANIFEST-GUIDED BOUNDED LOCATOR METHOD ESTABLISHED — STAGED MANIFEST IDENTITY THEN SEPARATELY AUTHORIZED MINIMAL MANIFEST READ — NO MANIFEST HYPOTHESIS, PROBE, READ, PACKAGE-ROOT LOCATOR, SOURCE IDENTIFIER, BINDING, INSPECTION, EVIDENCE, PUI, DESIGN, OR IMPLEMENTATION AUTHORITY`

Use only if all eight decisions use their first values and every required zero counter passes.

### Outcome B

`H-01 BIB-03 MANIFEST-GUIDED BOUNDED LOCATOR METHOD NOT ESTABLISHED — FAIL CLOSED — NO MANIFEST-GUIDED EXECUTION SUCCESSOR CREATED`

Use if any required boundary fails.

No third outcome.

End exactly:

`STOP AFTER MANIFEST-GUIDED BOUNDED LOCATOR METHOD REVIEW — BEFORE MANIFEST HYPOTHESIS INSTANTIATION, MANIFEST IDENTITY PROBE, MANIFEST READ, PACKAGE-ROOT LOCATOR IDENTIFICATION, SOURCE HYPOTHESIS, CANDIDATE ACCEPTANCE, BASIS-INSTANCE ACCEPTANCE, SOURCE-TARGET BINDING, INSPECTION, READ-BUDGET AUTHORIZATION, EVIDENCE COLLECTION, PUI RECONSIDERATION, DESIGN, OR IMPLEMENTATION`

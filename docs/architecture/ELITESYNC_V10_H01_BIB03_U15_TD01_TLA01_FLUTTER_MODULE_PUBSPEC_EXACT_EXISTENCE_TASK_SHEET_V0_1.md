# EliteSync v10 H-01 BIB-03 U-15 TD-01 TLA-01 Flutter Module Pubspec Exact-Existence Task Sheet v0.1

Status: `PROPOSED — TD-01 TLA-01 EXACT LOCATOR EXISTENCE CHECK ONLY — ONE EXACT NON-CONTENT CHECK — ZERO TYPE CHECK — ZERO CONTENT READ — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-tla01-flutter-module-pubspec-exact-existence-v0-1`

Exact durable live-main authority required at task start:

`0521b339d7294b0896683efc0d8c954bad78b228`

This task authorizes one later fresh GPT L3/Codex candidate action for exactly one purpose: perform exactly one non-content existence check against one already durably accepted Owner-supplied EliteSync-v10 locator at one predeclared V10 revision, then record a revision-scoped HIT or NON-HIT without reading content, determining file type, searching for alternatives, or making any architecture or inheritance decision.

## 1. Memory isolation and permanent boundaries

Global/cross-project memory is intentionally NOT an authority source for EliteSync-v10.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no V10 implementation-content read, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

## 2. Durable accepted locator input

Durable TLA-01 intake result:

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_OWNER_EXACT_LOCATOR_INTAKE_FLUTTER_MODULE_PUBSPEC_REVIEW_V0_1.md`

Required blob:

`8eccf1cd815ef60b31aaf4125e71f4907446df6a`

The durably accepted Owner-supplied candidate locator is exactly:

`apps/flutter_elitesync_module/pubspec.yaml`

TD class:

`TD-01 — V10 TOOLCHAIN / BUILD / DEPENDENCY FACTS`

Named future non-design question:

`WHAT TOOLCHAIN / BUILD / DEPENDENCY FACTS ARE EXPLICITLY PRESENT IN THE CONTENT AT THIS EXACT OWNER-SUPPLIED V10 LOCATOR?`

Preserve:

`OWNER-SUPPLIED V10 LOCATOR != VERIFIED EXISTING V10 PATH`

`OWNER-SUPPLIED V10 LOCATOR != TECHNICAL FACT`

`OWNER-SUPPLIED V10 LOCATOR != ARCHITECTURE CHOICE`

`OLD ELITESYNC REPOSITORY LOCATOR != ELITESYNC-V10 REPOSITORY LOCATOR`

## 3. Exact check revision

The existence check is authorized only against this exact predeclared EliteSync-v10 revision:

`0521b339d7294b0896683efc0d8c954bad78b228`

Preserve:

`EXACT REVISION+LOCATOR HIT != LATESTNESS DETERMINATION`

`EXACT REVISION+LOCATOR HIT != CORRECT REVISION DETERMINATION`

`REVISION-SCOPED NON-HIT != GLOBAL NON-EXISTENCE`

`NO ENTRY RETURNED != PERMISSION TO SEARCH`

No alternate revision may be tested in this action.

## 4. Exact authorized substantive-content read budget

Exactly TWO substantive repository-content reads are authorized:

1. this exact task sheet at this exact task-sheet commit;
2. the durable TLA-01 intake result above at exact durable `main`, required blob `8eccf1cd815ef60b31aaf4125e71f4907446df6a`.

No third substantive repository-content read is authorized.

The one exact non-content existence check described below is not a content read.

Minimum ref/commit metadata required to verify exact authority/topology is allowed only if it does not become tree/path discovery or content inspection.

## 5. Sole authorized repository check

Exactly ONE repository object/path existence check is authorized, and only in the EliteSync-v10 repository:

`git cat-file -e '0521b339d7294b0896683efc0d8c954bad78b228:apps/flutter_elitesync_module/pubspec.yaml'`

This exact command semantics may be implemented equivalently only if the substitute performs one exact non-content existence test against the same revision+locator and returns no object content, no directory/tree listing, and no type information.

Preferred execution is the exact `git cat-file -e` form above.

Interpretation:

- exit success / entry resolves -> `HIT`;
- non-success because the exact revision+locator does not resolve -> `REVISION-SCOPED NON-HIT`.

Do not use diagnostic follow-up to discover why a non-hit occurred.

No second existence check is authorized.

## 6. Explicitly unauthorized check variants

Perform ZERO:

- `git cat-file -t`;
- `git cat-file -p`;
- object content retrieval;
- type inspection;
- tree inspection;
- `git ls-tree`;
- `git ls-files`;
- `git grep`;
- `rg --files`;
- `find`;
- `fd`;
- recursive `ls` / `dir`;
- GitHub code search;
- filename search;
- directory listing used for discovery;
- wildcard/glob testing;
- prefix testing;
- basename testing;
- alternate-locator testing;
- alternate-revision testing;
- guessed path testing.

The prior non-content `git cat-file -t` deviation remains historical only and MUST NOT be repeated here.

## 7. Sole execution question

Determine ONLY:

`DOES THE EXACT ENTRY apps/flutter_elitesync_module/pubspec.yaml RESOLVE AT THE PREDECLARED ELITESYNC-V10 REVISION 0521b339d7294b0896683efc0d8c954bad78b228 UNDER ONE AUTHORIZED NON-CONTENT EXISTENCE CHECK?`

No content may be read even on HIT.

No type may be determined even on HIT.

No alternate path may be sought on NON-HIT.

## 8. Result A — HIT classification

If the exact check resolves successfully, record exactly:

`U-15 TD-01 TLA-01 EXACT LOCATOR EXISTENCE CHECK = ONE AUTHORIZED NON-CONTENT EXISTENCE CHECK COMPLETED AGAINST apps/flutter_elitesync_module/pubspec.yaml AT PREDECLARED ELITESYNC-V10 REVISION 0521b339d7294b0896683efc0d8c954bad78b228 — RESULT = HIT / ENTRY RESOLVES AT THIS EXACT REVISION+LOCATOR — HIT ESTABLISHES ONLY REVISION-SCOPED PATH EXISTENCE — HIT != FILE TYPE — HIT != CONTENT FACT — HIT != TOOLCHAIN / BUILD / DEPENDENCY FACT — HIT != ARCHITECTURE CHOICE — HIT != INHERITANCE DECISION — HIT != LATESTNESS / CORRECTNESS DETERMINATION — ZERO TYPE CHECK — ZERO CONTENT READ — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

A HIT may support authoring one later separately authorized exact content-read task against this same exact revision+locator.

It does not itself authorize that content read.

## 9. Result B — revision-scoped NON-HIT classification

If the exact check does not resolve the exact revision+locator, record exactly:

`U-15 TD-01 TLA-01 EXACT LOCATOR EXISTENCE CHECK = ONE AUTHORIZED NON-CONTENT EXISTENCE CHECK COMPLETED AGAINST apps/flutter_elitesync_module/pubspec.yaml AT PREDECLARED ELITESYNC-V10 REVISION 0521b339d7294b0896683efc0d8c954bad78b228 — RESULT = REVISION-SCOPED NON-HIT / NO ENTRY RESOLVED AT THIS EXACT REVISION+LOCATOR — REVISION-SCOPED NON-HIT != GLOBAL NON-EXISTENCE — NON-HIT != PERMISSION TO SEARCH — NON-HIT != PERMISSION TO TEST ALTERNATE LOCATORS OR REVISIONS — NO TOOLCHAIN / BUILD / DEPENDENCY FACT ESTABLISHED — ZERO TYPE CHECK — ZERO CONTENT READ — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

A NON-HIT does not authorize another locator, another revision, search, enumeration, or path discovery.

## 10. Operational blockage

If exact main/ref/blob/topology validation fails before the check, use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 TLA-01 EXACT LOCATOR EXISTENCE DETERMINATION MADE`

If the review branch advances beyond this exact task-sheet commit before candidate execution/publication, fail closed rather than rewriting or substituting history.

If the existence check is accidentally performed more than once, do not conceal it; stop and report the deviation for fresh GPT L3 disposition.

## 11. Mandatory zero activity beyond the sole check

The candidate action MUST perform ZERO:

- content read of `apps/flutter_elitesync_module/pubspec.yaml`;
- type check of that locator;
- any other locator check;
- any other revision check;
- repository-wide or directory-wide enumeration;
- repository-tree inspection;
- search or path discovery;
- source repository access;
- old-repository access;
- source-content reads;
- EliteSync-v10 implementation/code/config/build/workflow content reads;
- Backend/API/Database/Flutter/infrastructure/deployment design;
- implementation planning;
- implementation;
- participant/data activity;
- README use;
- FD02 use;
- LC-03;
- LC-04;
- Phase 36.

## 12. Required candidate publication

Create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_FLUTTER_MODULE_PUBSPEC_EXACT_EXISTENCE_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_FLUTTER_MODULE_PUBSPEC_EXACT_EXISTENCE_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: check H-01 BIB-03 U-15 TD-01 TLA-01 exact locator existence`

Publish non-force only to:

`review/h01-bib03-u15-td01-tla01-flutter-module-pubspec-exact-existence-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent ACCEPT/REJECT.

## 13. Strict phase separation

Task-sheet authoring -> STOP.

Candidate exact-existence execution/result -> STOP.

Acceptance/promotion -> STOP.

If HIT: exact content read -> separately authored later task.

If NON-HIT: any alternate locator/revision consideration -> separately governed later action with new authority; no automatic retry.

Technical design -> separately authorized later task.

Implementation planning / implementation -> separately authorized later task.
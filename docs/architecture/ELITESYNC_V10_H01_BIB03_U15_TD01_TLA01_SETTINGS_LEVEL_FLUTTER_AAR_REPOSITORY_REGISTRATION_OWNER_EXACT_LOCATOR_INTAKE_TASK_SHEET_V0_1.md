# EliteSync v10 H-01 BIB-03 U-15 TD-01 TLA-01 Settings-Level Flutter AAR Repository Registration Owner Exact-Locator Intake Task Sheet v0.1

Status: `PROPOSED — TD-01 TLA-01 OWNER EXACT-LOCATOR INTAKE ONLY FOR DURABLY SELECTED SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION EVIDENCE-GAP CLASS — ZERO EXISTENCE/TYPE/CONTENT CHECK — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-tla01-settings-level-flutter-aar-repository-registration-owner-exact-locator-intake-v0-1`

Exact durable live-main authority required at task start:

`41b26bf4610075198a28c4d3dd4884d35a205d7a`

This task authorizes one later fresh GPT L3/Codex candidate action for exactly one purpose: intake one explicit Owner-supplied EliteSync-v10 repository-relative candidate locator for the already durably selected TD-01 evidence-gap class `SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION`, without checking whether the path exists, determining its type, reading its content, searching for alternatives, importing old-repository build-graph truth into V10, or making repository-registration, dependency-resolution, architecture, inheritance, implementation-planning, or implementation decisions.

## 1. Memory isolation and permanent boundaries

Global/cross-project memory is intentionally NOT an authority source for EliteSync-v10.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no new V10 technical-content read, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

Historical incidents, rejected candidates, stale tasks, and deviations remain historical truth and must not be rewritten as if they did not occur.

Preserve the durable stale-task distinction:

`PREMATURELY AUTHORED STALE TASK != CURRENT GOVERNANCE AUTHORITY`

The prior prematurely authored single-file sufficiency task `ad84587a8bd0d7fb5e373417aa94b33be936b58c` remains stale and is not current authority.

## 2. Durable restart-gate basis

Durable host/native single-file sufficiency / next-gap RETRY disposition:

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_HOST_NATIVE_SINGLE_FILE_SUFFICIENCY_AND_NEXT_GAP_DISPOSITION_GOVERNANCE_RETRY_REVIEW_V0_1.md`

Required blob:

`d94e379651f9b009b38ccdcfc99c94a5fdf97a96`

Preserve from that durable result:

- TD-01 remains `PARTIALLY DISCOVERED`;
- host/native build integration materially advanced at revision-scoped single-file declaration level;
- app-level declarations establish a Flutter module reference, Flutter AAR generation task, `preBuild -> syncFlutterAar` task edge, AAR output location, and debug/profile/release Flutter AAR dependency coordinates for the exact authorized source only;
- settings-level Flutter AAR repository registration / dependency-repository source remains unestablished;
- actual AAR generation success remains unestablished;
- actual dependency resolution / artifact availability remains unestablished;
- installed toolchain state and build-success/workflow facts remain unestablished;
- current TD-01 facts remain insufficient for architecture-entry determination;
- next priority bounded TD-01 evidence-gap class = `SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION`;
- `EVIDENCE-GAP PRIORITY != LOCATOR`;
- `EVIDENCE-GAP PRIORITY != PERMISSION TO SEARCH`;
- `SETTINGS-LEVEL EVIDENCE CLASS != SETTINGS FILE LOCATOR`.

No settings-level repository path was declared, acquired, inferred, tested, searched for, or authorized by that disposition.

## 3. Frozen Owner input

The Owner has now explicitly supplied the following input.

TD class:

`TD-01 — V10 TOOLCHAIN / BUILD / DEPENDENCY FACTS`

Selected evidence-gap class:

`SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION`

Owner-supplied exact V10 repository-relative candidate locator:

`apps/android/settings.gradle.kts`

Named future non-design question:

`WHAT SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION / DEPENDENCY-REPOSITORY SOURCE FACTS — INCLUDING ANY EXPLICIT LOCAL FLUTTER AAR MAVEN REPOSITORY, FLUTTER ARTIFACT REPOSITORY, CONTENT FILTER, OR OTHER DIRECTLY DECLARED SETTINGS-LEVEL REPOSITORY CONFIGURATION RELEVANT TO RESOLVING THE APP-LEVEL FLUTTER AAR DEPENDENCY COORDINATES — ARE PRESENT IN THE CONTENT AT THIS EXACT OWNER-SUPPLIED V10 LOCATOR?`

The Owner supplied this exact string as an EliteSync-v10 TLA-01 candidate locator for the durably selected `SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION` evidence-gap class.

Preserve exactly:

`EVIDENCE-GAP PRIORITY != LOCATOR`

`SETTINGS-LEVEL EVIDENCE CLASS != SETTINGS FILE LOCATOR`

`OLD ELITESYNC REPOSITORY LOCATOR != ELITESYNC-V10 REPOSITORY LOCATOR`

`OWNER-SUPPLIED V10 LOCATOR != VERIFIED EXISTING V10 PATH`

`OWNER-SUPPLIED V10 LOCATOR != TECHNICAL FACT`

`OWNER-SUPPLIED V10 LOCATOR != ARCHITECTURE CHOICE`

`SETTINGS-LEVEL REPOSITORY DECLARATION != DEPENDENCY RESOLUTION SUCCESS`

`SETTINGS-LEVEL CONTENT FACT != COMPLETE HOST BUILD GRAPH`

No claim is made yet that `apps/android/settings.gradle.kts` exists in EliteSync-v10.

No claim is made yet that EliteSync-v10 registers a local Flutter AAR Maven repository, configures a Flutter artifact repository, uses any content filter for Flutter artifacts, successfully resolves any AAR dependency, or possesses a complete host/native build graph.

This Owner input satisfies the restart condition for one locator-intake action only.

It does NOT itself authorize existence checking, type checking, content reading, locator search, repository-registration inference, dependency-resolution inference, build-graph inference, architecture selection, inheritance, implementation planning, or implementation.

## 4. Exact authorized substantive-content read budget

Exactly TWO substantive repository-content reads are authorized for the later candidate action:

1. this exact task sheet at this exact task-sheet commit;
2. the durable host/native single-file sufficiency / next-gap RETRY disposition above at exact durable `main`, required blob `d94e379651f9b009b38ccdcfc99c94a5fdf97a96`.

No third substantive repository-content read is authorized.

Minimum exact ref/commit metadata required to verify authority/topology is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

Do not read `apps/android/settings.gradle.kts`, `apps/android/app/build.gradle.kts`, `.metadata`, `pubspec.yaml`, `pubspec.lock`, any other Gradle/settings/native-build file, AndroidManifest, Kotlin/Java source, CI/workflow, source/config/build files, README, FD02, source repository, or old repository.

## 5. Sole intake question

Determine ONLY:

`MAY THE EXACT OWNER-SUPPLIED STRING apps/android/settings.gradle.kts BE RECORDED AS THE SOLE TLA-01 CANDIDATE LOCATOR FOR THE DURABLY SELECTED TD-01 EVIDENCE-GAP CLASS SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION AND THE NAMED FUTURE NON-DESIGN QUESTION, WHILE PRESERVING THAT INTAKE DOES NOT VERIFY EXISTENCE, TYPE, CONTENT, REPOSITORY REGISTRATION, DEPENDENCY RESOLUTION, COMPLETE HOST BUILD GRAPH, TECHNICAL FACT, OR ARCHITECTURE SUITABILITY?`

This is locator intake only.

No repository operation may test the locator.

No technical evidence may be read.

## 6. Literal locator-form check permitted from frozen Owner input only

The candidate action may determine from the literal Owner-supplied string itself that it is syntactically a single repository-relative file-like locator because it is:

- one string;
- non-empty;
- not absolute;
- not directory-only;
- not basename-only;
- not a wildcard or glob;
- not an ellipsis or placeholder;
- not a list of alternatives.

This literal-form determination MUST NOT use repository operations.

Preserve:

`VALID LOCATOR FORM != VERIFIED PATH EXISTENCE`

`SETTINGS.GRADLE.KTS NAME != PROOF OF SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION CONTENT`

## 7. Outcome A — Owner exact locator intake established

If current authority supports intake, the candidate MUST use exactly this classification:

`U-15 TD-01 TLA-01 SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION OWNER EXACT-LOCATOR INTAKE = OWNER EXPLICITLY SUPPLIED apps/android/settings.gradle.kts AS THE SOLE ELITESYNC-V10 REPOSITORY-RELATIVE CANDIDATE LOCATOR FOR THE DURABLY SELECTED TD-01 EVIDENCE-GAP CLASS SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION AND THE NAMED FUTURE NON-DESIGN QUESTION — LOCATOR FORM ACCEPTED FOR INTAKE ONLY — OWNER RESTART CONDITION SATISFIED FOR ONE LATER SEPARATELY AUTHORED EXACT LOCATOR-EXISTENCE TASK — EVIDENCE-GAP PRIORITY != LOCATOR — SETTINGS-LEVEL EVIDENCE CLASS != SETTINGS FILE LOCATOR — OWNER-SUPPLIED V10 LOCATOR != VERIFIED EXISTING V10 PATH — OWNER-SUPPLIED V10 LOCATOR != TECHNICAL FACT — SETTINGS-LEVEL REPOSITORY DECLARATION != DEPENDENCY RESOLUTION SUCCESS — SETTINGS-LEVEL CONTENT FACT != COMPLETE HOST BUILD GRAPH — OWNER-SUPPLIED V10 LOCATOR != ARCHITECTURE CHOICE — OLD ELITESYNC REPOSITORY LOCATOR != ELITESYNC-V10 REPOSITORY LOCATOR — ZERO EXISTENCE CHECK — ZERO TYPE CHECK — ZERO CONTENT READ — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Outcome A means only:

- the exact string is durably recorded as the sole Owner-supplied TLA-01 candidate locator for this selected TD-01 evidence-gap class and named non-design question;
- a later separately authored exact task may perform one bounded non-content existence check against this exact V10 locator;
- no existence, type, content, repository-registration, Maven-repository, Flutter-artifact-repository, content-filter, dependency-resolution, artifact-availability, complete-build-graph, architecture, or inheritance fact is established now;
- no second settings-level or build-graph locator is authorized now.

Candidate accounting:

- candidate settings-level repository-registration TLA-01 intake reviews = `1`;
- accepted/durable intake reviews = `0`;
- exact locators tested = `0`;
- existence checks = `0`;
- type checks = `0`;
- technical-evidence content reads = `0`;
- repository search/enumeration/path discovery = `0/0/0`;
- technical-design actions = `0`;
- implementation-planning actions = `0`;
- implementation actions = `0`.

## 8. Outcome B / operational blockage

If the exact Owner input cannot truthfully be recorded under current authority, classify exactly:

`EXPECTED OUTCOME B — U-15 TD-01 TLA-01 SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION OWNER EXACT-LOCATOR INTAKE NOT ESTABLISHED UNDER CURRENT AUTHORITY — FAIL CLOSED — ZERO EXISTENCE/TYPE/CONTENT CHECK — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

For exact main/ref/blob/topology failure use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 TLA-01 SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION OWNER EXACT-LOCATOR INTAKE DETERMINATION MADE`

If `main` is not exactly `41b26bf4610075198a28c4d3dd4884d35a205d7a` at task start, STOP before substantive content read except minimum ref metadata needed to detect mismatch.

If the durable retry-review blob mismatches, STOP.

If the review branch advances beyond this exact task-sheet commit before candidate publication, fail closed rather than rewriting or substituting history.

## 9. Mandatory zero activity

The candidate intake action MUST perform ZERO:

- existence checking of `apps/android/settings.gradle.kts`;
- type checking of that locator;
- content reading of that locator;
- reread of `apps/android/app/build.gradle.kts`;
- any other Gradle/settings/native-build content read;
- AndroidManifest/Kotlin/Java source read;
- `.metadata` reread;
- `pubspec.yaml` reread;
- `pubspec.lock` read;
- CI/workflow read;
- repository-wide or directory-wide enumeration;
- repository-tree inspection;
- directory listing used for discovery;
- filename search;
- code search;
- GitHub code search;
- path discovery;
- guessed conventional Gradle/settings filename/path testing;
- alternate-locator testing;
- manifest/config/build/workflow/source locator testing beyond the frozen literal string;
- `git cat-file` object/path testing;
- locator acquisition beyond this frozen Owner input;
- TLA-02 selection/import;
- source repository access;
- old-repository access;
- source-content reads;
- Backend/API/Database/Flutter/infrastructure/deployment design;
- architecture selection;
- implementation planning;
- implementation;
- participant/data activity;
- README use;
- FD02 use;
- LC-03;
- LC-04;
- Phase 36.

## 10. Required candidate publication

For Outcome A or Outcome B create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_SETTINGS_LEVEL_FLUTTER_AAR_REPOSITORY_REGISTRATION_OWNER_EXACT_LOCATOR_INTAKE_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_SETTINGS_LEVEL_FLUTTER_AAR_REPOSITORY_REGISTRATION_OWNER_EXACT_LOCATOR_INTAKE_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: record H-01 BIB-03 U-15 TD-01 settings repository locator intake`

Publish non-force only to:

`review/h01-bib03-u15-td01-tla01-settings-level-flutter-aar-repository-registration-owner-exact-locator-intake-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent ACCEPT/REJECT.

## 11. Strict phase separation

Task-sheet authoring -> STOP.

Candidate locator intake -> STOP.

Acceptance/promotion -> STOP.

Exact locator existence check -> separately authored later task.

Any type check -> separately authorized only if specifically needed.

Any content read -> separately authored later task after a truthful locator-existence result.

Any second settings-level/build-graph locator -> separately governed later action after current single-file settings fact sufficiency is known.

Any fact-sufficiency determination -> separately authorized later governance task after durable settings-level facts.

Any architecture-entry determination -> separately authorized later governance task after sufficient bounded technical facts.

Technical design -> separately authorized later task.

Implementation planning / implementation -> separately authorized later task.
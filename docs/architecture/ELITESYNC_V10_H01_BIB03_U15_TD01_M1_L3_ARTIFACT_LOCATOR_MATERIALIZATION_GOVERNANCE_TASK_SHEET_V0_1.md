# EliteSync v10 H-01 BIB-03 U-15 TD-01 M1 L3 Artifact Locator Materialization Governance Task Sheet v0.1

Status: `PROPOSED — TD-01 M1 L3 ARTIFACT LOCATOR MATERIALIZATION GOVERNANCE ONLY — ONE EXACT LOCATOR STRING MAY BE DETERMINISTICALLY MATERIALIZED FROM ACCEPTED DURABLE V10 FACTS + ACCEPTED L3 RULE BASIS — ZERO ARTIFACT EXISTENCE/TYPE/CONTENT/HASH CHECK — ZERO LOCAL FILESYSTEM / GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO GRADLE/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-m1-l3-artifact-locator-materialization-governance-v0-1`

Exact durable live-main authority required at task start:

`d1b45cc5da30bba3f377ef4ead2c677d0fb6eb88`

This task authorizes one later fresh GPT L3/Codex governance candidate action for exactly one purpose: apply the already accepted L3 external-standard derivation basis to the already durable EliteSync-v10 repository-relative Flutter-module Maven repository root / release output directory / release Maven coordinate facts, and materialize exactly one repository-relative local generated-artifact file locator string for the selected M1 method class, without testing, reading, hashing, listing, searching, or otherwise inspecting the target or its parent directories.

This task does NOT authorize M1 execution.

This task does NOT establish that the materialized locator exists.

This task does NOT establish that the materialized artifact is fresh, generated by the current toolchain, selected by Gradle, resolvable, buildable, or suitable for architecture-entry.

## 1. Permanent governance boundaries

Global/cross-project memory is NOT EliteSync-v10 governance authority.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no new V10 technical-target read, no local generated-output inspection, no Maven-directory inspection, no cache inspection, no Gradle/build execution, no dependency-resolution execution, no network/repository artifact availability test, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

Historical incidents, rejected candidates, stale tasks, deviations, and protected local state remain historical/durable truth and must not be rewritten as if they did not occur.

The stale task `ad84587a8bd0d7fb5e373417aa94b33be936b58c` remains stale and not current authority.

The rejected existence-result candidate `701e0ad03b5fc22694d8fcef18250580e4549e82` remains rejected and must not be promoted, reused, amended, rebased, cherry-picked, merged, or used as substantive basis.

Do NOT use `git status`, `git status --short`, untracked checks, directory listing, cache listing, or generated-output listing as part of this task.

Do NOT inspect, modify, unstage, overwrite, discard, reset, clean, commit, cite, summarize, or use unrelated protected local staged state.

## 2. Exact durable basis A — accepted L3 external-standard derivation basis

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_L3_EXTERNAL_STANDARD_DERIVATION_BASIS_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`5d7dc84b2fbbae9e623896141e39e24d6983d752`

Preserve from Basis A:

- the accepted minimal L3 basis set consists of official Apache Maven `Maven Repository Layout – Maven` plus official Flutter `Integrate a Flutter module into your Android project` documentation;
- the accepted basis covers D1 — coordinate-to-repository-path mapping;
- the accepted basis covers D2 — final artifact filename construction;
- the accepted basis covers D3 — relevant Flutter Android Archive output uses `.aar`;
- the accepted basis covers D4 — the relevant release output class is represented without a classifier component;
- the accepted basis covers D5 — compatibility with the durable release coordinate/output class;
- the L3 basis is derivation authority only and does not establish V10 artifact presence;
- no V10 artifact locator was materialized, acquired, tested, or authorized by the L3 basis-governance action.

Preserve exactly:

`EXTERNAL STANDARD DERIVATION BASIS != ARTIFACT LOCATOR`

`OFFICIAL DOCUMENTATION != V10 ARTIFACT PRESENCE`

`STANDARD-SUPPORTED FILENAME RULE != ARTIFACT AVAILABILITY`

`STANDARD AUTHORITY != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`STANDARD AUTHORITY != FRESH GENERATION`

`STANDARD AUTHORITY != BUILD SUCCESS`

`L3 BASIS ACCEPTANCE != M1 EXECUTION AUTHORITY`

## 3. Exact durable basis B — app-level AAR declaration facts

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_HOST_NATIVE_BUILD_INTEGRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Required blob:

`b201348a6a62f7bd2c7d09d1f20b20554cdabef3`

Preserve only these exact revision-scoped declaration facts:

- Android root project context represented by the accepted host/native fact register uses the Flutter module directory reference `rootProject.file("../flutter_elitesync_module")`;
- declared release AAR output location = `File(flutterModuleDir, "build/host/outputs/repo/com/elitesync/flutter_elitesync_module/flutter_release/1.0")`;
- release Maven coordinate = `com.elitesync.flutter_elitesync_module:flutter_release:1.0`;
- debug also consumes `com.elitesync.flutter_elitesync_module:flutter_release:1.0`;
- profile consumes `com.elitesync.flutter_elitesync_module:flutter_profile:1.0`;
- these declarations do not establish AAR generation success, artifact availability, dependency-resolution success, or build success.

Preserve exactly:

`DECLARED AAR GENERATION TASK != AAR BUILD SUCCESS`

`DECLARED AAR DEPENDENCY != RESOLVED AAR DEPENDENCY`

`DECLARED ARTIFACT OUTPUT PATH != ARTIFACT AVAILABILITY`

## 4. Exact durable basis C — settings-level repository-source facts

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_SETTINGS_LEVEL_FLUTTER_AAR_REPOSITORY_REGISTRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Required blob:

`9ad03334b39779879124c4463d70b684480fc625`

Preserve only these exact revision-scoped declaration facts:

- dependency-resolution repositories declare local Flutter-module Maven repository source `../flutter_elitesync_module/build/host/outputs/repo` from the Android settings context;
- `RepositoriesMode.PREFER_SETTINGS` is declared;
- repository declarations do not establish artifact presence or dependency-resolution success.

Preserve exactly:

`SETTINGS-LEVEL REPOSITORY DECLARATION != DEPENDENCY RESOLUTION SUCCESS`

`DECLARED REPOSITORY SOURCE != ARTIFACT AVAILABILITY`

## 5. Repository-relative root normalization permitted for this task

This task may perform only deterministic string/path normalization required to express the already-declared Flutter-module repository source as an EliteSync-v10 repository-relative locator.

The durable Android settings/app project path context already governed in V10 is:

`apps/android`

The durable relative module reference/source is:

`../flutter_elitesync_module`

Therefore the normalized repository-relative Flutter module root to use for materialization is:

`apps/flutter_elitesync_module`

and the normalized repository-relative local Maven repository root is:

`apps/flutter_elitesync_module/build/host/outputs/repo`

This normalization is a string-level combination of already durable exact locators/path declarations. It is NOT an existence check, path discovery, directory listing, search, or generated-output inspection.

Preserve exactly:

`PATH NORMALIZATION != PATH EXISTENCE`

`REPOSITORY-RELATIVE LOCATOR MATERIALIZATION != LOCAL FILESYSTEM PRESENCE`

## 6. Exact substantive V10 repository-content read budget

Exactly FOUR substantive EliteSync-v10 repository-content reads are authorized for the later candidate action:

1. this exact task sheet;
2. Basis A at required blob `5d7dc84b2fbbae9e623896141e39e24d6983d752`;
3. Basis B at required blob `b201348a6a62f7bd2c7d09d1f20b20554cdabef3`;
4. Basis C at required blob `9ad03334b39779879124c4463d70b684480fc625`.

No fifth substantive EliteSync-v10 repository-content read is authorized.

Do NOT reread any V10 technical target, including:

- `apps/android/app/build.gradle.kts`;
- `apps/android/settings.gradle.kts`;
- any generated Maven repository output;
- any `.aar`, `.pom`, checksum, metadata, cache, build-output, or repository directory;
- `.metadata`;
- `pubspec.yaml`;
- `pubspec.lock`;
- AndroidManifest;
- Kotlin/Java source;
- CI/workflows;
- any other Gradle/settings/native-build file.

Do NOT re-read external web documentation in this materialization action. The accepted L3 review is the durable rule basis for this task.

Do NOT read README, FD02, source repository, old repository, or rejected candidate content.

Minimum exact ref/commit/compare metadata required to verify current authority/topology is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 7. Sole materialization question

Determine ONLY:

`WHEN THE ACCEPTED L3 MAVEN-LAYOUT / FLUTTER-AAR RULE BASIS IS APPLIED TO THE DURABLE ELITESYNC-V10 REPOSITORY-RELATIVE FLUTTER-MODULE MAVEN REPOSITORY ROOT, RELEASE DIRECTORY, AND RELEASE COORDINATE com.elitesync.flutter_elitesync_module:flutter_release:1.0, WHAT ONE EXACT REPOSITORY-RELATIVE LOCAL GENERATED-AAR FILE LOCATOR IS DETERMINISTICALLY MATERIALIZED FOR THE FUTURE M1 ARTIFACT-AVAILABILITY PROBE?`

No existence, type, content, hash, freshness, build, or resolution check is authorized.

## 8. Required deterministic derivation

The candidate must show the derivation as a governance calculation only:

- repository-relative local Maven root = `apps/flutter_elitesync_module/build/host/outputs/repo`;
- groupId = `com.elitesync.flutter_elitesync_module`;
- Maven layout group path = `com/elitesync/flutter_elitesync_module`;
- artifactId = `flutter_release`;
- version/baseVersion for this non-snapshot durable coordinate = `1.0`;
- classifier = none for the accepted relevant release output class;
- extension = `aar`;
- filename = `flutter_release-1.0.aar`;
- final repository-relative locator = repository root + Maven group path + artifactId + version + filename.

Expected exact materialized locator:

`apps/flutter_elitesync_module/build/host/outputs/repo/com/elitesync/flutter_elitesync_module/flutter_release/1.0/flutter_release-1.0.aar`

The candidate must not substitute profile/debug artifacts, checksums, POM files, metadata files, cache paths, absolute Windows paths, old-repository paths, or any alternative locator.

## 9. Required interpretation boundaries

Preserve exactly:

`MATERIALIZED ARTIFACT LOCATOR != VERIFIED ARTIFACT PRESENCE`

`MATERIALIZED ARTIFACT LOCATOR != FILE TYPE CHECK`

`MATERIALIZED ARTIFACT LOCATOR != CONTENT FACT`

`MATERIALIZED ARTIFACT LOCATOR != ARTIFACT FRESHNESS`

`MATERIALIZED ARTIFACT LOCATOR != BUILD SUCCESS`

`MATERIALIZED ARTIFACT LOCATOR != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`MATERIALIZED ARTIFACT LOCATOR != M1 EXECUTION AUTHORITY`

`MATERIALIZED ARTIFACT LOCATOR != CURRENT/LATEST GENERATED STATE`

`LOCAL GENERATED-ARTIFACT LOCATOR != GIT-TRACKED PATH CLAIM`

`NO EXISTENCE CHECK != ARTIFACT ABSENCE`

`LOCATOR MATERIALIZATION GOVERNANCE != TECHNICAL DESIGN`

The materialized locator is intended only as one exact future M1 probe target after separate execution governance.

## 10. Required Outcome A classification

If exact durable basis validation supports the deterministic derivation above, record exactly and verbatim:

`U-15 TD-01 M1 L3 ARTIFACT LOCATOR MATERIALIZATION GOVERNANCE = ACCEPTED L3 MAVEN-LAYOUT / FLUTTER-AAR DERIVATION BASIS APPLIED TO DURABLE ELITESYNC-V10 RELEASE REPOSITORY / COORDINATE FACTS DETERMINISTICALLY MATERIALIZES EXACTLY ONE REPOSITORY-RELATIVE LOCAL GENERATED-AAR LOCATOR = apps/flutter_elitesync_module/build/host/outputs/repo/com/elitesync/flutter_elitesync_module/flutter_release/1.0/flutter_release-1.0.aar — MATERIALIZATION IS STRING/RULE DERIVATION ONLY — MATERIALIZED ARTIFACT LOCATOR != VERIFIED ARTIFACT PRESENCE — MATERIALIZED ARTIFACT LOCATOR != ARTIFACT FRESHNESS — MATERIALIZED ARTIFACT LOCATOR != BUILD SUCCESS — MATERIALIZED ARTIFACT LOCATOR != GRADLE DEPENDENCY-RESOLUTION SUCCESS — LOCAL GENERATED-ARTIFACT LOCATOR != GIT-TRACKED PATH CLAIM — M1 REMAINS UNEXECUTED — NO EXISTENCE / TYPE / CONTENT / HASH CHECK AUTHORIZED OR PERFORMED — ZERO LOCAL FILESYSTEM / GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO GRADLE/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Do not shorten, paraphrase, translate, split, normalize, or semantically rewrite this classification.

Outcome A means only:

- the accepted L3 derivation basis is applied to durable V10 facts;
- exactly one repository-relative generated `.aar` locator is durably materialized;
- the locator is not tested;
- no claim of existence, freshness, generation, resolution, build success, or architecture sufficiency is made;
- M1 execution remains separately unauthorized;
- a later separately authored M1 exact-artifact-existence/probe governance task is required before any filesystem check.

## 11. Outcome B / operational blockage

If the exact durable basis cannot support the deterministic locator above without adding an unstated rule, classify exactly:

`EXPECTED OUTCOME B — U-15 TD-01 M1 L3 ARTIFACT LOCATOR MATERIALIZATION GOVERNANCE DOES NOT SUPPORT ONE UNIQUE EXACT REPOSITORY-RELATIVE GENERATED-AAR LOCATOR UNDER CURRENT ACCEPTED BASIS — FAIL CLOSED — NO LOCATOR AUTHORIZED FOR M1 EXECUTION — NO EXISTENCE / TYPE / CONTENT / HASH CHECK AUTHORIZED — ZERO LOCAL FILESYSTEM / GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO GRADLE/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

For exact main/ref/blob/topology failure before determination use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 M1 L3 ARTIFACT LOCATOR MATERIALIZATION DETERMINATION MADE`

If `main` is not exactly `d1b45cc5da30bba3f377ef4ead2c677d0fb6eb88` at task start, STOP before substantive content read except minimum ref metadata needed to detect mismatch.

If the review branch advances beyond this exact task-sheet commit before candidate publication, fail closed rather than rewriting or substituting history.

## 12. Mandatory zero activity

The candidate action MUST perform ZERO:

- artifact existence checks;
- artifact type checks;
- artifact content reads;
- artifact hash/re-hash checks;
- local filesystem target testing;
- generated-output inspection;
- Maven-directory inspection;
- Gradle-cache inspection;
- repository-tree inspection;
- directory listing used for discovery;
- wildcard/glob testing;
- filename search;
- code search;
- GitHub code search;
- repository-wide or directory-wide enumeration;
- path discovery;
- `git status` / `git status --short` / untracked checks;
- Gradle/build execution;
- dependency-resolution execution;
- `syncFlutterAar` execution;
- `flutter build aar` execution;
- network/repository reachability tests;
- artifact downloads/uploads;
- external web/documentation reread;
- source repository access;
- old-repository access;
- README use;
- FD02 use;
- protected local staged-state inspection/modification;
- architecture selection;
- technical design;
- implementation planning;
- implementation;
- participant/data activity;
- LC-03;
- LC-04;
- Phase 36.

## 13. Required candidate publication

For Outcome A or Outcome B create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_L3_ARTIFACT_LOCATOR_MATERIALIZATION_GOVERNANCE_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_L3_ARTIFACT_LOCATOR_MATERIALIZATION_GOVERNANCE_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: materialize H-01 BIB-03 U-15 TD-01 M1 L3 artifact locator`

Publish non-force only to:

`review/h01-bib03-u15-td01-m1-l3-artifact-locator-materialization-governance-v0-1`

Do NOT modify or promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent `ACCEPT` / `REJECT`.

## 14. Strict phase separation

Task-sheet authoring -> STOP.

Candidate locator materialization -> STOP.

Fresh GPT L3 ACCEPT/REJECT -> STOP.

If accepted, normal non-force promotion -> separate acceptance action.

M1 exact local artifact-availability probe method execution -> separately authored later task.

M1 result sufficiency / next-gap disposition -> separately authorized after a durable M1 result.

Targeted Gradle dependency-resolution method governance/execution remains separate and later if still required.

Architecture-entry determination -> separate later governance task.

Technical design -> separate later task.

Implementation planning / implementation -> separate later task.
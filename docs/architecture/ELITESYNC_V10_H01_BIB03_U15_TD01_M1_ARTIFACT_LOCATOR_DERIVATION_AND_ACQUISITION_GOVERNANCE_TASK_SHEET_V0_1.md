# EliteSync v10 H-01 BIB-03 U-15 TD-01 M1 Artifact Locator Derivation and Acquisition Governance Task Sheet v0.1

Status: `PROPOSED — TD-01 M1 EXACT LOCAL MAVEN ARTIFACT LOCATOR DERIVATION / ACQUISITION GOVERNANCE ONLY — ZERO ARTIFACT LOCATOR TESTING — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY INSPECTION — ZERO GRADLE/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY TEST — ZERO NEW TECHNICAL CONTENT READ — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-m1-artifact-locator-derivation-acquisition-governance-v0-1`

Exact durable live-main authority required at task start:

`f44ec7bcba5670d8c99e3b0c87e9ebb1efbb4770`

This task authorizes one later fresh GPT L3/Codex governance candidate action for exactly one purpose: determine whether one exact repository-relative local Maven artifact locator suitable for the already selected M1 method class can be lawfully materialized from current durable V10 declarations alone, or whether a new external exact-locator input / separately authorized derivation authority is required before any M1 probe can be authored.

This task does NOT authorize artifact probing, generated-output inspection, Maven-directory inspection, Gradle execution, dependency resolution, network access, technical design, or implementation.

## 1. Permanent governance boundaries

Global/cross-project memory is NOT EliteSync-v10 governance authority.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no new V10 technical-target read, no generated-output inspection, no local Maven-directory inspection, no cache inspection, no Gradle/build execution, no dependency-resolution execution, no network/repository reachability test, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

Historical incidents, rejected candidates, stale tasks, and deviations remain historical truth and must not be rewritten as if they did not occur.

The stale task `ad84587a8bd0d7fb5e373417aa94b33be936b58c` remains stale and not current authority.

The rejected existence-result candidate `701e0ad03b5fc22694d8fcef18250580e4549e82` remains rejected and must not be promoted, reused, amended, rebased, cherry-picked, merged, or used as substantive basis.

## 2. Exact durable basis A — accepted M1 evidence-method governance

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_AAR_ARTIFACT_AVAILABILITY_AND_DEPENDENCY_RESOLUTION_EVIDENCE_METHOD_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`259e61850befe4ddc0c21ecc0c3da0539699a7c0`

Preserve from Basis A:

- combined artifact-availability / dependency-resolution evidence gap is decomposed;
- first selected method class = `M1 EXACT LOCAL MAVEN ARTIFACT-AVAILABILITY PROBE`;
- M1 is non-network / non-build / non-Gradle;
- M1 requires one later separately predeclared exact artifact locator under the declared Flutter-module host-output Maven source;
- no artifact locator was derived, acquired, or authorized;
- no command was authorized;
- M1 cannot establish Gradle dependency-resolution success, artifact freshness, build success, or architecture suitability;
- M2 targeted Gradle dependency-resolution execution remains a separate later gate if still required.

Preserve exactly:

`LOCAL ARTIFACT AVAILABILITY != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`LOCAL ARTIFACT NON-HIT != GLOBAL ARTIFACT ABSENCE`

`LOCAL ARTIFACT HIT != FRESH GENERATION`

`LOCAL ARTIFACT HIT != BUILD SUCCESS`

`METHOD SELECTION != METHOD EXECUTION AUTHORITY`

`METHOD CLASS != ARTIFACT LOCATOR`

`METHOD CLASS != COMMAND AUTHORITY`

## 3. Exact durable basis B — app-level AAR generation / consumption declarations

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_HOST_NATIVE_BUILD_INTEGRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Required blob:

`b201348a6a62f7bd2c7d09d1f20b20554cdabef3`

Preserve only the following revision-scoped declaration facts:

- Flutter module directory reference = `rootProject.file("../flutter_elitesync_module")`;
- declared release AAR output location = `File(flutterModuleDir, "build/host/outputs/repo/com/elitesync/flutter_elitesync_module/flutter_release/1.0")`;
- `syncFlutterAar` is declared as an `Exec` task using `flutter build aar --no-debug --no-profile` plus recorded dart-define arguments;
- `preBuild` declares dependency on `syncFlutterAar`;
- `debugImplementation` declares `com.elitesync.flutter_elitesync_module:flutter_release:1.0`;
- `profileImplementation` declares `com.elitesync.flutter_elitesync_module:flutter_profile:1.0`;
- `releaseImplementation` declares `com.elitesync.flutter_elitesync_module:flutter_release:1.0`.

Preserve:

`DECLARED AAR GENERATION TASK != AAR BUILD SUCCESS`

`DECLARED AAR DEPENDENCY != RESOLVED AAR DEPENDENCY`

`DECLARED ARTIFACT OUTPUT PATH != ARTIFACT AVAILABILITY`

The durable basis explicitly declares a release-artifact repository directory and Maven coordinates. It does NOT explicitly name a final `.aar` file locator inside that directory.

## 4. Exact durable basis C — settings-level repository-source declarations

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_SETTINGS_LEVEL_FLUTTER_AAR_REPOSITORY_REGISTRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Required blob:

`9ad03334b39779879124c4463d70b684480fc625`

Preserve only the following revision-scoped declaration facts:

- `dependencyResolutionManagement` declares `RepositoriesMode.PREFER_SETTINGS`;
- dependency-resolution repositories declare `maven(url = uri("../flutter_elitesync_module/build/host/outputs/repo"))`;
- settings-level repository declarations do not establish artifact presence or dependency-resolution success.

Preserve:

`SETTINGS-LEVEL REPOSITORY DECLARATION != DEPENDENCY RESOLUTION SUCCESS`

`DECLARED REPOSITORY SOURCE != ARTIFACT AVAILABILITY`

## 5. Exact substantive-content read budget

Exactly FOUR substantive repository-content reads are authorized for the later candidate action:

1. this exact task sheet;
2. Basis A at required blob `259e61850befe4ddc0c21ecc0c3da0539699a7c0`;
3. Basis B at required blob `b201348a6a62f7bd2c7d09d1f20b20554cdabef3`;
4. Basis C at required blob `9ad03334b39779879124c4463d70b684480fc625`.

No fifth substantive repository-content read is authorized.

Do NOT reread any technical target, including:

- `apps/android/app/build.gradle.kts`;
- `apps/android/settings.gradle.kts`;
- any generated Maven repository output;
- any `.aar`, `.pom`, metadata, cache, build-output, or repository directory;
- `.metadata`;
- `pubspec.yaml`;
- `pubspec.lock`;
- AndroidManifest;
- Kotlin/Java source;
- CI/workflows;
- any other Gradle/settings/native-build file.

Do NOT read README, FD02, source repository, old repository, or rejected candidate content.

Minimum exact ref/commit/compare metadata required to verify current authority/topology is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 6. Sole governance question

Determine ONLY:

`DO THE CURRENT DURABLE V10 DECLARATIONS FULLY DETERMINE ONE EXACT REPOSITORY-RELATIVE LOCAL MAVEN ARTIFACT FILE LOCATOR FOR A FUTURE M1 PROBE WITHOUT INTRODUCING ANY UNGOVERNED MAVEN-LAYOUT / ARTIFACT-FILENAME CONVENTION, SEARCH, ENUMERATION, GENERATED-OUTPUT INSPECTION, OR GUESSED PATH; AND IF NOT, WHAT EXACT RESTART-GATE INPUT OR SEPARATELY AUTHORIZED DERIVATION BASIS IS REQUIRED?`

This task does not authorize an M1 probe.

This task does not authorize a generated artifact path check.

This task does not authorize Maven-layout inference from unstated conventions.

## 7. Locator-source routes permitted for governance comparison only

### Route L1 — Owner-supplied exact artifact locator

Definition:

- Owner explicitly supplies one exact EliteSync-v10 repository-relative local artifact file locator;
- literal intake may be governed separately;
- existence/content is not established by Owner supply alone.

Preserve:

`OWNER-SUPPLIED ARTIFACT LOCATOR != VERIFIED ARTIFACT PRESENCE`

`OWNER-SUPPLIED ARTIFACT LOCATOR != FRESH GENERATION`

`OWNER-SUPPLIED ARTIFACT LOCATOR != GRADLE RESOLUTION SUCCESS`

### Route L2 — deterministic declaration-only derivation

Definition:

- an exact artifact file locator may be materialized only if every path component and final filename is fully determined by explicit durable V10 declarations already in the authorized basis;
- no conventional Maven filename/layout rule may be silently imported;
- no guessed extension, classifier, filename, variant mapping, group-to-directory conversion, or metadata convention may be added unless already explicitly authorized in the durable basis.

Preserve:

`DETERMINISTIC DERIVATION != PATH SEARCH`

`EXPLICIT DIRECTORY + COORDINATE != EXPLICIT ARTIFACT FILENAME`

`DECLARED MAVEN COORDINATE != AUTHORITY TO INVENT MAVEN FILE LAYOUT`

### Route L3 — separately authorized external-standard derivation basis

Definition:

- a future separate governance task could authorize one exact external Maven-layout / artifact-filename specification as derivation authority;
- that future authority would still need to bind the explicit durable repository root / coordinate / variant to one exact V10 artifact locator;
- this task does not select, fetch, cite, or authorize any external standard.

Preserve:

`EXTERNAL STANDARD DERIVATION BASIS != CURRENT V10 DURABLE BASIS`

`EXTERNAL STANDARD AUTHORITY != ARTIFACT PRESENCE`

## 8. Required determination principles

The candidate MUST apply all of the following:

1. Do not inspect any generated output to discover a filename.
2. Do not enumerate a Maven repository directory.
3. Do not infer a `.aar` filename merely because the dependency is an AAR-producing Flutter module.
4. Do not transform group/artifact/version coordinates into a file path unless that transformation rule is already explicit in the authorized durable basis.
5. Do not treat the explicitly declared release output directory as if it explicitly names a final artifact file.
6. Do not use old-repository knowledge to fill the missing file locator.
7. Do not authorize a probe or command.
8. If current durable declarations are insufficient to uniquely identify the final artifact file, fail closed on locator materialization and identify the exact restart gate.

Preserve exactly:

`EXACT REPOSITORY DIRECTORY != EXACT ARTIFACT FILE LOCATOR`

`MAVEN COORDINATE != EXACT LOCAL ARTIFACT FILE LOCATOR UNDER CURRENT AUTHORITY`

`LOCATOR DERIVATION GOVERNANCE != LOCATOR EXISTENCE CHECK`

`LOCATOR MATERIALIZATION != ARTIFACT AVAILABILITY`

`LOCATOR ACQUISITION != METHOD EXECUTION AUTHORITY`

`NO EXACT ARTIFACT FILE LOCATOR != ARTIFACT DOES NOT EXIST`

## 9. Expected Outcome A — current basis insufficient for exact artifact-file locator materialization

If supported by the exact durable basis, use exactly:

`U-15 TD-01 M1 ARTIFACT LOCATOR DERIVATION / ACQUISITION GOVERNANCE = CURRENT DURABLE V10 DECLARATIONS IDENTIFY THE FLUTTER-MODULE HOST-OUTPUT MAVEN REPOSITORY SOURCE, A DECLARED RELEASE-ARTIFACT DIRECTORY, AND APP-LEVEL MAVEN COORDINATES, BUT DO NOT EXPLICITLY IDENTIFY ONE FINAL LOCAL ARTIFACT FILE LOCATOR — EXACT REPOSITORY DIRECTORY != EXACT ARTIFACT FILE LOCATOR — MAVEN COORDINATE != EXACT LOCAL ARTIFACT FILE LOCATOR UNDER CURRENT AUTHORITY — SILENT MAVEN-LAYOUT / ARTIFACT-FILENAME CONVENTION IMPORT IS NOT AUTHORIZED — M1 CANNOT YET BE EXECUTED — NEXT RESTART GATE = EITHER OWNER-SUPPLIED EXACT ELITESYNC-V10 ARTIFACT FILE LOCATOR THROUGH A SEPARATELY GOVERNED L1 INTAKE, OR A LATER SEPARATELY AUTHORIZED EXTERNAL-STANDARD DERIVATION BASIS UNDER L3 — NO ARTIFACT LOCATOR MATERIALIZED / ACQUIRED / AUTHORIZED — NO COMMAND AUTHORIZED — ZERO ARTIFACT PROBE — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY INSPECTION — ZERO GRADLE/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY TEST — ZERO NEW TECHNICAL CONTENT READ — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Outcome A means only:

- M1 remains the selected first method class;
- current durable V10 declaration facts do not fully determine the final artifact filename/file locator;
- no artifact locator is materialized now;
- no claim is made that an artifact exists or does not exist;
- Owner may later supply one exact V10 artifact file locator through L1;
- alternatively, a later exact governance task may establish one external Maven-layout derivation basis under L3;
- any subsequent locator intake/materialization, existence probe, content/hash check, Gradle-resolution action, technical design, or implementation requires separate authority.

Preserve exactly:

`NO EXACT ARTIFACT FILE LOCATOR != ARTIFACT DOES NOT EXIST`

`METHOD CLASS != ARTIFACT LOCATOR`

`LOCATOR ACQUISITION != METHOD EXECUTION AUTHORITY`

`LOCAL ARTIFACT AVAILABILITY != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`EVIDENCE-METHOD GOVERNANCE != TECHNICAL DESIGN`

## 10. Outcome B — current basis uniquely determines an exact locator

Use Outcome B only if the authorized durable basis itself explicitly and uniquely determines every path component and final artifact filename without importing an unstated convention.

If so, classify exactly:

`EXPECTED OUTCOME B — U-15 TD-01 M1 ARTIFACT LOCATOR DERIVATION / ACQUISITION GOVERNANCE FINDS CURRENT DURABLE V10 BASIS SUFFICIENT FOR ONE UNIQUE DECLARATION-ONLY EXACT ARTIFACT FILE LOCATOR — LOCATOR MAY BE RECORDED ONLY IN A LATER SEPARATELY AUTHORIZED MATERIALIZATION TASK — NO LOCATOR TESTING AUTHORIZED — NO COMMAND AUTHORIZED — ZERO ARTIFACT PROBE — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY INSPECTION — ZERO GRADLE/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY TEST — ZERO NEW TECHNICAL CONTENT READ — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Do NOT materialize the locator in this task even under Outcome B unless the task sheet itself explicitly provides every exact component needed to do so; this task does not.

## 11. Operational blockage

For exact main/ref/blob/topology failure use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 M1 ARTIFACT LOCATOR DERIVATION / ACQUISITION GOVERNANCE DETERMINATION MADE`

If `main` is not exactly `f44ec7bcba5670d8c99e3b0c87e9ebb1efbb4770` at task start, STOP before substantive content read except minimum ref metadata needed to detect mismatch.

If the review branch advances beyond this exact task-sheet commit before candidate publication, fail closed rather than rewriting or substituting history.

## 12. Mandatory zero activity

The candidate action MUST perform ZERO:

- artifact locator testing;
- artifact existence checks;
- artifact type/content/hash checks;
- generated-output inspection;
- local Maven directory inspection;
- Gradle cache inspection;
- repository-tree inspection;
- directory listing used for discovery;
- wildcard/glob testing;
- filename search;
- code search;
- GitHub code search;
- repository-wide or directory-wide enumeration;
- path discovery;
- Gradle/build execution;
- dependency-resolution execution;
- `syncFlutterAar` execution;
- `flutter build aar` execution;
- network/repository reachability tests;
- artifact download/upload;
- external Maven-layout specification research/read;
- source repository access;
- old-repository access;
- README use;
- FD02 use;
- architecture selection;
- technical design;
- implementation planning;
- implementation;
- participant/data activity;
- LC-03;
- LC-04;
- Phase 36.

## 13. Required candidate publication

For Outcome A or Outcome B create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_ARTIFACT_LOCATOR_DERIVATION_AND_ACQUISITION_GOVERNANCE_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_ARTIFACT_LOCATOR_DERIVATION_AND_ACQUISITION_GOVERNANCE_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: govern H-01 BIB-03 U-15 TD-01 M1 artifact locator`

Publish non-force only to:

`review/h01-bib03-u15-td01-m1-artifact-locator-derivation-acquisition-governance-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent `ACCEPT / REJECT`.

## 14. Strict phase separation

Task-sheet authoring -> STOP.

Candidate locator-derivation/acquisition governance -> STOP.

Fresh GPT L3 ACCEPT/REJECT -> STOP.

If Outcome A is accepted: Owner exact-locator input or separately authorized external-standard derivation governance -> separate later action.

Any artifact locator intake/materialization -> separate authority.

Any M1 exact artifact probe -> separate authority after exact locator is durable.

Any M2 Gradle dependency-resolution method governance/execution -> separate authority after M1 disposition if still required.

Architecture-entry determination -> separate authority.

Technical design -> separate authority.

Implementation planning / implementation -> separate authority.
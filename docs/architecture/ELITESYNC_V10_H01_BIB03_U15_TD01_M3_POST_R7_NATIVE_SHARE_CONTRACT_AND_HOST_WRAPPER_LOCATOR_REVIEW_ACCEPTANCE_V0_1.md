# EliteSync v10｜H01-BIB03-U15-TD01 M3 Post-R7 Native Share Contract + Host Wrapper Locator Review Acceptance｜v0.1

Status: `ACCEPTED — PUBLIC/STATIC FACT BUDGET EXHAUSTED FOR CURRENT PATH — FIVE NATIVE/WRAPPER GAPS REMAIN — OWNER DECISION REQUIRED BEFORE ANY CONTROLLED EXPERIMENT OR WRAPPER RECONSTRUCTION — NO SHARE RETRY AUTHORITY`

Date: 2026-09-11 (Asia/Singapore).

Repository: `zcx369658780/EliteSync-v10`.

## 1. Independent acceptance authority

Acceptance-base `main`:

`192acbce453cbd39ad7a192636ab4b25bc388c39`

Accepted candidate:

- branch: `review/h01-bib03-u15-td01-m3-post-r7-native-share-contract-and-host-wrapper-locator-review-v0-1`
- commit: `2bcd8de6956f3d669eae994454afb30a0ca5d228`
- sole parent: `192acbce453cbd39ad7a192636ab4b25bc388c39`
- tree: `74e19edcf4d1618791e1af329863cfe4844244f6`
- result path: `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M3_POST_R7_NATIVE_SHARE_CONTRACT_AND_HOST_WRAPPER_LOCATOR_REVIEW_V0_1.md`
- result blob: `1dca835648908469a0988043eeabbe0171ad0076`
- reported SHA-256: `FD2B31D900D45D6E65A91F11302DAA80542011D329FD4875039BF29E862AFC3D`

FIRST handoff remains:

`docs/architecture/ELITESYNC_V10_CURRENT_SESSION_CLOSEOUT_AND_NEXT_SESSION_HANDOFF_20260911_V0_1.md`

required blob:

`3aa2ead4cf1084b202b67856075c26ab80b0c9a2`

## 2. Acceptance findings

The candidate is accepted as a valid bounded native-contract / host-wrapper locator review.

It correctly establishes that:

- the exact installed projection exposes `IManagedWindowsVM.ShareFolder`, `VMShareFolderOptions.AllowWrite`, `VMShareFolderResult.ExtendedError`, and the limited `VMShareFolderStatus` values, but does not expose native path-separator, parent-chain creation, writable-path prerequisite, or specific `E_INVALIDARG` rules;
- bounded Microsoft-controlled documentation/source searches did not establish a public native contract for those missing semantics;
- official Sandbox documentation remains useful only for the published mapped-folder surface and does not justify promoting backslash examples or ordinary Win32 path conventions into the native UDK contract;
- the task-authorized repository anchor search found governance/result documents only and zero script/controller/source matches;
- therefore the actual host process-launch/quoting wrapper is not recoverable from the currently authorized/current-project repository evidence;
- no unresolved hypothesis was promoted to fact.

The candidate preserves source-class distinctions and remains fail closed.

## 3. Accepted classification

Accepted classification:

`WRITABLE SHARE FAILURE REMAINS PARTLY/NOT EXPLAINED — CORRECTED SHARE CONTRACT NOT YET AUTHORABLE`

Accepted causal conclusion:

`WRITABLE_SHARE_FAILURE_NOT_CAUSALLY_EXPLAINED_FROM_AUTHORIZED_EVIDENCE`

Accepted contract decision:

`CORRECTED_SHARE_CONTRACT_NOT_YET_AUTHORABLE — MISSING FACTS`

This creates no retry authority.

## 4. Accepted exhausted evidence directions

Within the current bounded authority, the following evidence directions are accepted as exhausted for this blocker unless genuinely new external evidence appears:

1. installed `wsb.exe` help/version;
2. exact installed Store package registration and manifest identity;
3. bounded installed managed implementation/projection metadata down to the `ManagedWindowsVM.ShareFolder` boundary;
4. Microsoft Learn / Microsoft-controlled public contract/source searches for the exact native identifiers and error semantics;
5. current-authority repository anchor searches for the exact failed mapping, Sandbox ID, continuation namespace, blocker text, and `wsb share` construction.

Repeated same-class searches without new evidence are not authorized merely to avoid the decision point.

## 5. Remaining material gaps

The following five accepted gaps remain:

1. native UDK accepted `guestFolder` separator/root syntax for installed behavior;
2. native UDK behavior when a new nested guest target has missing intermediate parents;
3. native UDK writable-specific guest-path/root/parent restrictions beyond `AllowWrite` and the already resolved managed policy gates;
4. native UDK accepted/canonicalized rooted `hostFolder` spelling for the recorded forward-slash path;
5. the actual host-side process-launch/quoting construction used for the failed invocation.

These are now classified as external/unavailable facts under the current evidence contract rather than ordinary repository/documentation gaps.

## 6. Owner decision gate

Owner standing automation stops here because continuing requires a genuine Owner choice that would expand risk/authority beyond the accepted fact-only path.

No next execution or experiment task is published automatically.

The Owner may choose one of these governance directions:

### Option A — remain fail closed

Preserve the existing Sandbox in quarantine and leave DEP13 blocked. No additional share experiment occurs. The recovery path remains unresolved until genuinely new Microsoft/native or wrapper evidence becomes available.

### Option B — authorize one bounded controlled share experiment

This would require a new explicit Owner authorization and a separately authored/accepted experiment contract defining exactly one hypothesis, one new evidence namespace, immutable preservation of `continuation-v0-1`, exact Sandbox binding, exact command construction, allowed pre-observations, stop conditions, and zero automatic progression to DEP13.

An experiment must not be described as proving undocumented native semantics generally; it would establish only the result of the exact tested contract on the exact installed build/runtime.

### Option C — authorize bounded host-wrapper reconstruction first

This would permit creating a new explicit current-project controller/wrapper contract from already accepted argv semantics, without running a share yet, so quoting/process-launch ambiguity is removed before any later experiment. This is implementation/recovery tooling work and therefore requires explicit Owner authorization before a task is published.

No option is selected by this acceptance.

## 7. Preserved immutable state

Remain unchanged:

`FAILED_START_RESULT_NOT_CAPTURED`

`WRITABLE_CONTINUATION_SHARE_FAILED — exit -2147024809 — Value does not fall within the expected range.`

Current rebound/quarantined Sandbox ID:

`7ebc163d-7e92-4a63-8218-47c03d5db697`

Preserved continuation namespace:

`C:/Users/zcxve/AppData/Local/Temp/EliteSync-v10-M3A-RECOVERY-cbbbaef73f3347d9ba72c0eeb04e9e71/continuation-v0-1`

It remains immutable evidence and must not be cleared, reused, renamed, overwritten, repaired, or repurposed.

## 8. Durable boundaries

Preserve D-02 durable UNKNOWN, U-14 exclusion, U-12 exact-scope rules, `TP-SOURCE-CLASS-01`, `TP-TARGET-01`, Backend/Database/PUI boundaries, README exhausted, FD02 excluded, protected index boundary, accepted legal/Safety/no-processing boundaries, no participant research/recruitment, no private-Conversation inspection, no telemetry/analytics/measurement, no Safety Operations, no new legal research, and no implementation/LC-03/LC-04/Phase36 authority.

B12 remains NOT AUTHORIZED. M2 remains separate and NOT automatically authorized.

DEP13 remains blocked until fixed-guest recovery continuation has actually succeeded and that success has been independently accepted.

## 9. Acceptance result

`ACCEPT — BOUNDED NATIVE/WRAPPER REVIEW VALID — CURRENT PUBLIC/STATIC FACT PATH EXHAUSTED — FIVE MATERIAL GAPS REMAIN — OWNER DECISION REQUIRED — NO RETRY AUTHORITY — DEP13 REMAINS BLOCKED`

# EliteSync v10 H-01 BIB-03 U-15 TD-01 M1 One-Shot Local Artifact-Existence Execution Governance Task Sheet v0.1

Status: `PROPOSED — TD-01 M1 ONE-SHOT EXACT LOCAL ARTIFACT EXISTENCE EXECUTION ONLY — ONE PRE-PROBE LOCAL HEAD CONTEXT READ + EXACTLY ONE PowerShell Test-Path -LiteralPath EVALUATION AGAINST THE FROZEN ABSOLUTE TARGET — ZERO PathType / CONTENT / HASH / FILE-METADATA / DIRECTORY-LIST / ALTERNATE-PATH / SECOND-PROBE — ZERO GRADLE/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-m1-one-shot-local-artifact-existence-execution-governance-v0-1`

Exact durable live-main authority required at task start:

`7b2b9c181b8b5ce39b818167dd854ba3cce3ae8a`

This task authorizes one later fresh GPT L3/Codex candidate action for exactly one purpose: execute the already selected M1 local artifact-availability method against the already bound Owner-selected local EliteSync-v10 evidence root and frozen absolute target, using exactly one non-content local HEAD context read followed by exactly one PowerShell literal-path existence evaluation.

This task DOES authorize exactly one M1 existence evaluation.

This task does NOT authorize any second probe, file-type check, content read, hash, metadata read, directory inspection, generated-output inspection, Gradle/build/dependency-resolution execution, network/repository artifact test, technical design, or implementation.

## 1. Permanent governance boundaries

Global/cross-project memory is NOT EliteSync-v10 governance authority.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no new V10 technical-target read, no generated-output inspection, no Maven-directory inspection, no cache inspection, no Gradle/build execution, no dependency-resolution execution, no network/repository artifact test, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

Protected unrelated local staged state remains protected. Do NOT inspect, modify, unstage, overwrite, discard, reset, clean, commit, cite, summarize, or use it.

Do NOT use `git status`, `git status --short`, untracked checks, staged/unstaged diff inspection, directory listing, cache listing, generated-output listing, filename search, code search, GitHub code search, repository-tree inspection, filesystem discovery, globbing, or path search.

Do NOT rerun `git worktree list --porcelain` or any worktree-listing command.

Historical incidents, rejected candidates, stale tasks, and deviations remain historical truth and must not be rewritten as if they did not occur.

## 2. Exact durable basis A — accepted Owner-root binding validation

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_OWNER_ROOT_INTAKE_AND_BINDING_VALIDATION_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`e5de9846d3545a912c93ea0810efb2044db6bd23`

Preserve exactly from Basis A:

`M1_EVIDENCE_WORKTREE_ROOT = D:\EliteSync-v10`

`M1_EVIDENCE_ABSOLUTE_TARGET = D:\EliteSync-v10\apps\flutter_elitesync_module\build\host\outputs\repo\com\elitesync\flutter_elitesync_module\flutter_release\1.0\flutter_release-1.0.aar`

The accepted binding validation established only that:

- the Owner root string matched the Git show-toplevel working-tree root under the explicitly allowed string normalization;
- `origin` identified `zcx369658780/EliteSync-v10`;
- local HEAD at that prior validation moment was `d1b45cc5da30bba3f377ef4ead2c677d0fb6eb88` as context only;
- root binding did NOT require `refs/heads/main`;
- cleanliness was not inspected or established;
- the absolute target string was frozen by literal joining only;
- no artifact probe was performed.

Preserve exactly:

`BOUND EVIDENCE ROOT != CLEAN WORKTREE`

`BOUND EVIDENCE ROOT != refs/heads/main WORKTREE`

`BOUND EVIDENCE ROOT != ARTIFACT PRESENCE`

`ABSOLUTE TARGET STRING != TARGET PRESENCE`

`ROOT VALIDATION != ARTIFACT PROBE`

`LOCAL WORKTREE HEAD != REMOTE MAIN UNLESS SEPARATELY VERIFIED`

`LOCAL HEAD CONTEXT != ARTIFACT PRESENCE`

## 3. Exact durable basis B — accepted M1 method-class governance

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_AAR_ARTIFACT_AVAILABILITY_AND_DEPENDENCY_RESOLUTION_EVIDENCE_METHOD_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`259e61850befe4ddc0c21ecc0c3da0539699a7c0`

Preserve from Basis B:

- selected first method class = `M1 EXACT LOCAL MAVEN ARTIFACT-AVAILABILITY PROBE`;
- M1 is non-network / non-build / non-Gradle;
- M1 may establish only exact local state-scoped presence or non-hit for the one exact expected local artifact entry;
- M1 cannot establish Gradle dependency-resolution success, artifact freshness, build success, or architecture suitability;
- M2 targeted Gradle dependency-resolution remains a later separate gate if still required.

Preserve exactly:

`LOCAL ARTIFACT AVAILABILITY != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`LOCAL ARTIFACT NON-HIT != GLOBAL ARTIFACT ABSENCE`

`LOCAL ARTIFACT HIT != FRESH GENERATION`

`LOCAL ARTIFACT HIT != BUILD SUCCESS`

## 4. Exact substantive V10 repository-content read budget

Exactly THREE substantive EliteSync-v10 repository-content reads are authorized for the later candidate action:

1. this exact task sheet;
2. Basis A at required blob `e5de9846d3545a912c93ea0810efb2044db6bd23`;
3. Basis B at required blob `259e61850befe4ddc0c21ecc0c3da0539699a7c0`.

No fourth substantive EliteSync-v10 repository-content read is authorized.

Do NOT reread any V10 technical target, external web documentation, rejected candidate, old/source repository content, README, FD02, generated output, Maven metadata, `.aar`, `.pom`, checksum, cache, or build-output content.

Minimum exact GitHub ref/commit/compare metadata needed to verify current authority/topology is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 5. Exact pre-probe local state-context command

After exact authority/blob validation, execute exactly ONCE:

`git -C 'D:\EliteSync-v10' rev-parse HEAD`

Record exactly:

`M1_PROBE_LOCAL_HEAD = <returned SHA>`

This is non-content local state context only.

The returned local HEAD is NOT required to equal remote GitHub `main`.

A change from the previously recorded local HEAD `d1b45cc5da30bba3f377ef4ead2c677d0fb6eb88` does not by itself block M1; record the current returned SHA truthfully and continue if the command succeeds.

Do NOT use any other Git command.

If this exact local HEAD command fails, STOP before the M1 probe and use the operational-blocked classification in Section 10.

Preserve exactly:

`M1 PROBE LOCAL HEAD != REMOTE MAIN UNLESS SEPARATELY VERIFIED`

`M1 PROBE LOCAL HEAD != CLEAN WORKTREE`

`M1 PROBE LOCAL HEAD != ARTIFACT PRESENCE`

## 6. Sole authorized M1 filesystem action

After the exact pre-probe local HEAD context command succeeds, execute exactly ONCE and only once:

`Test-Path -LiteralPath 'D:\EliteSync-v10\apps\flutter_elitesync_module\build\host\outputs\repo\com\elitesync\flutter_elitesync_module\flutter_release\1.0\flutter_release-1.0.aar'`

No `-PathType` is authorized.

No alternative spelling, separator form, parent path, profile/debug artifact, `.pom`, metadata, checksum, wildcard, glob, relative path, or second target may be tested.

No retry is authorized even if the returned result is surprising.

No second existence evaluation is authorized for confirmation.

No `Get-Item`, `Resolve-Path`, `Get-ChildItem`, `dir`, `ls`, `Test-Path` against any parent path, file-size read, timestamp read, attribute read, hash, content read, or type check is authorized.

## 7. Exact result interpretation

If the sole authorized `Test-Path -LiteralPath` returns exactly PowerShell boolean `True`, classify the result only as:

`M1 EXACT LOCAL ARTIFACT LOCATOR = WORKTREE-STATE HIT ONLY`

If the sole authorized `Test-Path -LiteralPath` returns exactly PowerShell boolean `False`, classify the result only as:

`M1 EXACT LOCAL ARTIFACT LOCATOR = WORKTREE-STATE NON-HIT ONLY`

Because `-PathType` is not authorized, preserve exactly:

`WORKTREE-STATE HIT != FILE TYPE FACT`

`WORKTREE-STATE HIT != AAR CONTENT FACT`

`WORKTREE-STATE HIT != FRESH GENERATION`

`WORKTREE-STATE HIT != BUILD SUCCESS`

`WORKTREE-STATE HIT != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`WORKTREE-STATE HIT != CURRENT/LATEST GENERATED STATE`

`WORKTREE-STATE NON-HIT != GLOBAL ARTIFACT ABSENCE`

`WORKTREE-STATE NON-HIT != BUILD FAILURE`

`WORKTREE-STATE NON-HIT != INVALID INTEGRATION`

`WORKTREE-STATE NON-HIT != GRADLE DEPENDENCY-RESOLUTION FAILURE`

`EXACT EXISTENCE RESULT != CONTENT FACT`

`EXACT EXISTENCE RESULT != ARCHITECTURE CHOICE`

## 8. Required Outcome A — HIT classification

If the one exact authorized M1 probe returns `True`, record exactly and verbatim:

`U-15 TD-01 M1 ONE-SHOT EXACT LOCAL ARTIFACT EXISTENCE EXECUTION = ONE AUTHORIZED PowerShell Test-Path -LiteralPath EVALUATION COMPLETED AGAINST THE FROZEN ABSOLUTE TARGET D:\EliteSync-v10\apps\flutter_elitesync_module\build\host\outputs\repo\com\elitesync\flutter_elitesync_module\flutter_release\1.0\flutter_release-1.0.aar — RESULT = True / M1 EXACT LOCAL ARTIFACT LOCATOR = WORKTREE-STATE HIT ONLY — HIT ESTABLISHES ONLY THAT AN ENTRY RESOLVES AT THIS EXACT LITERAL PATH IN THE OBSERVED LOCAL WORKTREE STATE — WORKTREE-STATE HIT != FILE TYPE FACT — WORKTREE-STATE HIT != AAR CONTENT FACT — WORKTREE-STATE HIT != FRESH GENERATION — WORKTREE-STATE HIT != BUILD SUCCESS — WORKTREE-STATE HIT != GRADLE DEPENDENCY-RESOLUTION SUCCESS — WORKTREE-STATE HIT != CURRENT/LATEST GENERATED STATE — LOCAL ARTIFACT AVAILABILITY != GRADLE DEPENDENCY-RESOLUTION SUCCESS — EXACT EXISTENCE RESULT != ARCHITECTURE CHOICE — EXACTLY ONE PRE-PROBE LOCAL HEAD CONTEXT READ — EXACTLY ONE Test-Path EXECUTION — ZERO PathType / CONTENT / HASH / FILE-METADATA / DIRECTORY-LIST / ALTERNATE-PATH / SECOND-PROBE — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO Gradle/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Do not shorten, paraphrase, translate, split, normalize, or semantically rewrite this classification.

Outcome A means only:

- the exact literal target yielded `True` once in the observed local worktree state;
- no file-type, content, freshness, build-success, dependency-resolution-success, or architecture-entry fact is established;
- no second probe is authorized;
- any next evidence-gap determination requires a separate task.

## 9. Required Outcome B — NON-HIT classification

If the one exact authorized M1 probe returns `False`, record exactly and verbatim:

`U-15 TD-01 M1 ONE-SHOT EXACT LOCAL ARTIFACT EXISTENCE EXECUTION = ONE AUTHORIZED PowerShell Test-Path -LiteralPath EVALUATION COMPLETED AGAINST THE FROZEN ABSOLUTE TARGET D:\EliteSync-v10\apps\flutter_elitesync_module\build\host\outputs\repo\com\elitesync\flutter_elitesync_module\flutter_release\1.0\flutter_release-1.0.aar — RESULT = False / M1 EXACT LOCAL ARTIFACT LOCATOR = WORKTREE-STATE NON-HIT ONLY — NON-HIT ESTABLISHES ONLY THAT NO ENTRY RESOLVES AT THIS EXACT LITERAL PATH IN THE OBSERVED LOCAL WORKTREE STATE — WORKTREE-STATE NON-HIT != GLOBAL ARTIFACT ABSENCE — WORKTREE-STATE NON-HIT != BUILD FAILURE — WORKTREE-STATE NON-HIT != INVALID INTEGRATION — WORKTREE-STATE NON-HIT != GRADLE DEPENDENCY-RESOLUTION FAILURE — LOCAL ARTIFACT NON-HIT != GLOBAL ARTIFACT ABSENCE — EXACT EXISTENCE RESULT != ARCHITECTURE CHOICE — EXACTLY ONE PRE-PROBE LOCAL HEAD CONTEXT READ — EXACTLY ONE Test-Path EXECUTION — ZERO PathType / CONTENT / HASH / FILE-METADATA / DIRECTORY-LIST / ALTERNATE-PATH / SECOND-PROBE — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO Gradle/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Do not shorten, paraphrase, translate, split, normalize, or semantically rewrite this classification.

Outcome B means only:

- the exact literal target yielded `False` once in the observed local worktree state;
- no global absence, build failure, dependency-resolution failure, integration invalidity, or architecture-entry conclusion is established;
- no retry, alternate path, directory inspection, build execution, or second probe is authorized;
- any next evidence-gap determination requires a separate task.

## 10. Operational blockage

If the exact pre-probe `git -C 'D:\EliteSync-v10' rev-parse HEAD` fails before the probe, use exactly:

`OPERATIONAL BLOCKED — U-15 TD-01 M1 ONE-SHOT EXACT LOCAL ARTIFACT EXISTENCE EXECUTION NOT PERFORMED BECAUSE THE SOLE AUTHORIZED PRE-PROBE LOCAL HEAD CONTEXT READ FAILED — ZERO Test-Path EXECUTION — ZERO ARTIFACT RESULT — ZERO DIAGNOSTIC FOLLOW-UP`

If the sole `Test-Path -LiteralPath` invocation is attempted but errors, terminates without an exact boolean result, or otherwise fails operationally, use exactly:

`OPERATIONAL BLOCKED — U-15 TD-01 M1 ONE-SHOT EXACT LOCAL ARTIFACT EXISTENCE EXECUTION ATTEMPTED ONCE BUT DID NOT RETURN AN EXACT BOOLEAN RESULT — NO HIT / NON-HIT CLASSIFICATION MADE — ZERO RETRY — ZERO DIAGNOSTIC FOLLOW-UP`

If GitHub main/ref/blob/topology validation fails before local action, use exactly:

`OPERATIONAL BLOCKED — NO U-15 TD-01 M1 ONE-SHOT LOCAL ARTIFACT EXISTENCE EXECUTION DETERMINATION MADE`

No operational-blocked path authorizes diagnostics, alternate commands, second probes, directory listings, or cleanup actions.

## 11. Mandatory zero activity

The candidate action MUST perform ZERO:

- `git worktree list --porcelain` or any worktree-listing command;
- worktree create/add/remove/move/repair/lock/unlock operations;
- branch checkout/switch/attach/detach operations;
- `git status`, `git status --short`, untracked checks;
- staged/unstaged diff inspection of protected local state;
- any Git command except exactly one `git -C 'D:\EliteSync-v10' rev-parse HEAD`;
- any `Test-Path` other than exactly one invocation against the frozen absolute target;
- `-PathType` use;
- `Get-Item`, `Resolve-Path`, `Get-ChildItem`, `dir`, `ls` or parent-directory checks;
- artifact type/content/hash/file-size/timestamp/attribute reads;
- alternate-path tests;
- second probes or confirmation probes;
- generated-output inspection;
- Maven-directory inspection;
- cache inspection;
- wildcard/glob testing;
- filename search;
- code search;
- GitHub code search;
- repository-tree inspection;
- repository-wide or directory-wide enumeration;
- path discovery;
- Gradle/build execution;
- dependency-resolution execution;
- `syncFlutterAar` execution;
- `flutter build aar` execution;
- network/repository reachability tests;
- artifact downloads/uploads;
- V10 technical-target reads;
- external web research;
- source repository access;
- old-repository access;
- README use;
- FD02 use;
- architecture selection;
- technical design;
- implementation planning;
- implementation;
- LC-03;
- LC-04;
- Phase 36.

## 12. Required candidate publication

For Outcome A, Outcome B, or operational blockage after candidate action, create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_ONE_SHOT_LOCAL_ARTIFACT_EXISTENCE_EXECUTION_GOVERNANCE_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to this task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_ONE_SHOT_LOCAL_ARTIFACT_EXISTENCE_EXECUTION_GOVERNANCE_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: execute H-01 BIB-03 U-15 TD-01 M1 one-shot artifact existence`

Publish non-force only to:

`review/h01-bib03-u15-td01-m1-one-shot-local-artifact-existence-execution-governance-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent `ACCEPT` / `REJECT`.

## 13. Strict phase separation

Task-sheet authoring -> STOP.

Candidate one-shot M1 execution -> STOP.

Fresh GPT L3 ACCEPT/REJECT -> STOP.

If accepted, normal non-force promotion -> separate acceptance action.

Post-M1 evidence-gap / next-step disposition -> separately authored later task only.

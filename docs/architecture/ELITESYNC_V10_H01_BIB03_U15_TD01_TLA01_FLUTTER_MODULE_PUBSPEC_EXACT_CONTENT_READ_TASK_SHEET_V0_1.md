# EliteSync v10 H-01 BIB-03 U-15 TD-01 TLA-01 Flutter Module Pubspec Exact Content-Read Task Sheet v0.1

Status: `PROPOSED — TD-01 EXACT CONTENT READ ONLY — ONE EXACT TARGET CONTENT READ — REVISION-SCOPED FACT EXTRACTION ONLY — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-tla01-flutter-module-pubspec-exact-content-read-v0-1`

Exact durable live-main authority required at task start:

`8ba681ef6a34c7006ab85e519229ff2b3eb8372c`

This task authorizes one later fresh GPT L3/Codex candidate action for exactly one purpose: read exactly once the complete content at one already durably accepted and revision-scoped HIT EliteSync-v10 locator, extract only explicit TD-01 toolchain / build / dependency facts from that exact content, and record those facts without reading adjacent files, searching for corroboration, deciding architecture, deciding inheritance, or making implementation decisions.

## 1. Memory isolation and permanent boundaries

Global/cross-project memory is intentionally NOT an authority source for EliteSync-v10.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

## 2. Durable exact locator and HIT basis

Durable TLA-01 intake result:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_OWNER_EXACT_LOCATOR_INTAKE_FLUTTER_MODULE_PUBSPEC_REVIEW_V0_1.md`

Required blob:

`8eccf1cd815ef60b31aaf4125e71f4907446df6a`

Durable exact-existence HIT result:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_FLUTTER_MODULE_PUBSPEC_EXACT_EXISTENCE_REVIEW_V0_1.md`

Required blob:

`dff435fbc6d5c4ca8cc2375df23eb8a99861d25d`

Exact content-read revision:

`0521b339d7294b0896683efc0d8c954bad78b228`

Exact content-read locator:

`apps/flutter_elitesync_module/pubspec.yaml`

Named non-design question:

`WHAT TOOLCHAIN / BUILD / DEPENDENCY FACTS ARE EXPLICITLY PRESENT IN THE CONTENT AT THIS EXACT OWNER-SUPPLIED V10 LOCATOR?`

The prior accepted HIT establishes only that this exact locator resolves at this exact revision.

Preserve:

`EXACT REVISION+LOCATOR HIT != FILE TYPE`

`EXACT REVISION+LOCATOR HIT != CONTENT FACT`

`EXACT REVISION+LOCATOR HIT != LATESTNESS DETERMINATION`

`EXACT REVISION+LOCATOR HIT != CORRECT REVISION DETERMINATION`

`REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT`

`CONTENT FACT != ARCHITECTURE CHOICE`

`CONTENT FACT != INHERITANCE DECISION`

## 3. Exact authorized substantive-content read budget

Exactly FOUR substantive repository-content reads are authorized for the candidate action:

1. this exact task sheet at this exact task-sheet commit;
2. the durable exact-existence HIT result at durable `main`, required blob `dff435fbc6d5c4ca8cc2375df23eb8a99861d25d`;
3. the durable TLA-01 intake result at durable `main`, required blob `8eccf1cd815ef60b31aaf4125e71f4907446df6a`;
4. exactly one complete content read of `apps/flutter_elitesync_module/pubspec.yaml` at exact revision `0521b339d7294b0896683efc0d8c954bad78b228`.

No fifth substantive repository-content read is authorized.

The target content read may occur exactly once. Do not reread any portion of the target through a second repository operation.

Minimum ref/commit metadata needed to verify exact authority/topology is allowed only if it does not become tree/path discovery or content inspection.

## 4. Sole authorized target-content read

Preferred exact read command:

`git show '0521b339d7294b0896683efc0d8c954bad78b228:apps/flutter_elitesync_module/pubspec.yaml'`

An equivalent exact read is allowed only if it retrieves the complete content of that same exact revision+locator once and does not retrieve type metadata, directory listings, adjacent files, history, blame, diffs, or other repository content.

Do NOT perform an existence check again before or after the content read.

Do NOT perform a type check.

Do NOT hash or re-fetch the target through another repository operation.

If the one exact content read unexpectedly fails despite the durable HIT, STOP and classify operational blockage; do not diagnose by searching or testing alternatives.

## 5. Authorized fact extraction

From the one exact target content only, record only facts explicitly present in that content and relevant to TD-01.

The fact register may include, when explicitly present:

### A. Manifest/package identity

- package `name`;
- package `description`;
- package `version`;
- `publish_to`;
- other directly declared manifest identity fields relevant to build/dependency interpretation.

If a listed field is absent, record `NOT PRESENT IN THIS EXACT CONTENT` only when useful for clarity; do not infer a value.

### B. Environment / SDK constraints

Record verbatim semantic values for any explicitly declared:

- Dart SDK constraint;
- Flutter SDK constraint;
- other environment constraints actually present in this exact content.

Do not convert declared constraints into claims about installed SDK versions or build success.

### C. Declared dependencies

For every entry explicitly present under `dependencies`, record:

- dependency name;
- declared constraint or source specification exactly enough to preserve its technical meaning;
- whether the manifest explicitly identifies an SDK/path/git/hosted/other source form, if present.

Do not resolve versions, inspect package registries, or inspect lockfiles.

### D. Declared development dependencies

For every entry explicitly present under `dev_dependencies`, record the same bounded fields as above.

### E. Dependency overrides

If `dependency_overrides` is explicitly present, record its entries with the same bounded semantics.

If absent, do not infer anything about resolution behavior beyond absence from this exact content.

### F. Flutter-manifest facts directly relevant to TD-01

Record only directly declared Flutter manifest facts that materially describe the package/toolchain/build/dependency shape, such as an explicit Flutter SDK dependency or package/module/plugin/platform declaration if present in this exact content.

Do not turn unrelated UI assets, fonts, application content, or product configuration into TD-01 facts unless the task's sole question requires them. Do not read other files to interpret them.

## 6. Explicitly prohibited inference

Do NOT claim from this content alone:

- that any declared dependency version is the resolved or installed version;
- that dependencies are currently downloadable;
- that the project builds, tests, runs, or deploys successfully;
- that Flutter/Dart is the final V10 architecture choice;
- that this package is the canonical or preferred V10 client;
- that old EliteSync implementation should be inherited;
- that another V10 Flutter tree does or does not exist;
- that a lockfile exists or matches this manifest;
- that current `main` contains identical target content unless separately proven;
- that the checked revision is latest or correct for architecture decisions;
- any Backend/API/Database/Flutter design conclusion.

Preserve:

`DECLARED DEPENDENCY != RESOLVED DEPENDENCY`

`DECLARED SDK CONSTRAINT != INSTALLED SDK VERSION`

`MANIFEST CONTENT != BUILD SUCCESS`

`MANIFEST CONTENT != CANONICAL V10 CLIENT`

`REVISION-SCOPED FACT != LATESTNESS / CORRECTNESS DETERMINATION`

## 7. Sole execution question

Determine ONLY:

`WHAT TD-01 TOOLCHAIN / BUILD / DEPENDENCY FACTS ARE EXPLICITLY PRESENT IN THE ONE AUTHORIZED CONTENT READ OF apps/flutter_elitesync_module/pubspec.yaml AT ELITESYNC-V10 REVISION 0521b339d7294b0896683efc0d8c954bad78b228?`

## 8. Result A — exact revision-scoped TD-01 fact register established

If the one exact target content read succeeds and supports a truthful bounded fact register, use exactly this classification:

`U-15 TD-01 EXACT CONTENT READ = ONE AUTHORIZED COMPLETE CONTENT READ OF apps/flutter_elitesync_module/pubspec.yaml AT PREDECLARED ELITESYNC-V10 REVISION 0521b339d7294b0896683efc0d8c954bad78b228 COMPLETED — REVISION-SCOPED TD-01 TOOLCHAIN / BUILD / DEPENDENCY FACT REGISTER EXTRACTED FROM THIS EXACT CONTENT ONLY — DECLARED FACTS ONLY — NO LOCKFILE / ADJACENT-FILE / HISTORY / SOURCE-REPOSITORY CORROBORATION — REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT — CONTENT FACT != ARCHITECTURE CHOICE — CONTENT FACT != INHERITANCE DECISION — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

The candidate must then include a concise structured fact register covering only the authorized categories in Section 5 that are actually supported by the target content.

Candidate accounting:

- candidate TD-01 exact-content-read reviews = `1`;
- accepted/durable exact-content-read reviews = `0`;
- target content reads = `1`;
- additional target rereads = `0`;
- adjacent implementation/config/build/workflow reads = `0`;
- lockfile reads = `0`;
- type checks = `0`;
- existence rechecks = `0`;
- repository search/enumeration/path discovery = `0/0/0`;
- technical-design actions = `0`;
- implementation-planning actions = `0`;
- implementation actions = `0`.

## 9. Operational blockage

If exact main/ref/blob/topology validation fails before the target content read, or if the one exact content read unexpectedly fails, use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 EXACT CONTENT FACT REGISTER DETERMINATION MADE`

Do not diagnose target-read failure through another repository operation.

If the review branch advances beyond this exact task-sheet commit before candidate execution/publication, fail closed rather than rewriting or substituting history.

If the target is accidentally read more than once, do not conceal it; stop and report the deviation for fresh GPT L3 disposition.

## 10. Mandatory zero activity beyond the authorized reads

The candidate action MUST perform ZERO:

- second read or partial reread of the target through repository operations;
- `pubspec.lock` read;
- `.metadata` read;
- Android Gradle file read;
- any adjacent Flutter file read;
- type check of the target;
- existence recheck of the target;
- alternate locator or revision test;
- repository-wide or directory-wide enumeration;
- repository-tree inspection;
- filename search;
- code search;
- GitHub code search;
- path discovery;
- source repository access;
- old-repository access;
- source-content reads;
- Backend/API/Database/Flutter/infrastructure/deployment design;
- architecture selection;
- implementation planning;
- implementation;
- participant/data activity;
- README use;
- FD02 use;
- LC-03;
- LC-04;
- Phase 36.

## 11. Required candidate publication

Create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_FLUTTER_MODULE_PUBSPEC_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_FLUTTER_MODULE_PUBSPEC_EXACT_CONTENT_READ_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: read H-01 BIB-03 U-15 TD-01 exact pubspec content`

Publish non-force only to:

`review/h01-bib03-u15-td01-tla01-flutter-module-pubspec-exact-content-read-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent ACCEPT/REJECT.

## 12. Strict phase separation

Task-sheet authoring -> STOP.

Candidate exact-content-read / fact extraction -> STOP.

Acceptance/promotion -> STOP.

Any follow-up TD-01 corroboration through `pubspec.lock`, `.metadata`, Gradle, CI, or another file -> separately authored exact task with its own exact locator and read budget.

Any architecture-entry determination -> separately authorized later governance task after sufficient technical facts.

Technical design -> separately authorized later task.

Implementation planning / implementation -> separately authorized later task.
# EliteSync v10 H-01 BIB-03 U-15 TD-01 M1 Exact Local Artifact-Availability Probe Environment and Method Governance Task Sheet v0.1

Status: `PROPOSED — TD-01 M1 EXACT LOCAL ARTIFACT-AVAILABILITY PROBE ENVIRONMENT / METHOD GOVERNANCE ONLY — ONE REGISTERED MAIN-WORKTREE METADATA READ MAY BE USED TO FREEZE THE FUTURE EVIDENCE WORKTREE — ZERO ARTIFACT PROBE — ZERO ARTIFACT EXISTENCE/TYPE/CONTENT/HASH CHECK — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO GRADLE/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-m1-probe-environment-method-governance-v0-1`

Exact durable live-main authority required at task start:

`20b243fe1c87db9f27c902ee6a1be27450c6d291`

This task authorizes one later fresh GPT L3/Codex governance candidate action for exactly one purpose: freeze the exact local worktree environment and exact lowest-side-effect future probe method for the already materialized M1 local generated-AAR locator, without performing the probe or inspecting the artifact, its parent directories, generated output, caches, Gradle state, dependency-resolution state, or protected staged state.

This task does NOT execute M1.

This task does NOT establish artifact presence or absence.

This task does NOT authorize any build, dependency resolution, network access, artifact read, or technical design.

## 1. Permanent governance boundaries

Global/cross-project memory is NOT EliteSync-v10 governance authority.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no new V10 technical-target read, no generated-output inspection, no Maven-directory inspection, no cache inspection, no Gradle/build execution, no dependency-resolution execution, no repository/network artifact test, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

Protected unrelated local staged state remains protected. Do NOT inspect, modify, unstage, overwrite, discard, reset, clean, commit, cite, summarize, or use it.

Do NOT use `git status`, `git status --short`, untracked checks, directory listing, cache listing, generated-output listing, filename search, code search, GitHub code search, repository-tree inspection, or filesystem discovery.

The only local worktree-topology metadata command authorized in this governance action is exactly one invocation of:

`git worktree list --porcelain`

This command is authorized only to identify the registered worktree whose branch record is exactly `refs/heads/main` and to record that worktree's exact root path for a later separately authorized M1 execution task.

Do not use metadata for other worktrees as substantive evidence.

## 2. Exact durable basis A — accepted M1 L3 artifact-locator materialization

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_L3_ARTIFACT_LOCATOR_MATERIALIZATION_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`b80d0d05065003ccd1585d4e3d2f7a00286dca3f`

Preserve exactly from Basis A:

`apps/flutter_elitesync_module/build/host/outputs/repo/com/elitesync/flutter_elitesync_module/flutter_release/1.0/flutter_release-1.0.aar`

This is the sole materialized repository-relative local generated-artifact locator for the selected M1 lane.

Preserve exactly:

`MATERIALIZED ARTIFACT LOCATOR != VERIFIED ARTIFACT PRESENCE`

`MATERIALIZED ARTIFACT LOCATOR != ARTIFACT FRESHNESS`

`MATERIALIZED ARTIFACT LOCATOR != BUILD SUCCESS`

`MATERIALIZED ARTIFACT LOCATOR != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`LOCAL GENERATED-ARTIFACT LOCATOR != GIT-TRACKED PATH CLAIM`

`MATERIALIZED ARTIFACT LOCATOR != M1 EXECUTION AUTHORITY`

## 3. Exact durable basis B — accepted M1 method-class governance

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_AAR_ARTIFACT_AVAILABILITY_AND_DEPENDENCY_RESOLUTION_EVIDENCE_METHOD_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`259e61850befe4ddc0c21ecc0c3da0539699a7c0`

Preserve from Basis B:

- selected first method class = `M1 EXACT LOCAL MAVEN ARTIFACT-AVAILABILITY PROBE`;
- M1 is non-network / non-build / non-Gradle;
- M1 may establish only exact state-scoped presence or non-hit for one exact expected local artifact entry;
- M1 cannot establish Gradle dependency-resolution success, artifact freshness, build success, or architecture suitability;
- M2 targeted Gradle dependency-resolution remains a later separate gate if still required.

Preserve exactly:

`LOCAL ARTIFACT AVAILABILITY != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`LOCAL ARTIFACT NON-HIT != GLOBAL ARTIFACT ABSENCE`

`LOCAL ARTIFACT HIT != FRESH GENERATION`

`LOCAL ARTIFACT HIT != BUILD SUCCESS`

`METHOD CLASS != COMMAND AUTHORITY`

## 4. Exact substantive V10 repository-content read budget

Exactly THREE substantive EliteSync-v10 repository-content reads are authorized for the later candidate action:

1. this exact task sheet;
2. Basis A at required blob `b80d0d05065003ccd1585d4e3d2f7a00286dca3f`;
3. Basis B at required blob `259e61850befe4ddc0c21ecc0c3da0539699a7c0`.

No fourth substantive EliteSync-v10 repository-content read is authorized.

Do NOT reread any technical target or external web documentation.

Do NOT read README, FD02, old/source repository content, rejected candidate content, generated output, Maven metadata, `.aar`, `.pom`, checksum, cache, or build-output content.

Minimum exact GitHub ref/commit/compare metadata needed to verify authority/topology is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 5. Sole environment-selection action

After exact authority/blob validation, execute exactly once:

`git worktree list --porcelain`

From that output, identify the registered worktree entry whose branch line is exactly:

`branch refs/heads/main`

Outcome A requires exactly one such registered main-branch worktree entry.

Record only:

- `M1_EVIDENCE_WORKTREE_ROOT = <exact worktree path from that entry>`

Do NOT inspect the root directory.

Do NOT use `git status`.

Do NOT inspect the main worktree's staged, unstaged, or untracked state.

Do NOT read or test the artifact locator.

If zero or more than one `refs/heads/main` worktree entries are present, fail closed under Outcome B.

Preserve exactly:

`REGISTERED MAIN WORKTREE ROOT != CLEAN WORKTREE`

`REGISTERED MAIN WORKTREE ROOT != CURRENT ARTIFACT PRESENCE`

`WORKTREE METADATA READ != REPOSITORY FILE/PATH ENUMERATION`

`WORKTREE ROOT SELECTION != ARTIFACT PROBE`

## 6. Exact future probe-target construction rule

For a later separately authorized execution task only, freeze the target construction as:

`M1_EVIDENCE_TARGET = M1_EVIDENCE_WORKTREE_ROOT + apps/flutter_elitesync_module/build/host/outputs/repo/com/elitesync/flutter_elitesync_module/flutter_release/1.0/flutter_release-1.0.aar`

The later execution task must use literal path joining only. It must not resolve, canonicalize by probing, enumerate parent directories, glob, search, or test alternate paths.

This governance action may record the future exact absolute target string after literal joining only if the worktree root is uniquely resolved from `git worktree list --porcelain`.

Literal joining is not an existence check.

Preserve exactly:

`ABSOLUTE TARGET STRING != TARGET PRESENCE`

`LITERAL PATH JOIN != FILESYSTEM PROBE`

`EXACT TARGET != PERMISSION TO SEARCH PARENT DIRECTORY`

## 7. Exact future M1 probe method to freeze

The future execution method shall be exactly one non-content local filesystem existence evaluation against the frozen absolute target using PowerShell literal-path semantics:

`Test-Path -LiteralPath '<FROZEN_EXACT_ABSOLUTE_TARGET>'`

No `-PathType` option is authorized in the future M1 existence action unless separately governed later.

No content read, hash, file length, timestamp, metadata listing, parent-directory listing, wildcard, alternate path, or second existence check is implied.

Future interpretation must be frozen as:

- `True` -> `M1 EXACT LOCAL ARTIFACT LOCATOR = WORKTREE-STATE HIT ONLY`;
- `False` -> `M1 EXACT LOCAL ARTIFACT LOCATOR = WORKTREE-STATE NON-HIT ONLY`.

Preserve exactly:

`WORKTREE-STATE HIT != FRESH GENERATION`

`WORKTREE-STATE HIT != BUILD SUCCESS`

`WORKTREE-STATE HIT != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`WORKTREE-STATE NON-HIT != GLOBAL ARTIFACT ABSENCE`

`WORKTREE-STATE NON-HIT != BUILD FAILURE`

`WORKTREE-STATE NON-HIT != INVALID INTEGRATION`

`EXACT EXISTENCE RESULT != FILE TYPE FACT`

`EXACT EXISTENCE RESULT != CONTENT FACT`

## 8. Future execution-state metadata contract

This governance task does not collect this metadata now, but a later execution task may separately authorize minimum non-content metadata immediately before the one exact probe to contextualize the local evidence state, such as:

- verifying the frozen worktree root is still registered to `refs/heads/main`;
- recording the local worktree `HEAD` commit via a non-content exact-ref command;
- recording probe time.

No cleanliness check is required or authorized.

Local `HEAD` equality with remote `main` is not established by this governance task and must not be silently assumed.

Preserve exactly:

`LOCAL WORKTREE HEAD != REMOTE MAIN UNLESS SEPARATELY VERIFIED`

`ARTIFACT WORKTREE STATE != GIT COMMIT CONTENT`

`PROTECTED STAGED STATE != ARTIFACT EVIDENCE`

## 9. Required Outcome A classification

If exactly one registered `refs/heads/main` worktree is identified and the future method contract can be frozen without probing the artifact, record exactly and verbatim:

`U-15 TD-01 M1 EXACT LOCAL ARTIFACT-AVAILABILITY PROBE ENVIRONMENT / METHOD GOVERNANCE = EXACTLY ONE REGISTERED LOCAL WORKTREE FOR refs/heads/main IDENTIFIED BY ONE AUTHORIZED git worktree list --porcelain METADATA READ — THAT WORKTREE ROOT IS FROZEN AS THE FUTURE M1 EVIDENCE WORKTREE — THE ACCEPTED REPOSITORY-RELATIVE GENERATED-AAR LOCATOR IS TO BE LITERALLY JOINED TO THAT ROOT TO FORM ONE EXACT FUTURE ABSOLUTE TARGET — FUTURE M1 METHOD IS FROZEN AS EXACTLY ONE PowerShell Test-Path -LiteralPath EXISTENCE EVALUATION AGAINST THAT FROZEN TARGET — NO PathType / CONTENT / HASH / FILE-METADATA / DIRECTORY-LIST / ALTERNATE-PATH / SECOND-PROBE AUTHORIZED — THIS GOVERNANCE ACTION DOES NOT EXECUTE Test-Path — NO ARTIFACT PRESENCE OR ABSENCE DETERMINATION MADE — WORKTREE-STATE HIT != FRESH GENERATION — WORKTREE-STATE HIT != BUILD SUCCESS — WORKTREE-STATE HIT != GRADLE DEPENDENCY-RESOLUTION SUCCESS — WORKTREE-STATE NON-HIT != GLOBAL ARTIFACT ABSENCE — LOCAL WORKTREE HEAD != REMOTE MAIN UNLESS SEPARATELY VERIFIED — ZERO ARTIFACT PROBE — ZERO ARTIFACT EXISTENCE/TYPE/CONTENT/HASH CHECK — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO Gradle/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Do not shorten, paraphrase, translate, split, normalize, or semantically rewrite this classification.

Outcome A means only:

- one registered main worktree root is selected and recorded;
- one future absolute target may be constructed by literal joining only;
- one future `Test-Path -LiteralPath` method is frozen but not executed;
- protected staged state is not inspected or used;
- no artifact availability result exists yet;
- future M1 execution requires a new separately authored execution task.

## 10. Outcome B / operational blockage

If zero or more than one registered `refs/heads/main` worktree entry is found, or exact authority/blob validation fails, classify exactly:

`EXPECTED OUTCOME B — U-15 TD-01 M1 EXACT LOCAL ARTIFACT-AVAILABILITY PROBE ENVIRONMENT / METHOD GOVERNANCE CANNOT FREEZE ONE UNIQUE REGISTERED refs/heads/main EVIDENCE WORKTREE UNDER CURRENT LOCAL METADATA — FAIL CLOSED — NO FUTURE ABSOLUTE TARGET FROZEN — NO Test-Path METHOD EXECUTION AUTHORIZED — ZERO ARTIFACT PROBE — ZERO ARTIFACT EXISTENCE/TYPE/CONTENT/HASH CHECK — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO Gradle/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

If `main` is not exactly `20b243fe1c87db9f27c902ee6a1be27450c6d291` at task start, STOP before substantive content read except minimum ref metadata required to detect mismatch.

If the review branch advances beyond this exact task-sheet commit before candidate publication, fail closed rather than rewriting or substituting history.

## 11. Mandatory zero activity

The candidate action MUST perform ZERO:

- `Test-Path` execution against the artifact;
- artifact existence/type/content/hash checks;
- generated-output inspection;
- Maven-directory inspection;
- cache inspection;
- directory listing used for discovery;
- wildcard/glob testing;
- filename search;
- code search;
- GitHub code search;
- repository-tree inspection;
- repository-wide or directory-wide enumeration;
- path discovery;
- `git status` / `git status --short` / untracked checks;
- staged/unstaged diff inspection of protected local state;
- Gradle/build execution;
- dependency-resolution execution;
- `syncFlutterAar` execution;
- `flutter build aar` execution;
- network/repository reachability tests;
- artifact downloads/uploads;
- V10 technical-target reads;
- external web research;
- source repository access;
- old-repository access;
- README use;
- FD02 use;
- architecture selection;
- technical design;
- implementation planning;
- implementation;
- LC-03;
- LC-04;
- Phase 36.

## 12. Required candidate publication

For Outcome A or Outcome B create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_EXACT_LOCAL_ARTIFACT_AVAILABILITY_PROBE_ENVIRONMENT_AND_METHOD_GOVERNANCE_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to this task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_EXACT_LOCAL_ARTIFACT_AVAILABILITY_PROBE_ENVIRONMENT_AND_METHOD_GOVERNANCE_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: govern H-01 BIB-03 U-15 TD-01 M1 probe environment method`

Publish non-force only to:

`review/h01-bib03-u15-td01-m1-probe-environment-method-governance-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent `ACCEPT` / `REJECT`.

## 13. Strict phase separation

Task-sheet authoring -> STOP.

Candidate environment/method governance -> STOP.

Fresh GPT L3 ACCEPT/REJECT -> STOP.

If accepted, normal non-force promotion -> separate acceptance action.

M1 one-shot exact filesystem existence execution -> separately authored later task only.

M1 result sufficiency / next-gap disposition -> separate later governance.

Targeted Gradle dependency-resolution, if still required -> separate later method gate.

Architecture-entry determination -> separate later authority.

Technical design -> separate later authority.

Implementation planning / implementation -> separate later authority.
# EliteSync v10 H-01 BIB-03 Basis-Instance Governance Task Sheet v0.1

Status: `PROPOSED — BASIS-INSTANCE GOVERNANCE REVIEW ONLY — DURABLE SOURCE CANDIDATE = 1 — ZERO SOURCE READS — ZERO BINDING/U-12 ACTIVITY`

Review branch:

`review/h01-bib03-basis-instance-governance-v0-1`

Exact durable live-main authority required at task start:

`6b493e115100c0afefdcb0cee1f0de665c57226a`

This task sheet is issued under Owner-delegated bounded governance for exactly one purpose: determine whether the one exact durable source candidate may be admitted as exactly one bounded identity-only basis instance for a later, separately governed source-target binding gate.

This task does NOT authorize old-repository access, source-repository access, source reads, source-path discovery, source-target binding, U-12 target creation, latestness determination, inheritance-suitability determination, technical design, implementation, or irreversible action.

## 1. Durable gate-start state

Preserve exactly:

- accepted NR-METHOD-01 eligibility reviews = `1`
- accepted NR-METHOD-01 selection reviews = `1`
- accepted NR-METHOD-01 execution-authority governance reviews = `1`
- `NR-METHOD-01 eligible / selected / executed = 1 / 1 / 1`
- `NR-METHOD-01 execution tasks authored / accepted / executed = 1 / 1 / 1`
- `source identity emitted / accepted = 1 / 1`
- accepted source-candidate governance reviews = `1`
- durable source candidate = `1`
- basis instance = `0`
- source-target binding = `0`
- U-12 concrete targets = `0`
- accepted source-path hits = `0`
- old-repository access = `0`
- source-repository access = `0`
- source reads = `0`
- accepted / unauthorized content-read ledger = `13 / 1`
- cumulative unauthorized enumeration incidents = `2`
- `POST-E3 PROMOTION PUSH-OPERATION COUNT = UNKNOWN / NOT INDEPENDENTLY VERIFIABLE`
- `OWNER LIMITATION — CURRENT SHA IS THE LATEST VERSION TO INHERIT = UNPROVEN`

Exact durable source-candidate classification:

`SOURCE-CANDIDATE GOVERNANCE = EXACT ACCEPTED PA-01 IMMUTABLE SOURCE IDENTITY ADMISSIBLE AS ONE BOUNDED SOURCE CANDIDATE ONLY — NR-METHOD-01 EXECUTED PRESERVED — ZERO SOURCE READS — ZERO OLD-REPOSITORY/SOURCE-REPOSITORY ACCESS — ZERO BASIS/BINDING/U-12 AUTHORITY — LATEST/CORRECT-INHERITANCE REMAINS UNPROVEN`

Exact durable source-candidate artifact:

`docs/architecture/ELITESYNC_V10_H01_BIB03_SOURCE_CANDIDATE_GOVERNANCE_REVIEW_V0_1.md`

Required source-candidate artifact blob:

`35705fc8dfe295263a79fa98779438ce2de13f70`

## 2. Exact durable source candidate

The sole durable source candidate under this governance review is exactly the already accepted PA-01 immutable source identity:

`source_repository=zcx369658780/EliteSync`

`source_identity_kind=Git commit object`

`source_identity_value=0f06e895a89629a7f4f0cf0c62b784446ecf515a`

`immutable_revision=0f06e895a89629a7f4f0cf0c62b784446ecf515a`

`expected_object_kind=commit`

`source identity emitted / accepted = 1 / 1`

No alternate repository, revision, commit, OID, ref, branch, tag, tree, file, path, locator, metadata object, endpoint, package URI, source coordinate, source version, or candidate may be introduced, discovered, ranked, normalized, substituted, or inferred.

The durable source candidate is governance metadata only for this task. It MUST NOT be dereferenced, fetched, inspected, shown as source content, diffed, searched, enumerated, re-verified, or otherwise read from the old/source repository.

## 3. Definition of a bounded identity-only basis instance

For this task only, a `basis instance` means exactly one governance object whose sole content is the already durable source-candidate identity tuple in Section 2 plus its existing durable governance provenance.

A bounded identity-only basis instance:

- contains no source code, source text, commit content, tree content, file content, diff, path, locator, package URI, or source-derived evidence;
- performs no source dereference or source read;
- does not assert that the source candidate is latest, correct to inherit, suitable to inherit, technically valid, complete, safe, or production-ready;
- does not bind the basis to `TP-TARGET-01` or any other target;
- does not create a U-12 target;
- does not create source-path authority;
- does not create technical-design or implementation authority.

The only purpose of a durable basis instance, if later independently accepted and promoted, is to provide one exact governance-side basis object that a later separately governed source-target binding gate may consider.

## 4. Sole governance question

Determine ONLY:

`GIVEN ONE DURABLE BOUNDED SOURCE CANDIDATE, MAY THAT EXACT CANDIDATE — WITHOUT SOURCE ACCESS, SOURCE READS, SOURCE-PATH DISCOVERY, TARGET BINDING, U-12 ACTIVITY, LATESTNESS DETERMINATION, OR INHERITANCE-SUITABILITY DETERMINATION — BE ADMITTED AS EXACTLY ONE IDENTITY-ONLY BOUNDED BASIS INSTANCE FOR A LATER SEPARATELY GOVERNED SOURCE-TARGET BINDING GATE?`

No other source, candidate, basis, target, binding, inheritance, design, implementation, legal, Safety, or lifecycle question may be decided here.

## 5. Mandatory distinctions

Preserve exactly:

`SOURCE CANDIDATE != BASIS INSTANCE`

`BASIS INSTANCE != SOURCE-TARGET BINDING`

`BASIS INSTANCE != U-12 TARGET`

`BASIS INSTANCE != SOURCE READ AUTHORITY`

`BASIS INSTANCE != SOURCE-PATH AUTHORITY`

`BASIS INSTANCE != LATEST INHERITABLE VERSION`

`BASIS INSTANCE != CORRECT REVISION TO INHERIT`

`BASIS INSTANCE != INHERITANCE-SUITABILITY DETERMINATION`

`BASIS INSTANCE != TECHNICAL DESIGN AUTHORITY`

`BASIS INSTANCE != IMPLEMENTATION AUTHORITY`

`BASIS INSTANCE != EVIDENCE ACCEPTANCE`

Durable source candidacy does not automatically create a basis instance. This governance review is required precisely because basis-instance status is a distinct later state.

## 6. Outcome A — bounded basis-instance admission

Outcome A is permitted only if the exact durable source candidate can be admitted as one identity-only bounded basis instance without any new source inspection, target binding, or inheritance determination.

Required Outcome A classification:

`BASIS-INSTANCE GOVERNANCE = EXACT DURABLE SOURCE CANDIDATE ADMISSIBLE AS ONE IDENTITY-ONLY BOUNDED BASIS INSTANCE ONLY — ZERO SOURCE READS — ZERO OLD-REPOSITORY/SOURCE-REPOSITORY ACCESS — ZERO SOURCE-TARGET BINDING — ZERO U-12 AUTHORITY — LATEST/CORRECT-INHERITANCE REMAINS UNPROVEN`

For candidate-stage Outcome A accounting only:

- candidate basis-instance governance reviews = `1`
- accepted basis-instance governance reviews = `0` pending fresh GPT L3 independent acceptance and durable promotion
- durable source candidate = `1`
- basis instance = `1 candidate / 0 durable`
- source-target binding = `0`
- U-12 concrete targets = `0`
- accepted source-path hits = `0`
- source reads = `0`
- old-repository access = `0`
- source-repository access = `0`
- network fetch/pull = `0`

Outcome A may authorize only later independent acceptance and durable promotion of the basis-instance determination.

Outcome A MUST NOT create a source-target binding, U-12 target, source-read authority, source-path authority, evidence/PUI authority, latestness determination, inheritance-suitability determination, technical-design authority, or implementation authority.

A later source-target binding action requires another separately authored exact governance task after Outcome A is independently accepted and durably promoted.

## 7. Expected Outcome B — fail closed

If durable governance is insufficient to admit the durable source candidate as a basis instance without forbidden source or target activity, classify exactly:

`EXPECTED OUTCOME B — BASIS INSTANCE NOT ADMITTED UNDER CURRENT DURABLE GOVERNANCE — FAIL CLOSED — DURABLE SOURCE CANDIDATE PRESERVED — ZERO NEW BINDING/U-12/SOURCE AUTHORITY CREATED`

Outcome B preserves:

- durable source candidate = `1`
- basis instance = `0`
- source-target binding = `0`
- U-12 concrete targets = `0`
- source reads = `0`
- old/source repository access = `0`

Outcome B creates no fallback source search, alternate candidate, alternate revision, source-path search, target search, binding, enumeration, latestness check, or inheritance-suitability review.

## 8. Operational blockage

For any prerequisite failure, use only:

`OPERATIONAL BLOCKED — NO BASIS-INSTANCE GOVERNANCE DETERMINATION MADE`

Operational blockage is not Outcome B and must not be rewritten as substantive negative evidence.

## 9. Mandatory zero-activity semantics

This governance review MUST perform ZERO:

- old `D:\EliteSync` access;
- source repository access;
- source commit dereference;
- source content reads;
- source identity re-verification;
- source candidate re-discovery or substitution;
- `git show` against the accepted source;
- `git log` against the accepted source;
- `git diff` against the accepted source;
- `git cat-file` against the accepted source;
- source branch/tag/ref/history/reachability/ancestry/recency inspection;
- source-path discovery;
- locator reconstruction;
- package-URI normalization;
- network fetch/pull;
- repository/source/web search for source material;
- repository-wide or directory-wide enumeration;
- alternate-source discovery;
- alternate-method review, comparison, ranking, selection, activation, or execution;
- latestness determination;
- correct-revision-to-inherit determination;
- inheritance-suitability determination;
- source-target binding;
- U-12 activity;
- evidence/PUI activity;
- technical design;
- implementation;
- LC-03;
- LC-04;
- Phase 36;
- FD02 access/search/hash/inspection/use.

Preserve explicitly:

`old-repository access = 0`

`source-repository access = 0`

`source reads = 0`

`network fetch/pull = 0`

`accepted source-path hits = 0`

## 10. Target boundary

Preserve `TP-TARGET-01` as the sole evidence-subject target already defined by higher governance, but do not bind the basis instance to it in this task.

Preserve exactly:

`BASIS INSTANCE != TP-TARGET-01 BINDING`

`TP-TARGET-01 EXISTS AS ABSTRACT TARGET != SOURCE-TARGET BINDING EXISTS`

No target-side scope, locator, path, implementation object, technical object, or U-12 target may be introduced here.

## 11. Permanent enumeration boundary

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

Do not use:

- `rg --files`
- `find`
- `fd`
- `git ls-files`
- `git ls-tree`
- recursive `ls`
- recursive `dir`
- `tree`
- repository/directory globs
- GitHub code search
- filename search
- `git grep`
- `git status`
- `git status --short`
- untracked-file checks

Allowed EliteSync-v10 cleanliness checks only:

- `git diff --quiet`
- `git diff --cached --quiet`
- `git stash list`
- `git worktree list --porcelain`

## 12. Incident and historical accounting

Preserve exactly:

`INCIDENT-BIB03-PA01-VB-PROMOTION-ENUM-02 — UNAUTHORIZED V10 REPOSITORY FILE/PATH ENUMERATION VIA rg --files OCCURRED DURING THE PRIOR V-B PROMOTION ATTEMPT — ENUMERATION OUTPUT IS NOT AUTHORIZED FOR USE — ZERO OLD-REPOSITORY ACCESS — ZERO CONTENT READS — ZERO PROMOTION PUSHES — PRIOR ATTEMPT OPERATIONALLY BLOCKED`

Preserve:

`Do not inspect, reconstruct, summarize, search within, or use prior enumeration output.`

Historical accepted V-B remains immutable:

- historical Check 1 = `PASS`
- historical Check 2 = `PASS`
- historical Check 3 = `PASS`
- historical Check 4 = `FAIL — exact authorized invocation failed before successful Git execution`
- historical Check 5 = `NOT REACHED`

Later accepted PowerShell-safe C-A remains separately durable:

- accepted completion executions = `1`
- accepted PA-01 completion outcome = `C-A`
- accepted verified PA-01 identity facts = `1`
- new C4 / C5 = `PASS / PASS`
- new completion old-repository Git commands = `1`
- new completion old-repository content reads = `0`
- accepted source-identity acceptance reviews = `1`

Also preserve:

- accepted NR-METHOD-01 eligibility reviews = `1`
- accepted NR-METHOD-01 selection reviews = `1`
- accepted NR-METHOD-01 execution-authority governance reviews = `1`
- `NR-METHOD-01 execution tasks authored / accepted / executed = 1 / 1 / 1`
- `NR-METHOD-01 eligible / selected / executed = 1 / 1 / 1`
- accepted source-candidate governance reviews = `1`
- accepted / unauthorized content-read ledger = `13 / 1`
- cumulative unauthorized enumeration incidents = `2`

Do not rewrite or increment any immutable historical fact merely because this basis-instance gate is being reviewed.

## 13. Owner limitation and inheritance boundary

Preserve exactly:

`OWNER LIMITATION — CURRENT SHA IS THE LATEST VERSION TO INHERIT = UNPROVEN`

`VERIFICATION OF THE PA-01 COMMIT IDENTITY MUST NOT BE TREATED AS PROOF THAT IT IS THE LATEST OR CORRECT REVISION TO INHERIT`

`ACCEPTED SOURCE IDENTITY != LATEST INHERITABLE VERSION`

`ACCEPTED SOURCE IDENTITY != CORRECT REVISION TO INHERIT`

`ACCEPTED SOURCE IDENTITY != INHERITANCE-SUITABILITY DETERMINATION`

`SOURCE CANDIDATE != LATEST INHERITABLE VERSION`

`SOURCE CANDIDATE != CORRECT REVISION TO INHERIT`

`SOURCE CANDIDATE != INHERITANCE-SUITABILITY DETERMINATION`

`BASIS INSTANCE != LATEST INHERITABLE VERSION`

`BASIS INSTANCE != CORRECT REVISION TO INHERIT`

`BASIS INSTANCE != INHERITANCE-SUITABILITY DETERMINATION`

No alternate commits, branches, tags, refs, history, ancestry, reachability, recency, or latestness may be inspected in the accepted source repository.

## 14. README, FD02, and higher governance

README documentary-read budget remains exhausted. Do not read, quote, summarize, inspect, search, parse, hash, reconstruct, or use README content or locators.

FD02 remains permanently excluded. Perform ZERO FD02 access/search/hash/re-hash/inspection/read/modify/move/delete/stage/commit/citation/selection/allowlisting/use. Do not search for or re-prove FD02 presence or uniqueness.

Preserve all accepted ADRs, durable UNKNOWNs, legal/Safety boundaries, U-14 exclusion, U-12 exact-scope rules, and no-processing/no-implementation limits.

Preserve specifically:

- D-02 unresolved;
- `D02-DURABLE-UNKNOWN-01`;
- U-15 unresolved;
- U-14 exclusion;
- `TP-SOURCE-CLASS-01` as the sole abstract source-class envelope;
- `TP-TARGET-01` as the sole evidence-subject target;
- Backend `0/10 NOT INSPECTED / NOT AUTHORIZED`;
- Database `0/8 NOT INSPECTED / NOT AUTHORIZED`;
- deferred PUI;
- PUI-PREREQ-12=`0`;
- technical design unauthorized;
- implementation unauthorized;
- LC-03 unauthorized;
- LC-04 unauthorized;
- Phase 36 unauthorized.

Do not perform participant research/recruitment/data activity, private-Conversation inspection, telemetry/analytics/measurement, Safety Operations, legal research, technical design, implementation, LC-03, LC-04, or Phase 36 activity.

## 15. Historical push-audit UNKNOWN

Preserve exactly:

`POST-E3 PROMOTION PUSH-OPERATION COUNT = UNKNOWN / NOT INDEPENDENTLY VERIFIABLE`

Do not convert this historical UNKNOWN into an exact aggregate count or combine it with current-call push counts.

## 16. Required review result

For Outcome A or Expected Outcome B, create exactly one candidate result artifact:

`docs/architecture/ELITESYNC_V10_H01_BIB03_BASIS_INSTANCE_GOVERNANCE_REVIEW_V0_1.md`

If the result path already exists on the exact task-sheet commit, STOP rather than overwrite it.

The candidate result must state clearly whether Outcome A or Expected Outcome B applies and must preserve every mandatory distinction and zero-activity boundary in this task sheet.

The candidate result is NOT independently accepted and does NOT create a durable basis instance merely by being authored.

## 17. Candidate publication

Candidate result sole parent must be this exact task-sheet commit.

Changed-path count relative to this task-sheet commit must be exactly `1`.

Sole changed path must be:

`docs/architecture/ELITESYNC_V10_H01_BIB03_BASIS_INSTANCE_GOVERNANCE_REVIEW_V0_1.md`

Required commit subject:

`docs: review H-01 BIB-03 basis instance governance`

Publish non-force only to:

`review/h01-bib03-basis-instance-governance-v0-1`

Do NOT promote `main`.

Do NOT create a durable basis instance, source-target binding, or U-12 target during candidate authoring.

After candidate publication, STOP.

A fresh GPT L3 must independently ACCEPT or REJECT the exact candidate before any durable promotion.

Even after a later Outcome A acceptance/promotion, source-target binding remains a separate later governance gate.
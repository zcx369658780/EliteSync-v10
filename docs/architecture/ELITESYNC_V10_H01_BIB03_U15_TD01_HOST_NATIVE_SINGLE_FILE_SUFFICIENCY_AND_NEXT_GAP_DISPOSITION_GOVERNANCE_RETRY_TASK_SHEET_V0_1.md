# EliteSync v10 H-01 BIB-03 U-15 TD-01 Host / Native Single-File Sufficiency and Next-Gap Disposition Governance RETRY Task Sheet v0.1

Status: `PROPOSED — TD-01 HOST / NATIVE SINGLE-FILE FACT SUFFICIENCY / NEXT-GAP DISPOSITION GOVERNANCE RETRY ONLY — ZERO NEW TECHNICAL CONTENT READ — ZERO TARGET REREAD — ZERO LOCATOR ACQUISITION — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-host-native-single-file-sufficiency-next-gap-disposition-governance-retry-v0-1`

Exact durable live-main authority required at task start:

`c437345d9bf906b33641fde11050a80f8068da75`

This task authorizes one later fresh GPT L3/Codex governance candidate action for exactly one purpose: determine what the now-durable revision-scoped single-file `apps/android/app/build.gradle.kts` fact register establishes for the selected TD-01 `HOST / NATIVE BUILD INTEGRATION` evidence-gap class, whether that single-file evidence is sufficient for architecture-entry determination, what material TD-01/host-integration facts remain unestablished, and, if more evidence is needed, identify exactly one next bounded evidence-gap class without acquiring, guessing, searching for, or testing any locator.

This is a RETRY task because a prior prematurely authored task commit exists on a separate review branch:

`ad84587a8bd0d7fb5e373417aa94b33be936b58c`

That prior task has sole parent:

`8b118261f2e4ec1b698bd25ce35db935034074f0`

and was authored before the pre-acceptance promotion deviation disposition became durable on current `main`.

Therefore preserve:

`PREMATURELY AUTHORED STALE TASK != CURRENT GOVERNANCE AUTHORITY`

Do not promote, amend, rewrite, rebase, cherry-pick, merge, reuse, or rely on `ad84587a8bd0d7fb5e373417aa94b33be936b58c` as the current task authority.

## 1. Memory isolation and permanent boundaries

Global/cross-project memory is intentionally NOT an authority source for EliteSync-v10.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no new V10 technical-content read, no target reread, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

## 2. Exact durable basis

### Basis A — accepted pre-acceptance promotion deviation disposition

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_HOST_NATIVE_CONTENT_READ_PREACCEPTANCE_PROMOTION_DEVIATION_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`53b82934711b8ba71e89f0fe72f74cb9890d7f2b`

Preserve from Basis A:

- exact content-read candidate `8b118261f2e4ec1b698bd25ce35db935034074f0` was present on `main` before the fresh GPT L3 acceptance gate completed;
- required promotion order was violated;
- the same exact candidate was later fresh-independently reviewed and substantively accepted as Result A;
- no force / rollback / rewrite / re-promotion is required or authorized;
- `PRE-ACCEPTANCE PROMOTION DEVIATION != TECHNICAL FACT INVALIDATION`;
- `SUBSTANTIVE ACCEPTANCE != NORMAL PROMOTION-SEQUENCE COMPLIANCE`;
- `POST-E3 PROMOTION PUSH-OPERATION COUNT = UNKNOWN / NOT INDEPENDENTLY VERIFIABLE`.

### Basis B — accepted host/native single-file content fact register

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_TLA01_HOST_NATIVE_BUILD_INTEGRATION_EXACT_CONTENT_READ_REVIEW_V0_1.md`

Required blob:

`b201348a6a62f7bd2c7d09d1f20b20554cdabef3`

Preserve from Basis B, only at its exact authorized revision+locator source:

`5ed1c04b822b72327864e9c6de63a80a77e88eb4:apps/android/app/build.gradle.kts`

Established declarations include:

- Flutter module directory reference = `../flutter_elitesync_module`;
- module-local `.android/local.properties` reference for `flutter.sdk` when present;
- declared Flutter SDK selection order: module-local `flutter.sdk`, host `local.properties` `flutter.sdk`, then `FLUTTER_HOME`;
- declared Flutter executable derivation/fallback;
- declared release AAR output location beneath `build/host/outputs/repo/.../flutter_release/1.0`;
- `syncFlutterAar` registered as an `Exec` task;
- declared command = Flutter `build aar --no-debug --no-profile` plus declared Dart defines;
- declared task inputs/outputs relevant to AAR generation;
- declared CI condition around an existing release AAR output path;
- `preBuild` declares `dependsOn(syncFlutterAar)`;
- declared Flutter artifact consumption:
  - `debugImplementation("com.elitesync.flutter_elitesync_module:flutter_release:1.0")`;
  - `profileImplementation` -> `com.elitesync.flutter_elitesync_module:flutter_profile:1.0`;
  - `releaseImplementation("com.elitesync.flutter_elitesync_module:flutter_release:1.0")`.

Preserve exactly:

`DECLARED FLUTTER MODULE PATH != VERIFIED MODULE PATH`

`DECLARED AAR GENERATION TASK != AAR BUILD SUCCESS`

`DECLARED TASK DEPENDENCY != SUCCESSFUL TASK EXECUTION`

`DECLARED AAR DEPENDENCY != RESOLVED AAR DEPENDENCY`

`SINGLE FILE CONTENT FACT != SETTINGS-LEVEL REPOSITORY REGISTRATION`

`SINGLE FILE CONTENT FACT != COMPLETE HOST BUILD GRAPH`

`REVISION-SCOPED CONTENT FACT != CURRENT/LATEST FACT`

`CONTENT FACT != ARCHITECTURE CHOICE`

`CONTENT FACT != INHERITANCE DECISION`

### Basis C — prior TD-01 second-fact sufficiency / next-gap disposition

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_SECOND_FACT_SUFFICIENCY_AND_NEXT_GAP_DISPOSITION_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`af4e0cde80dc2bbd8854c4a98f4dfc93f39cb2e4`

Preserve from Basis C:

- TD-01 remained `PARTIALLY DISCOVERED`;
- manifest + generated/project toolchain metadata were established at revision-scoped evidence level;
- resolved dependency state, installed toolchain state, host/native build integration, and build-success/workflow facts remained unestablished;
- TD-01 was not yet sufficient for architecture-entry determination;
- previous next priority evidence-gap class was `HOST / NATIVE BUILD INTEGRATION`.

No other repository artifact is authorized as substantive basis.

## 3. Exact substantive-content read budget

Exactly FOUR substantive repository-content reads are authorized for the candidate action:

1. this exact RETRY task sheet;
2. Basis A;
3. Basis B;
4. Basis C.

No fifth substantive repository-content read is authorized.

Do NOT reread:

`apps/android/app/build.gradle.kts`

Do NOT check or read any settings-level file, including any path previously seen in old-repository material.

Do NOT reread `.metadata`, `pubspec.yaml`, or read `pubspec.lock`, AndroidManifest, Kotlin/Java source, CI/workflows, or any other Gradle/native-build file.

Minimum ref/commit metadata needed for exact authority/topology validation is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 4. Sole governance question

Determine ONLY:

`GIVEN THE DURABLE REVISION-SCOPED SINGLE-FILE HOST / NATIVE BUILD INTEGRATION FACT REGISTER, WHAT HOST/NATIVE INTEGRATION FACTS ARE NOW ESTABLISHED, WHAT MATERIAL FACTS REMAIN UNESTABLISHED, IS THE CURRENT TD-01 FACT SET NOW SUFFICIENT FOR ARCHITECTURE-ENTRY DETERMINATION, AND, IF NOT, WHAT SINGLE NEXT BOUNDED TD-01 EVIDENCE-GAP CLASS SHOULD BE PRIORITIZED WITHOUT ACQUIRING OR TESTING A LOCATOR?`

This task does not authorize architecture selection.

This task does not authorize any new locator acquisition/intake.

This task does not authorize any new technical-content read.

## 5. Authorized disposition dimensions

### A. Established single-file host/native integration scope

The candidate may determine only whether Basis B establishes revision-scoped declarations about:

- a Flutter module reference used by the Android app build script;
- a Flutter AAR generation task;
- the declared AAR-generation command/input/output relationship;
- a declared `preBuild` -> `syncFlutterAar` task edge;
- debug/profile/release Flutter AAR dependency coordinates/source forms;
- declared Flutter executable/SDK path selection logic materially related to that task.

Do not infer successful execution, resolution, runtime launch, or complete build graph.

### B. Remaining host/native integration fact gaps

The candidate may identify evidence classes only, including:

- settings-level repository registration / dependency-repository source establishing how the declared Flutter AAR coordinates are made resolvable;
- actual dependency resolution / artifact availability;
- successful AAR generation or build execution;
- runtime Android host/Flutter embedding evidence, if later needed, but do not prioritize it unless current bounded basis supports that priority;
- installed toolchain state and resolved dependency state as broader remaining TD-01 gaps.

Do not infer any exact settings filename or repository path.

### C. Architecture-entry sufficiency

Determine only whether current authorized TD-01 facts are sufficient for an architecture-entry determination.

Preserve exactly:

`FACT SUFFICIENCY DETERMINATION != ARCHITECTURE DESIGN`

`DECLARED HOST INTEGRATION != VERIFIED BUILD GRAPH EXECUTION`

`SINGLE FILE CONTENT FACT != COMPLETE HOST BUILD GRAPH`

### D. Next bounded evidence-gap priority

If more evidence is needed, select exactly ONE evidence-gap class.

Expected next priority:

`SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION`

Rationale boundary: the app-level build script now declares Flutter AAR generation and variant-specific coordinates, while settings-level repository registration / dependency source remains unestablished; without that evidence, the declared AAR coordinates are not yet connected to a complete declared Gradle resolution path.

This expected priority is an evidence-gap class only.

Preserve:

`EVIDENCE-GAP PRIORITY != LOCATOR`

`EVIDENCE-GAP PRIORITY != PERMISSION TO SEARCH`

`SETTINGS-LEVEL EVIDENCE CLASS != SETTINGS FILE LOCATOR`

Do not name, infer, normalize, guess, search for, or test a V10 repository path.

## 6. Expected Outcome A

If supported, use exactly:

`U-15 TD-01 HOST / NATIVE SINGLE-FILE FACT SUFFICIENCY / NEXT-GAP DISPOSITION = TD-01 REMAINS PARTIALLY DISCOVERED BUT HOST / NATIVE BUILD INTEGRATION HAS MATERIALLY ADVANCED AT REVISION-SCOPED SINGLE-FILE DECLARATION LEVEL — FLUTTER MODULE REFERENCE, DECLARED FLUTTER SDK/EXECUTABLE SELECTION LOGIC RELEVANT TO AAR GENERATION, DECLARED FLUTTER AAR GENERATION TASK, DECLARED PREBUILD -> AAR-GENERATION TASK DEPENDENCY, DECLARED AAR OUTPUT LOCATION, AND DEBUG / PROFILE / RELEASE FLUTTER AAR DEPENDENCY COORDINATES ARE ESTABLISHED FOR THE EXACT AUTHORIZED SOURCE ONLY — SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION / DEPENDENCY-REPOSITORY SOURCE, ACTUAL AAR GENERATION SUCCESS, ACTUAL DEPENDENCY RESOLUTION / ARTIFACT AVAILABILITY, INSTALLED TOOLCHAIN STATE, AND BUILD-SUCCESS / WORKFLOW FACTS REMAIN UNESTABLISHED — CURRENT TD-01 FACTS ARE NOT YET SUFFICIENT FOR ARCHITECTURE-ENTRY DETERMINATION — NEXT PRIORITY BOUNDED TD-01 EVIDENCE-GAP CLASS = SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION — NO LOCATOR ACQUIRED OR AUTHORIZED — ZERO NEW TECHNICAL CONTENT READ — ZERO TARGET REREAD — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Outcome A means only:

- host/native integration is materially more discovered than before the single-file content read;
- app-level declarations establish an intended AAR generation/consumption integration chain at that exact revision+locator;
- settings-level repository registration / dependency source remains unestablished;
- actual artifact generation/resolution/build success remains unestablished;
- architecture-entry remains not yet determinable from current facts;
- the next evidence-gap class is `SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION`;
- no settings locator is declared, acquired, inferred, tested, searched for, or authorized;
- no technical design or implementation decision is made.

Preserve exactly:

`DECLARED AAR GENERATION TASK != AAR BUILD SUCCESS`

`DECLARED AAR DEPENDENCY != RESOLVED AAR DEPENDENCY`

`SINGLE FILE CONTENT FACT != SETTINGS-LEVEL REPOSITORY REGISTRATION`

`SINGLE FILE CONTENT FACT != COMPLETE HOST BUILD GRAPH`

`PARTIALLY DISCOVERED != ARCHITECTURE-ENTRY SUFFICIENT`

`EVIDENCE-GAP PRIORITY != LOCATOR`

`EVIDENCE-GAP PRIORITY != PERMISSION TO SEARCH`

`SETTINGS-LEVEL EVIDENCE CLASS != SETTINGS FILE LOCATOR`

`FACT SUFFICIENCY DETERMINATION != ARCHITECTURE DESIGN`

## 7. Outcome B / operational blockage

If current durable basis does not support the expected settings-level repository-registration priority, use exactly:

`EXPECTED OUTCOME B — U-15 TD-01 HOST / NATIVE SINGLE-FILE FACT SUFFICIENCY / NEXT-GAP DISPOSITION DOES NOT SUPPORT THE EXPECTED SETTINGS-LEVEL FLUTTER AAR REPOSITORY REGISTRATION PRIORITY UNDER CURRENT AUTHORITY — FAIL CLOSED — NO LOCATOR ACQUIRED — ZERO NEW TECHNICAL CONTENT READ — ZERO TARGET REREAD — ZERO SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

For exact main/ref/blob/topology failure use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 HOST / NATIVE SINGLE-FILE FACT SUFFICIENCY / NEXT-GAP DISPOSITION DETERMINATION MADE`

If the RETRY review branch advances beyond this exact task-sheet commit before candidate publication, fail closed rather than rewriting or substituting history.

## 8. Mandatory zero activity

The candidate action MUST perform ZERO:

- target technical-content reread;
- `apps/android/app/build.gradle.kts` reread;
- settings-level path check/read;
- any other Gradle/native-build read;
- AndroidManifest/Kotlin/Java source read;
- `.metadata` reread;
- `pubspec.yaml` reread;
- `pubspec.lock` read;
- CI/workflow read;
- new locator acquisition/intake;
- TLA-01 execution;
- TLA-02 selection/import;
- existence/type checks;
- guessed conventional Gradle/settings path testing;
- repository-wide or directory-wide enumeration;
- repository-tree inspection;
- directory listing used for discovery;
- filename search;
- code search;
- GitHub code search;
- path discovery;
- source repository access;
- old-repository access;
- README use;
- FD02 use;
- technical design;
- implementation planning;
- implementation;
- participant/data activity;
- LC-03;
- LC-04;
- Phase 36.

## 9. Required candidate publication

For Outcome A or Outcome B create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_HOST_NATIVE_SINGLE_FILE_SUFFICIENCY_AND_NEXT_GAP_DISPOSITION_GOVERNANCE_RETRY_REVIEW_V0_1.md`

Candidate sole parent must be this exact RETRY task-sheet commit.

Relative to the task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_HOST_NATIVE_SINGLE_FILE_SUFFICIENCY_AND_NEXT_GAP_DISPOSITION_GOVERNANCE_RETRY_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: review H-01 BIB-03 U-15 TD-01 host native single-file sufficiency disposition retry`

Publish non-force only to:

`review/h01-bib03-u15-td01-host-native-single-file-sufficiency-next-gap-disposition-governance-retry-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent ACCEPT/REJECT.

## 10. Strict phase separation and future restart gate

RETRY task-sheet authoring -> STOP.

Candidate sufficiency / next-gap disposition -> STOP.

Fresh GPT L3 ACCEPT/REJECT -> STOP.

If accepted, normal non-force promotion -> separate acceptance action.

Only after durable Outcome A may a settings-level exact locator be supplied or governed through TLA-01/TLA-02.

No settings-level content read may occur until locator intake and a later exact existence task separately succeed.

Architecture-entry determination -> separately authorized later governance task.

Technical design -> separately authorized later task.

Implementation planning / implementation -> separately authorized later task.

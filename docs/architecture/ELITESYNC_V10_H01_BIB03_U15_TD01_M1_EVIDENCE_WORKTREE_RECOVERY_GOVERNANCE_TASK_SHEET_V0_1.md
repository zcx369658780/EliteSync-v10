# EliteSync v10 H-01 BIB-03 U-15 TD-01 M1 Evidence-Worktree Recovery Governance Task Sheet v0.1

Status: `PROPOSED — TD-01 M1 EVIDENCE-WORKTREE RECOVERY GOVERNANCE ONLY — SELECT RESTART-GATE INPUT AFTER REGISTERED refs/heads/main WORKTREE COUNT = 0 — ZERO LOCAL WORKTREE MUTATION — ZERO WORKTREE METADATA REREAD — ZERO ARTIFACT PROBE — ZERO ARTIFACT EXISTENCE/TYPE/CONTENT/HASH CHECK — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO GRADLE/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Review branch:

`review/h01-bib03-u15-td01-m1-evidence-worktree-recovery-governance-v0-1`

Exact durable live-main authority required at task start:

`2f56a9c1b0cdda16170076241a059104fd15af8d`

This task authorizes one later fresh GPT L3/Codex governance candidate action for exactly one purpose: determine the narrowest safe restart-gate input after the accepted M1 environment/method governance result found zero registered worktree entries whose branch record was exactly `refs/heads/main`, without rereading local worktree metadata, auto-selecting any other registered worktree, creating/switching/registering/removing a worktree, inspecting protected staged state, probing the artifact, or making technical-design or implementation decisions.

This task does NOT execute M1.

This task does NOT establish artifact presence or absence.

This task does NOT authorize local worktree mutation.

## 1. Permanent governance boundaries

Global/cross-project memory is NOT EliteSync-v10 governance authority.

Do not reconstruct, import, infer from, or rely on legacy EliteSync project memory as V10 governance authority.

Preserve exactly:

`NO REPOSITORY-WIDE OR DIRECTORY-WIDE FILE/PATH ENUMERATION IS AUTHORIZED.`

README documentary-read budget remains exhausted.

FD02 remains permanently excluded.

No source repository access, no old `D:\EliteSync` access, no source-content read, no new V10 technical-target read, no generated-output inspection, no Maven-directory inspection, no cache inspection, no Gradle/build execution, no dependency-resolution execution, no repository/network artifact test, no technical design, no implementation planning, no implementation, no LC-03, no LC-04, and no Phase 36 are authorized.

Protected unrelated local staged state remains protected. Do NOT inspect, modify, unstage, overwrite, discard, reset, clean, commit, cite, summarize, or use it.

Do NOT use `git status`, `git status --short`, untracked checks, directory listing, cache listing, generated-output listing, filename search, code search, GitHub code search, repository-tree inspection, or filesystem discovery.

Historical incidents, rejected candidates, stale tasks, and deviations remain historical truth and must not be rewritten as if they did not occur.

## 2. Exact durable basis A — accepted environment/method Outcome B

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_EXACT_LOCAL_ARTIFACT_AVAILABILITY_PROBE_ENVIRONMENT_AND_METHOD_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`5300d8fe445a17baa88fd35e834cca34dbf9ff0a`

Preserve exactly from Basis A:

- exactly one authorized `git worktree list --porcelain` metadata read occurred;
- registered entries whose branch record was exactly `refs/heads/main` = `0`;
- no `M1_EVIDENCE_WORKTREE_ROOT` was frozen;
- no future absolute target was constructed;
- no `Test-Path -LiteralPath` method was frozen or executed;
- artifact existence/type/content/hash checks remained `0/0/0/0`;
- generated-output / Maven-directory / cache inspections remained `0/0/0`;
- staged/unstaged/untracked-state inspections or use remained `0`;
- the zero count was only a registered-worktree metadata result and did not establish artifact absence.

Preserve exactly:

`REGISTERED MAIN WORKTREE ROOT != CLEAN WORKTREE`

`REGISTERED MAIN WORKTREE ROOT != CURRENT ARTIFACT PRESENCE`

`WORKTREE METADATA READ != REPOSITORY FILE/PATH ENUMERATION`

`WORKTREE ROOT SELECTION != ARTIFACT PROBE`

`LOCAL WORKTREE HEAD != REMOTE MAIN UNLESS SEPARATELY VERIFIED`

`PROTECTED STAGED STATE != ARTIFACT EVIDENCE`

Additionally preserve:

`REGISTERED refs/heads/main WORKTREE COUNT = 0 != NO ELITESYNC-V10 WORKTREE EXISTS`

`REGISTERED refs/heads/main WORKTREE COUNT = 0 != ARTIFACT ABSENCE`

`REGISTERED refs/heads/main WORKTREE COUNT = 0 != PERMISSION TO AUTO-SELECT ANOTHER WORKTREE`

## 3. Exact durable basis B — accepted M1 artifact locator

Path:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_L3_ARTIFACT_LOCATOR_MATERIALIZATION_GOVERNANCE_REVIEW_V0_1.md`

Required blob:

`b80d0d05065003ccd1585d4e3d2f7a00286dca3f`

Preserve exactly:

`apps/flutter_elitesync_module/build/host/outputs/repo/com/elitesync/flutter_elitesync_module/flutter_release/1.0/flutter_release-1.0.aar`

This remains the sole materialized repository-relative local generated-artifact locator for the selected M1 lane.

Preserve exactly:

`MATERIALIZED ARTIFACT LOCATOR != VERIFIED ARTIFACT PRESENCE`

`MATERIALIZED ARTIFACT LOCATOR != ARTIFACT FRESHNESS`

`MATERIALIZED ARTIFACT LOCATOR != BUILD SUCCESS`

`MATERIALIZED ARTIFACT LOCATOR != GRADLE DEPENDENCY-RESOLUTION SUCCESS`

`MATERIALIZED ARTIFACT LOCATOR != M1 EXECUTION AUTHORITY`

`LOCAL GENERATED-ARTIFACT LOCATOR != GIT-TRACKED PATH CLAIM`

## 4. Exact substantive V10 repository-content read budget

Exactly THREE substantive EliteSync-v10 repository-content reads are authorized for the later candidate action:

1. this exact task sheet;
2. Basis A at required blob `5300d8fe445a17baa88fd35e834cca34dbf9ff0a`;
3. Basis B at required blob `b80d0d05065003ccd1585d4e3d2f7a00286dca3f`.

No fourth substantive EliteSync-v10 repository-content read is authorized.

Do NOT reread any V10 technical target, external web documentation, rejected candidate, old/source repository content, README, FD02, generated output, Maven metadata, `.aar`, `.pom`, checksum, cache, or build-output content.

Minimum exact GitHub ref/commit/compare metadata needed to verify authority/topology is allowed only if it does not become repository-tree inspection, path discovery, or content inspection.

## 5. No local metadata or mutation action in this task

This governance action MUST NOT rerun:

`git worktree list --porcelain`

It MUST NOT use any other worktree-listing, directory-listing, filesystem-enumeration, branch-switching, worktree-creation, worktree-removal, worktree-repair, or worktree-registration command.

No local path may be probed, canonicalized by filesystem access, or inferred from prior unauthorized output.

This task determines only the next restart-gate INPUT TYPE.

## 6. Recovery routes permitted for governance comparison only

### Route R1 — Owner-supplied exact existing local evidence-worktree root

Definition:

- Owner explicitly supplies one exact absolute local path string intended to identify the existing EliteSync-v10 worktree/root whose local generated-artifact state should be evaluated by the future M1 lane;
- the literal root string may be accepted only in a later separately governed intake task;
- Owner supply alone does not establish that the path exists, is registered as a Git worktree, points to EliteSync-v10, has any branch/HEAD identity, is clean, or contains the artifact;
- later root-binding validation and M1 execution remain separate phases.

Preserve exactly:

`OWNER-SUPPLIED EVIDENCE ROOT != VERIFIED FILESYSTEM PATH`

`OWNER-SUPPLIED EVIDENCE ROOT != VERIFIED ELITESYNC-V10 WORKTREE`

`OWNER-SUPPLIED EVIDENCE ROOT != refs/heads/main WORKTREE`

`OWNER-SUPPLIED EVIDENCE ROOT != CLEAN WORKTREE`

`OWNER-SUPPLIED EVIDENCE ROOT != ARTIFACT PRESENCE`

`ROOT INTAKE != ROOT VALIDATION`

`ROOT VALIDATION != ARTIFACT PROBE`

### Route R2 — automatically select another registered non-main worktree from worktree metadata

This route is NOT authorized under current durable basis.

Reason boundary: Basis A intentionally used only the exact `refs/heads/main` count for its determination and did not authorize other registered worktree metadata as substantive evidence for selecting an artifact evidence state. This task must not silently broaden that scope.

Preserve:

`OTHER REGISTERED WORKTREE METADATA != AUTHORIZED EVIDENCE ROOT`

`REGISTERED WORKTREE != OWNER-SELECTED ARTIFACT EVIDENCE STATE`

### Route R3 — create, switch, attach, detach, repair, or register a worktree

This route is NOT authorized by this task.

Any local worktree mutation requires a later separately authored governance action and must separately address protected staged state and whether a newly created/changed worktree would represent the intended local generated-artifact evidence state.

Preserve:

`WORKTREE RECOVERY GOVERNANCE != WORKTREE MUTATION AUTHORITY`

`NEW WORKTREE != PREEXISTING GENERATED-ARTIFACT STATE`

No command to implement R3 may be selected or authorized here.

## 7. Sole recovery-governance question

Determine ONLY:

`AFTER THE ACCEPTED ENVIRONMENT/METHOD GOVERNANCE FOUND ZERO REGISTERED refs/heads/main WORKTREE ENTRIES, WHAT SINGLE NARROWEST SAFE RESTART-GATE INPUT SHOULD BE REQUIRED BEFORE ANY FURTHER LOCAL EVIDENCE-ROOT BINDING OR M1 ARTIFACT PROBE, WITHOUT AUTO-SELECTING ANOTHER WORKTREE OR MUTATING LOCAL WORKTREE STATE?`

Expected next restart gate, if supported:

`OWNER-SUPPLIED EXACT EXISTING LOCAL ELITESYNC-V10 EVIDENCE-WORKTREE ROOT`

This task does not receive or validate that root.

## 8. Required determination principles

The candidate MUST apply all of the following:

1. Treat Basis A Outcome B as an environment-binding failure only.
2. Do not infer that no EliteSync-v10 worktree exists.
3. Do not infer artifact absence.
4. Do not use or reproduce metadata for other worktrees.
5. Do not auto-select a worktree from branch name, path memory, common local layout, or prior unrelated task context.
6. Do not create/switch/register/repair/remove a worktree.
7. Prefer explicit Owner selection of the exact local evidence root before any later binding validation.
8. Preserve strict separation: Owner root input -> later root-intake/binding validation -> later one-shot M1 execution.

## 9. Required Outcome A classification

If the exact durable basis supports R1 as the narrowest safe restart gate, record exactly and verbatim:

`U-15 TD-01 M1 EVIDENCE-WORKTREE RECOVERY GOVERNANCE = ACCEPTED ENVIRONMENT/METHOD OUTCOME B ESTABLISHES ONLY THAT ZERO REGISTERED WORKTREE ENTRIES HAD branch refs/heads/main IN THE ONE AUTHORIZED LOCAL WORKTREE METADATA READ — THIS DOES NOT ESTABLISH THAT NO ELITESYNC-V10 WORKTREE EXISTS AND DOES NOT ESTABLISH ARTIFACT ABSENCE — CURRENT AUTHORITY DOES NOT PERMIT AUTO-SELECTION OF ANOTHER REGISTERED WORKTREE AND DOES NOT PERMIT WORKTREE CREATION / SWITCH / ATTACH / DETACH / REPAIR / REGISTRATION — NEXT RESTART GATE = OWNER-SUPPLIED EXACT EXISTING LOCAL ELITESYNC-V10 EVIDENCE-WORKTREE ROOT INTENDED TO REPRESENT THE LOCAL GENERATED-ARTIFACT STATE FOR THE M1 LANE — OWNER ROOT SUPPLY WILL REQUIRE A LATER SEPARATELY GOVERNED INTAKE / ROOT-BINDING VALIDATION BEFORE ANY ARTIFACT PROBE — NO ROOT SUPPLIED / VALIDATED / AUTHORIZED NOW — ZERO LOCAL WORKTREE METADATA REREAD — ZERO WORKTREE MUTATION — ZERO ARTIFACT PROBE — ZERO ARTIFACT EXISTENCE/TYPE/CONTENT/HASH CHECK — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO Gradle/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

Do not shorten, paraphrase, translate, split, normalize, or semantically rewrite this classification.

Outcome A means only:

- current M1 environment binding remains blocked;
- the next required input is one Owner-supplied exact existing local EliteSync-v10 evidence-worktree root string;
- no root is supplied, validated, or authorized by this action;
- no filesystem or worktree command is executed;
- no artifact availability result exists;
- later root intake/binding validation and later M1 execution each require separate tasks.

## 10. Outcome B / operational blockage

If the current durable basis does not support R1 as the next restart gate, classify exactly:

`EXPECTED OUTCOME B — U-15 TD-01 M1 EVIDENCE-WORKTREE RECOVERY GOVERNANCE DOES NOT ESTABLISH ONE SAFE RESTART-GATE INPUT TYPE UNDER CURRENT AUTHORITY — FAIL CLOSED — NO ROOT SUPPLIED / VALIDATED / AUTHORIZED — ZERO LOCAL WORKTREE METADATA REREAD — ZERO WORKTREE MUTATION — ZERO ARTIFACT PROBE — ZERO ARTIFACT EXISTENCE/TYPE/CONTENT/HASH CHECK — ZERO GENERATED-OUTPUT / MAVEN-DIRECTORY / CACHE INSPECTION — ZERO Gradle/BUILD/DEPENDENCY-RESOLUTION EXECUTION — ZERO NETWORK/REPOSITORY ARTIFACT TEST — ZERO NEW V10 TECHNICAL TARGET READ — ZERO REPOSITORY SEARCH/ENUMERATION/PATH DISCOVERY — ZERO TECHNICAL DESIGN — ZERO IMPLEMENTATION`

For exact main/ref/blob/topology failure before determination use only:

`OPERATIONAL BLOCKED — NO U-15 TD-01 M1 EVIDENCE-WORKTREE RECOVERY GOVERNANCE DETERMINATION MADE`

If `main` is not exactly `2f56a9c1b0cdda16170076241a059104fd15af8d` at task start, STOP before substantive content read except minimum ref metadata needed to detect mismatch.

If the review branch advances beyond this exact task-sheet commit before candidate publication, fail closed rather than rewriting or substituting history.

## 11. Mandatory zero activity

The candidate action MUST perform ZERO:

- `git worktree list --porcelain` reruns;
- any other worktree-listing command;
- worktree create/add/remove/move/repair/lock/unlock operations;
- branch checkout/switch/attach/detach operations;
- `Test-Path` or any filesystem target probe;
- artifact existence/type/content/hash checks;
- generated-output inspection;
- Maven-directory inspection;
- cache inspection;
- directory listing used for discovery;
- wildcard/glob testing;
- filename search;
- code search;
- GitHub code search;
- repository-tree inspection;
- repository-wide or directory-wide enumeration;
- path discovery;
- `git status` / `git status --short` / untracked checks;
- staged/unstaged diff inspection of protected local state;
- Gradle/build execution;
- dependency-resolution execution;
- `syncFlutterAar` execution;
- `flutter build aar` execution;
- network/repository reachability tests;
- artifact downloads/uploads;
- V10 technical-target reads;
- external web research;
- source repository access;
- old-repository access;
- README use;
- FD02 use;
- architecture selection;
- technical design;
- implementation planning;
- implementation;
- LC-03;
- LC-04;
- Phase 36.

## 12. Required candidate publication

For Outcome A or Outcome B create exactly:

`docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_EVIDENCE_WORKTREE_RECOVERY_GOVERNANCE_REVIEW_V0_1.md`

Candidate sole parent must be this exact task-sheet commit.

Relative to this task-sheet commit require:

- `ahead_by = 1`;
- `behind_by = 0`;
- changed-path count = `1`;
- sole changed path = `docs/architecture/ELITESYNC_V10_H01_BIB03_U15_TD01_M1_EVIDENCE_WORKTREE_RECOVERY_GOVERNANCE_REVIEW_V0_1.md`.

Required candidate commit subject:

`docs: govern H-01 BIB-03 U-15 TD-01 M1 evidence worktree recovery`

Publish non-force only to:

`review/h01-bib03-u15-td01-m1-evidence-worktree-recovery-governance-v0-1`

Do NOT promote `main` in the candidate action.

After candidate publication, STOP for fresh GPT L3 independent `ACCEPT` / `REJECT`.

## 13. Strict phase separation

Task-sheet authoring -> STOP.

Candidate recovery governance -> STOP.

Fresh GPT L3 ACCEPT/REJECT -> STOP.

If accepted, normal non-force promotion -> separate acceptance action.

Owner supplies exact evidence-worktree root -> separate external restart input.

Root intake/binding validation -> separately authored task only.

M1 one-shot exact filesystem existence execution -> separately authored later task only.
